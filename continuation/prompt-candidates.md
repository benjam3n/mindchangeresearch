Intended mind change: Decide whether the reviewed PBTC, MRC, and MTCG results justify changing the reusable research prompt.

Actual starting judgment: One precise addition could be warranted if it blocks a failure still permitted by the current prompt. More instruction is not beneficial by itself, and `methods/improved-research-prompt.txt` already requires fixed inputs and criteria, supported effects, distinct later uptake, future change selection, preserved boundaries, and zero new credit for repeated findings.

Actor and scope: The current model compares candidate prompt additions against the saved prompt and the completed local records. This is documentary prompt evaluation, not a prospective trial of future authors, a claim about model weights, or evidence of human benefit. The active prompt is not modified.

Original SP source: `methods/methods-finish-sp.original.md`; separate requirements: `methods/methods-finish-sp.requirements.txt`. Fresh reader streams match the retained files. SP defines no numerical 8x floor. The current prompt is META + GOAL + METHOD + EXPLORATORY and failure-prone; its relevant components are already unbundled below.

## Candidate comparison

| Candidate addition | Motivating record | Current-prompt coverage | Distinct failure blocked | Demonstrated advantage | Disposition |
|---|---|---|---|---|---|
| “Verify everything carefully.” | General semantic review | Existing requirements demand concrete products, evidence, boundary tests, and repairs | None; the wording supplies no operation or success condition | None | REJECT |
| “Performing differs from preparing.” | A story draft once weakened `sends` to `prepares to send` | Existing prompt fixes input and criterion and separates performed operations from unperformed interventions | The story defect is already rejected by the current criterion | No unseen prompt execution shows an additional gain | REJECT |
| “Let a result change the next question.” | MRC-03 shifted from a seed trace to image classification after the 15-cycle appeared | Existing prompt includes future change selection and distinct later operations; original MRC Q1/Q3 already require goal/progress revision | No failure under literal current-prompt compliance is exhibited | MRC-03 succeeded without the addition | REJECT |
| “Every support probe must be labeled fresh or repeated.” | MRC-02 mislabeled the same nonred-square observation as a holdout in all three oracle runs | Existing prompt already says repeated results earn no new KEEP credit and requires preserved concrete input | It would make one terminology error harder, but the error was repaired locally without changing the central verdict | No comparison shows better future execution; adding every local schema field centrally would overfit | REJECT |
| “Store the active control set with every reachability formula.” | MTCG-02's two-control formula fails when B is removed | Existing prompt requires actor, scope, concrete input, preserved conditions, and boundary reuse | The local record now carries the guard; no current-prompt-compliant unguarded KEEP has survived review | No prospective difference demonstrated | REJECT |
| “State the sampling mechanism before every probability.” | MTCG-03 distinguishes combined-token `1/3` from equal-source `9/11` | Existing prompt already requires concrete input, source, supported criteria, and boundary comparison | Useful domain-specific rule, but not a general research-prompt omission | No future case comparison under old versus new prompt | REJECT as central addition; retain in MTCG-03 |

## SP products

TYPE: META + GOAL + METHOD + EXPLORATORY; compound and failure-prone.

QUESTIONS FOUND:

1. Does any candidate preserve the program's intent?
2. Does it add a missing criterion rather than restate an existing one?
3. Is there a concrete current-prompt-compliant failure that the candidate blocks?
4. Has the candidate shown an advantage on a distinct later operation?

MISSING CRITERIA: None established by these records. A future addition needs a fixed comparison where the current prompt permits a harmful or useless result and the added clause changes that result without causing a new loss.

FAILURE MODES:

- A candidate can name a virtue such as care without specifying a discriminating operation; all outputs above reject that as an improvement.
- A candidate can restate an existing constraint in narrower vocabulary and be credited because its motivating defect is vivid; the coverage column blocks that substitution.
- A local field can be promoted into the central prompt after one repair; the comparison retains local guards in their records unless cross-case advantage is demonstrated.

IMPROVED PROMPT: No replacement is warranted. `methods/improved-research-prompt.txt` remains byte-for-byte outside this evaluation.

SUGGESTED SKILL ROUTING: Retain `/sp` for a future prompt comparison only after a concrete failure survives literal compliance with the current prompt. PBTC's recursive audits remain documentary CLEAN with historical reversal responsiveness UNCLEAR; they do not provide prospective authorship evidence or new KEEP credit.

Verdict certificate: Exact claim—none of the six additions has a demonstrated advantage over the current reusable prompt. Decisive premise—each addition either lacks an operation, repeats an existing criterion, or remains a local rule with no old/new prompt comparison. Inference—SP's “add what's missing, don't add what's not needed” rule returns the prompt unchanged. Strongest contrary branch—the probe, control-set, and sampling clauses are concrete and useful in their own records; their utility is retained locally but does not establish central-prompt improvement. Unresolved dependency—a prospective unseen execution could show a failure not blocked by the present text.

Actual mind change: No central prompt addition was adopted. The evaluation became stricter about distinguishing a useful local repair from a demonstrated reusable-prompt advantage.

Benefit: The current prompt remains concise while the MRC probe correction and MTCG guards remain inspectable at the exact claims they govern.

Verdict: REJECT all six as demonstrated prompt improvements.

Organization: Candidate → motivating evidence → current coverage → distinct blocked failure → demonstrated advantage. This preserves why a local clause can be true and useful without being a warranted global instruction.

Next attempts: Freeze an unseen task before running the current prompt; find a harmful result that satisfies every present clause; compare current and candidate prompts on the same input and criterion; test whether a concrete example outperforms another abstract rule.
