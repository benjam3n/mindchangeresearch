Intended mind change: Determine whether the remaining failure is missing instructions, missing work, or a mechanism that accepts evidence of one thing as evidence of another.

Current-state update: while this audit was running, commit `c419b119ba8a18258ef55a97da02d158e543b0fa` superseded baseline `07e6af64c65fb682305659b2ec830e21743c0f15`. The current 1,747-file tree was acquired and every blob verified; its 1,732-file checkpoint reconstructs to SHA-256 `de280269cfb40b8eadcafa8b6a4b8c35d1d9ed5928e2ac7d662733f443110ff1`, with exact manifest coverage. The baseline audit below remains evidence about that snapshot. Current changes are assessed at the end and in mind-current-audit.md.

The pinned Mind Change Research repository is `benjam3n/mindchangeresearch` at `07e6af64c65fb682305659b2ec830e21743c0f15`. Its 54 Git blobs were retrieved and checked against their Git blob hashes. Eleven binary parts reconstruct the advertised ZIP exactly: SHA-256 `7ba425b197be8d38ba9715873464dd0ba2eca43e2da3aeb985ab5ffda83fba6b`. The ZIP contains 1,465 files, passes its integrity test, and all 1,464 manifest-listed files match their SHA-256 hashes. This examination actually completes the recoverability and extraction verification still described prospectively in README.md, repository-sync.md, and checkpoint-parts/README.md. Their future-tense wording alone does not prove that earlier verification never happened elsewhere.

The root controls accurately distinguish 233 initial applications reported complete within their scopes, 66 partial, and one blocked. They explicitly deny that these statuses certify semantic completion of every original procedure, human benefit, or global optimality. These qualifications are substantive counterevidence against an indiscriminate accusation that every future instruction is a fabricated result. The initial quota and per-file reflection requirements came from the user's preserved request; fulfilling that exposure schedule is a real obligation, but it cannot establish adequacy for the larger mind-change goal.

There is a mechanically demonstrated acceptance vulnerability in `merge_reviewed_proposals.py:11–30` inside the archive. Any noncanonical status beginning with `complete` becomes `complete`. If it has missing requirements, the normalizer appends their text to depth_status, calls them external or semantic limits, and empties missing_requirements. The operation does not inspect whether a missing item is an actual unperformed procedure stage. In an isolated execution of the function, a synthetic row with status `complete_unverified` and missing requirement `Derive the central conclusion; no derivation has been performed.` became complete with an empty missing-requirements array. The missing text survived only as an alleged effect limit. No original ledger was changed. `mind-normalization-counterexample.json` contains the full before and after.

The logical defect is precise: a declaration of completion determines the category of the missing work. The missing work does not determine whether completion is warranted. A hard gate later requiring complete rows to have empty missing_requirements cannot repair this because the normalizer manufactures that condition. `finalize_integration.py:70–75` checks the expected totals, record existence, and missing-array consistency; it does not establish that the emptied item was irrelevant to procedure completion.

Five preserved proposal rows actually use this conversion: PCD 1 and 2, PCI 1 and 2, ITERATE 1 in `continuation/specs-proposal.json`. These are not five demonstrated false completions. The exact PCD original asks for discovering and specifying procedures, not executing every human procedure discovered. PCI and ITERATE likewise preserve distinctions between completed local work and unobserved effect. The original-procedure scope can justify completion in these cases. The counterexample proves that the code cannot make that distinction, not that every observed conversion was substantively wrong. The applicable review must compare the actual original obligation with the actual product; a status prefix is irrelevant evidence.

The central ledger itself adds `integration_review: not individually certified by root; see review records` to every allocated slot in `checkpoint.py`. It calls its status basis scoped reported statuses. Therefore 233 is an accounting result, not 233 independently confirmed successful reasoning episodes. A phrase such as complete must carry its referent: the source procedure, the specified example, the intended effect, or the whole research objective. These have different completion conditions. Conflating them inflates achievement without requiring a single false arithmetic count.

The index has 36 Markdown link occurrences whose targets are absent from the directly browsable repository tree but present in the verified archive. `mind-index-link-occurrences.json` enumerates every one with the index line, label, and target. The data is preserved; the direct navigation is not implemented. This is a qualified organizational failure, because the README discloses that the archive holds the historical corpus. Nevertheless, the index cannot perform its advertised direct selection function for these links until extraction or expansion occurs. Preservation and usable access are different achievements. The present audit did perform extraction and access, so no retrieval instruction is being passed to the reader as a substitute for this audit.

The only actual differences between directly exposed files and same-path archive copies are README.md, Mind_Change_Research_Index.md, and automation-prompt.txt. The README describes split reconstruction; the index substitutes relative links for older sandbox links; the prompt adds mandatory repository persistence. All other overlapping text, including cycle 03, is identical. This is versioned transport context, not evidence that the archival substantive findings disappeared.

Root-file coverage: README, working instruction, synchronization policy, both prompts, index, run/save state, allocation, central ledger, source integrity, checkpoint generator, proposal normalizer, integration finalizer, manifest, and reconstruction script were inspected directly or parsed in full. Source integrity and allocation records were checked as provenance and accounting, not counted as semantic findings merely because their fields are populated. Application-level conclusions and exact occurrences are in the two Mind Change Research component audits.

Actual mind change: The initial broad suspicion that incomplete stages had simply been called complete was narrowed by source-level checking. Some scoped completions are legitimate. The demonstrated defect is that the normalizer cannot distinguish those legitimate scope limits from an omitted central derivation, and the later structural checks inherit that mistake.

Benefit: The audit supplies an executed acceptance counterexample, verified corpus recovery, and all 36 direct-navigation failures. It does not claim a measured improvement in the user's mind or future model behavior.

Verdict: KEEP the demonstrated distinctions and counterexample. They change which statuses are accepted as evidence in this audit; they do not certify the source repository as repaired.

The newer commit was inspected before publication of this audit. README and the index now organize access by current subjects, contributors, construction, and supported findings; the previous 36 inaccessible links are no longer a live entry-view defect. The historical files are expanded. checkpoint.py imports build_mind_change rather than regenerating the old status-first index. The old two PASS claims concerning recipe implementation are explicitly replaced with UNRESOLVED and PARTIAL in the SPG record, and the change is propagated into the cycle ledger and integration review. The working instruction no longer requires the old recipe card as a universal initial sequence. These are actual repairs performed by the newer commit, not by this audit.

The 300 initial statuses remain 233 complete, 66 partial, one blocked; the new integration takes zero initial-allocation credit and zero distinct-finding credit. The root normalizer and integration finalizer are unchanged, so the isolated completion-prefix counterexample remains applicable. The EQI future recommendation, the generated positional judgments, and other unchanged application-level findings retain their original evidential force. The newer intent audit explicitly admits that most target contributors are not complete recipes; that honest candidate status prevents treating the new 616-target inventory as 616 fabricated interventions. It also leaves the underlying contribution-construction goal open wherever no actual operation is supplied. Current target/output and imported-history audits map those new files separately.
