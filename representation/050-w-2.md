Intended mind change: Replace position-dependent edit prose with a version-bound, atomic instruction whose target identity survives deletion and reordering.

# High-quality writing application — original-position edit

Actual starting judgment: “Delete the first item, then tag the second” appeared sufficient when read against `[A,B,C]`. It did not say whether the second selector was resolved before or after deletion.

Concrete input: `047-wre-2.md`, the list `[A:alpha,B:beta,C:gamma]`, reordered `[C,A,B]`, duplicate-B and noncommuting positional fixtures.

Actor and scope: This model authors and executes the instruction on stipulated lists. Human editor comprehension, speed, preference and target-selection accuracy remain unobserved.

Source: original `w`, interpretation 1, SHA-256 `996c29785f54fada9264fffca06a5855a0d2999ab4ceaf0fea289fb4179215da`. No numerical 8× floor exists; upstream reader criteria precede downstream text criteria.

## Reader and resolution path

WHO: an editor/system able to inspect stable IDs. NEED: perform the original-snapshot request without retargeting after deletion. UNRESOLVED: when and against which state are A and B resolved? STATE AFTER: A absent, B tagged, C unchanged, or no mutation on preflight failure.

The finite operation is resolvable. Whether a real editor cares or begins with this question unresolved is not observed.

| Step | Textual job | Local advancement | Removal result |
|---|---|---|---|
| S1 | Bind task version and original IDs. | fixes referents before mutation | removal permits stale-task or live-position readings |
| S2 | Preflight unique live matches. | selects one A and B or aborts | removal permits arbitrary duplicate selection |
| S3 | State unchanged-on-failure. | defines atomic failure | removal permits partial deletion |
| S4 | Apply delete A/tag B. | performs the request | removal gives no revision |
| S5 | Verify exact after-state and positional boundary. | closes and scopes the result | removal hides the C-tagged countercase |

Recognition: no editor recognition observed. Advancement: each step supplies a missing precondition or state transition. Momentum: the written sequence leaves a determinate next question until the verification line; subjective pull is unobserved. Non-skippability: every removal changes accepted inputs, mutation behavior or verification. Reader-drawn conclusion: B’s preserved identity makes the final state derivable before it is stated, but actual pre-arrival is unobserved.

Upstream verdict: PARTIAL for the same reason; symbolic resolvability and necessity pass, reader-dependent properties do not.

## Candidate and concrete interpretation checks

Revision target: original snapshot v1 with `A:alpha`, `B:beta`, `C:gamma`. Resolve original A and original B by ID before changing the list. Preflight: each ID must match exactly one live record and the request must match v1. If either ID is missing or duplicated, or the version differs, abort without mutation. Apply atomically: delete A; set `tag=true` on B. Verify: A absent, B present and tagged, C unchanged.

The same request on reordered `[C,A,B]` yields `[C,B(tagged)]`. If the request instead means live positions, say so: delete-current-first then tag-current-second on `[A,B,C]` tags C, not original B.

Checks: live positional execution yields `[B,C(tagged)]`; original ID execution yields `[B(tagged),C]`. Reordered IDs yield `[C,B(tagged)]`. Duplicate B yields two matches and aborts. Deleting A and tagging B commute under unique IDs; their positional analogues do not. The candidate uses every required distinction from 047 and makes the two interpretations produce visibly different after-states.

## Downstream diagnostic audit

Precise scope: original-snapshot language, version v1 and A≠B are explicit; no universal ID or commutativity claim. Voice: neutral operational register. Weak patterns: no “be clear,” persuasive buildup, defensive balance, generic aspiration, unnamed authority or content-stability inference. Claim markers: input/version and desired state `[O: 047-wre-2.md]`; after-states `[T: finish-fixture-results.json]`; atomic abort is a specified policy `[D: preflight before mutation]`. This is diagnostic because external upstream criteria remain open.

Revision: the final instruction adds the version precondition after the ID-only alternative proved insufficient for a changed task with the same IDs. It states exact failure behavior rather than treating “stable” as self-validating. Candidate word count: 94; T1 ceiling 120 passes.

Later use: Executed above on live-position, original-ID, reordered and duplicate-ID fixtures; the atomicity rule also distinguishes preflight failure from deleting A before discovering missing B.

Actual mind change: I changed the operative referent from current ordinal to original unique identity and added a version/unique-match preflight before mutation.

Benefit: One instruction now selects B rather than C on the initial fixture, survives the reorder, and aborts the duplicate case without partial mutation. No editor effect is observed.

Verdict: KEEP as later uptake of `047-wre-2.md`; repeated identity/scope findings receive no new independent credit. Overall W status remains PARTIAL.

Organization: Reader question → necessary step chain → final instruction → four interpretation checks → bounded diagnostic.

Next attempts: Test content-precondition failure separately from the now-executed version/missing-ID aborts; obtain real editor selections before claiming comprehension or accuracy.
