# Perspective modification

3,000 operations, arranged by the distinction or relation they change. An operation becomes part of a usable recipe only when its inputs, conditions, sequence, and consequences are supplied.

## Subject selection

- Replace the stated subject with what its description presupposes. (`PM0001`)
- Replace the named object with the activity sustaining it. (`PM0002`)
- Replace the event with the conditions permitting it. (`PM0003`)
- Replace the individual case with its class of cases. (`PM0004`)
- Replace the class with one case it obscures. (`PM0005`)
- Replace the product with the work required to produce it. (`PM0006`)
- Replace the failure with the expectation it violated. (`PM0007`)
- Replace the dispute with the distinction being disputed. (`PM0008`)
- Replace the answer with the question that made it relevant. (`PM0009`)
- Replace the problem with the process that identifies problems. (`PM0010`)
- Replace the decision with the available decision procedures. (`PM0011`)
- Replace the actor with the relationship between actors. (`PM0012`)
- Replace the resource with its competing possible uses. (`PM0013`)
- Replace the outcome with the alternatives it displaced. (`PM0014`)
- Replace the visible behavior with the situation eliciting it. (`PM0015`)
- Replace the rule with the cases it separates. (`PM0016`)
- Replace the exception with the rule it exposes. (`PM0017`)
- Replace the representation with what it cannot represent. (`PM0018`)
- Replace the argument with the commitment it seeks to change. (`PM0019`)
- Replace the immediate obstacle with the dependency creating it. (`PM0020`)
- Replace the solution with the problem it leaves unresolved. (`PM0021`)
- Replace the familiar subject with an incidental detail. (`PM0022`)
- Keep the subject fixed while changing the aspect considered. (`PM0023`)
- Combine two separately considered subjects into one inquiry. (`PM0024`)
- Split a compound subject into independently examinable subjects. (`PM0025`)

## Focal question

- Change whether something happens into how it happens. (`PM0026`)
- Change how something happens into why this mechanism operates. (`PM0027`)
- Change why something happens into what would prevent it. (`PM0028`)
- Change what something is into what it distinguishes. (`PM0029`)
- Change what something means into what follows from it. (`PM0030`)
- Change what should happen into who should decide. (`PM0031`)
- Change who is right into where their claims diverge. (`PM0032`)
- Change which option wins into which options are missing. (`PM0033`)
- Change how much exists into how it is distributed. (`PM0034`)
- Change what changed into what remained invariant. (`PM0035`)
- Change what caused the outcome into what sustained it. (`PM0036`)
- Change what is possible into what makes it possible. (`PM0037`)
- Change whether something works into under which conditions it works. (`PM0038`)
- Change how to improve into what improvement would sacrifice. (`PM0039`)
- Change what to do into what to stop doing. (`PM0040`)
- Change what is absent into how its absence is detected. (`PM0041`)
- Change what is known into what would change the answer. (`PM0042`)
- Change what to explain into what requires no explanation. (`PM0043`)
- Change what a statement says into what it excludes. (`PM0044`)
- Change what happens next into what continuations remain available. (`PM0045`)
- Replace a binary question with a question about degree. (`PM0046`)
- Replace a question about degree with a threshold question. (`PM0047`)
- Turn a requested ranking into a search for incomparability. (`PM0048`)
- Turn an apparent question into a challenge to its premise. (`PM0049`)
- Hold several incompatible questions open about the same matter. (`PM0050`)

## Problem formulation

- Recast a shortage as a mismatch between supply and use. (`PM0051`)
- Recast a defect as a failure to meet a particular standard. (`PM0052`)
- Recast a disagreement as incompatible definitions of the task. (`PM0053`)
- Recast an obstacle as evidence of a hidden dependency. (`PM0054`)
- Recast a delay as a sequencing problem. (`PM0055`)
- Recast overload as an admission problem. (`PM0056`)
- Recast inaction as unresolved competition among possible actions. (`PM0057`)
- Recast repeated failure as a defective feedback process. (`PM0058`)
- Recast confusion as a missing distinction. (`PM0059`)
- Recast inconsistency as a change in operating conditions. (`PM0060`)
- Recast an isolated mistake as a failure of detection. (`PM0061`)
- Recast wasted effort as work on the wrong target. (`PM0062`)
- Recast scarcity as restricted access to existing capacity. (`PM0063`)
- Recast poor performance as an inappropriate comparison. (`PM0064`)
- Recast a local problem as a transferred external cost. (`PM0065`)
- Recast an optimization problem as a feasibility problem. (`PM0066`)
- Recast a feasibility problem as a constraint-design problem. (`PM0067`)
- Recast prediction difficulty as insufficient observability. (`PM0068`)
- Recast coordination difficulty as incompatible incentives. (`PM0069`)
- Recast complexity as an unsuitable representation. (`PM0070`)
- Recast a missing capability as a missing activation condition. (`PM0071`)
- Recast resistance as protection of another valued outcome. (`PM0072`)
- Recast a solution requirement as an untested proposed means. (`PM0073`)
- Separate the reported symptom from the condition requiring change. (`PM0074`)
- Suspend the designation of a condition as a problem. (`PM0075`)

## Desired result

- Replace producing an answer with changing a specific understanding. (`PM0076`)
- Replace agreement with accurate identification of disagreement. (`PM0077`)
- Replace persuasion with enabling an informed independent decision. (`PM0078`)
- Replace immediate success with preserving future opportunities. (`PM0079`)
- Replace maximum output with minimum sufficient output. (`PM0080`)
- Replace elimination of uncertainty with decision-relevant uncertainty reduction. (`PM0081`)
- Replace finding a solution with making solutions discoverable. (`PM0082`)
- Replace performing an action with making it reliably repeatable. (`PM0083`)
- Replace a finished product with a capacity to revise it. (`PM0084`)
- Replace a local improvement with improvement across affected parties. (`PM0085`)
- Replace reaching a state with maintaining that state. (`PM0086`)
- Replace maintaining a state with enabling orderly departure. (`PM0087`)
- Replace avoiding failure with limiting its consequences. (`PM0088`)
- Replace choosing a winner with preserving valuable differences. (`PM0089`)
- Replace satisfying a request with addressing its underlying need. (`PM0090`)
- Replace the assumed beneficiary with another affected beneficiary. (`PM0091`)
- Replace an outcome target with a process constraint. (`PM0092`)
- Replace a process target with its intended outcome. (`PM0093`)
- Split one goal into independently assessable goals. (`PM0094`)
- Combine goals that depend on the same enabling condition. (`PM0095`)
- Make an implicit goal explicit and reconsider its priority. (`PM0096`)
- Treat the current goal as provisional during exploration. (`PM0097`)
- Allow the inquiry to discover a previously unspecified goal. (`PM0098`)
- Keep the desired result fixed while replacing its success indicator. (`PM0099`)
- Remove a goal that survives only through previous commitment. (`PM0100`)

## Relevance

- Make consequences for the stated goal determine relevance. (`PM0101`)
- Make consequences for excluded people determine relevance. (`PM0102`)
- Make reversibility determine which details receive consideration. (`PM0103`)
- Make uncertainty reduction determine which facts matter next. (`PM0104`)
- Make causal influence determine which variables enter the account. (`PM0105`)
- Make discrimination between alternatives determine evidential relevance. (`PM0106`)
- Make actionability determine which differences remain foregrounded. (`PM0107`)
- Make explanatory necessity determine which details are retained. (`PM0108`)
- Make recurrence determine which events deserve a general account. (`PM0109`)
- Make exceptional consequences override frequency in selecting relevant cases. (`PM0110`)
- Include information relevant only after a possible future change. (`PM0111`)
- Exclude information that cannot alter any available conclusion. (`PM0112`)
- Restore a detail dismissed because its use was unclear. (`PM0113`)
- Treat disagreement over relevance as part of the subject. (`PM0114`)
- Separate relevance to truth from relevance to persuasion. (`PM0115`)
- Separate relevance to understanding from relevance to execution. (`PM0116`)
- Separate relevance to selection from relevance to implementation. (`PM0117`)
- Separate relevance to diagnosis from relevance to repair. (`PM0118`)
- Restrict relevance to one explicitly chosen question. (`PM0119`)
- Extend relevance across interacting questions. (`PM0120`)
- Replace familiar relevance cues with independently chosen criteria. (`PM0121`)
- Consider a fact relevant because it defeats the framing. (`PM0122`)
- Consider an absence relevant because its presence was expected. (`PM0123`)
- Change relevance thresholds as the available alternatives change. (`PM0124`)
- Let an initially irrelevant observation redirect the inquiry. (`PM0125`)

## Attention

- Attend exclusively to differences between otherwise similar cases. (`PM0126`)
- Attend exclusively to similarities between apparently different cases. (`PM0127`)
- Track transitions instead of stable states. (`PM0128`)
- Track interruptions instead of uninterrupted activity. (`PM0129`)
- Track what receives no response. (`PM0130`)
- Track where effort accumulates without producing change. (`PM0131`)
- Track the first divergence between intention and execution. (`PM0132`)
- Track the last common condition before outcomes diverge. (`PM0133`)
- Follow one variable through every stage of a process. (`PM0134`)
- Follow one participant across changing situations. (`PM0135`)
- Follow one consequence backward through its dependencies. (`PM0136`)
- Alternate attention between the whole and one selected part. (`PM0137`)
- Distribute attention equally across initially competing interpretations. (`PM0138`)
- Concentrate attention on the least examined interpretation. (`PM0139`)
- Concentrate attention on what seems too obvious to inspect. (`PM0140`)
- Move attention from vivid details to consequential details. (`PM0141`)
- Move attention from central objects to their margins. (`PM0142`)
- Attend to the order in which details become noticeable. (`PM0143`)
- Attend to information discarded during initial reading. (`PM0144`)
- Attend to the effort required to maintain an interpretation. (`PM0145`)
- Attend to changes in your own response while observing. (`PM0146`)
- Keep attention on one unresolved distinction across distractions. (`PM0147`)
- Interrupt habitual attention whenever a chosen cue appears. (`PM0148`)
- Remove the usual attention cue and observe what becomes salient. (`PM0149`)
- Let attention roam without requiring immediate relevance. (`PM0150`)

## Salience

- Increase the prominence of a low-frequency severe consequence. (`PM0151`)
- Decrease the prominence of a vivid but unrepresentative case. (`PM0152`)
- Give an omitted stakeholder the same prominence as named stakeholders. (`PM0153`)
- Make an invisible dependency visually prominent. (`PM0154`)
- Make uncertainty as prominent as the estimated value. (`PM0155`)
- Make the baseline as prominent as the reported change. (`PM0156`)
- Make the rejected alternatives visible beside the selected option. (`PM0157`)
- Make accumulated small effects prominent beside isolated large effects. (`PM0158`)
- Make delayed consequences prominent beside immediate consequences. (`PM0159`)
- Make opportunity costs prominent beside direct expenditures. (`PM0160`)
- Make maintenance work prominent beside initial construction. (`PM0161`)
- Make failure recovery prominent beside ordinary operation. (`PM0162`)
- Make the source of a claim prominent beside its wording. (`PM0163`)
- Make missing observations prominent beside recorded observations. (`PM0164`)
- Make disagreement within a category more prominent than its label. (`PM0165`)
- Make distributional extremes prominent beside the average. (`PM0166`)
- Reduce prominence caused solely by presentation order. (`PM0167`)
- Reduce prominence caused solely by repetition. (`PM0168`)
- Reduce prominence caused solely by prestigious attribution. (`PM0169`)
- Reduce prominence caused solely by emotional intensity. (`PM0170`)
- Equalize the visual weight of competing explanations. (`PM0171`)
- Highlight cases where the preferred explanation performs poorly. (`PM0172`)
- Highlight changes too gradual to attract spontaneous notice. (`PM0173`)
- Highlight the boundary where the current distinction breaks down. (`PM0174`)
- Randomize initial prominence to disrupt habitual interpretation. (`PM0175`)

## Foreground and background

- Move an assumed background condition into the main account. (`PM0176`)
- Move the apparent main event into its surrounding routine. (`PM0177`)
- Treat the environment as active and the actor as responsive. (`PM0178`)
- Treat the actor as active and the environment as modifiable. (`PM0179`)
- Foreground the infrastructure supporting the visible activity. (`PM0180`)
- Foreground the unpaid work supporting the measured output. (`PM0181`)
- Foreground the selection process behind the displayed examples. (`PM0182`)
- Foreground the conventions making a statement intelligible. (`PM0183`)
- Foreground the alternatives hidden by the apparent default. (`PM0184`)
- Foreground the costs borne outside the represented system. (`PM0185`)
- Foreground the observer's position in constructing the description. (`PM0186`)
- Foreground the medium shaping what can be communicated. (`PM0187`)
- Foreground the pauses organizing a sequence of actions. (`PM0188`)
- Foreground absences within an apparently complete arrangement. (`PM0189`)
- Foreground the stable conditions enabling visible variation. (`PM0190`)
- Foreground variation concealed by a stable overall appearance. (`PM0191`)
- Background individual differences to expose shared situational pressures. (`PM0192`)
- Background shared conditions to expose individual differences. (`PM0193`)
- Exchange foreground and background repeatedly without settling either. (`PM0194`)
- Represent foreground and background as mutually determining. (`PM0195`)
- Remove the usual background and reinterpret the same event. (`PM0196`)
- Substitute a different background while preserving the focal event. (`PM0197`)
- Make the foreground-background distinction itself the object of attention. (`PM0198`)
- Allow several independent foregrounds within the same account. (`PM0199`)
- Eliminate a privileged foreground and represent only relationships. (`PM0200`)

## Inclusion

- Include cases excluded by the initial wording. (`PM0201`)
- Exclude cases included only through a misleading label. (`PM0202`)
- Include unsuccessful attempts alongside successful examples. (`PM0203`)
- Include nonparticipants alongside participants. (`PM0204`)
- Include people who left before measurement began. (`PM0205`)
- Include unchosen options in the decision account. (`PM0206`)
- Include expected events that did not occur. (`PM0207`)
- Include unrecorded maintenance activities. (`PM0208`)
- Include indirect consequences through intermediaries. (`PM0209`)
- Include effects occurring after the usual reporting period. (`PM0210`)
- Include preconditions normally treated as given. (`PM0211`)
- Include the costs of obtaining the evidence. (`PM0212`)
- Include the observer's interventions in the observed sequence. (`PM0213`)
- Include unknown cases without inventing their contents. (`PM0214`)
- Include borderline cases as a separate set. (`PM0215`)
- Include hypothetical cases that distinguish competing definitions. (`PM0216`)
- Restrict the account to directly observed events. (`PM0217`)
- Restrict the account to consequences for a selected participant. (`PM0218`)
- Restrict the account to what was knowable at the time. (`PM0219`)
- Restrict the account to changes attributable to one intervention. (`PM0220`)
- Replace automatic inclusion with an explicit membership rule. (`PM0221`)
- Replace automatic exclusion with an explicit exclusion test. (`PM0222`)
- Let inclusion vary with the question being examined. (`PM0223`)
- Retain disputed inclusions under multiple alternative boundaries. (`PM0224`)
- Include the consequences of choosing the inclusion rule. (`PM0225`)

## Boundaries

- Expand the system boundary to include upstream suppliers. (`PM0226`)
- Expand the system boundary to include downstream users. (`PM0227`)
- Expand the boundary to include displaced costs. (`PM0228`)
- Contract the boundary around one independently controllable process. (`PM0229`)
- Move the boundary to where decisions are actually made. (`PM0230`)
- Move the boundary to where consequences are actually experienced. (`PM0231`)
- Replace an administrative boundary with a causal boundary. (`PM0232`)
- Replace a physical boundary with a functional boundary. (`PM0233`)
- Replace a functional boundary with an ownership boundary. (`PM0234`)
- Replace a fixed boundary with one that changes over time. (`PM0235`)
- Replace a sharp boundary with graded membership. (`PM0236`)
- Replace one boundary with overlapping boundaries. (`PM0237`)
- Represent a boundary as an interaction surface. (`PM0238`)
- Represent a boundary as a rule for admitting information. (`PM0239`)
- Represent a boundary as a rule for assigning responsibility. (`PM0240`)
- Distinguish the observed boundary from the imposed analytical boundary. (`PM0241`)
- Separate boundaries for explanation, measurement, and intervention. (`PM0242`)
- Join regions whose interaction defeats separate analysis. (`PM0243`)
- Split a region whose internal differences matter operationally. (`PM0244`)
- Treat boundary crossings as the primary events. (`PM0245`)
- Count what crosses the boundary in both directions. (`PM0246`)
- Track changes produced solely by relocating the boundary. (`PM0247`)
- Remove the inside-outside distinction for a selected relationship. (`PM0248`)
- Represent nested systems without privileging one outer boundary. (`PM0249`)
- Make boundary selection a revisable part of the perspective. (`PM0250`)

## Spatial scale

- Examine the same arrangement at the scale of one component. (`PM0251`)
- Examine the arrangement at the scale of its containing system. (`PM0252`)
- Examine local variation hidden by a regional account. (`PM0253`)
- Examine regional regularities hidden by local detail. (`PM0254`)
- Replace distance traveled with territory made accessible. (`PM0255`)
- Replace total area with the arrangement of usable patches. (`PM0256`)
- Compare patterns across several spatial resolutions. (`PM0257`)
- Choose scale according to the reach of the causal mechanism. (`PM0258`)
- Choose scale according to the reach of an intervention. (`PM0259`)
- Choose scale according to the observer's sensing limits. (`PM0260`)
- Expand the field until displaced effects reenter the account. (`PM0261`)
- Contract the field until a previously averaged interaction becomes visible. (`PM0262`)
- Represent one location simultaneously at several spatial scales. (`PM0263`)
- Keep spatial scale fixed while changing the unit observed. (`PM0264`)
- Track a pattern as its observation scale changes. (`PM0265`)
- Treat scale-dependent differences as substantive rather than measurement noise. (`PM0266`)
- Separate effects of enlargement from effects of added surroundings. (`PM0267`)
- Express spatial extent relative to the actor's movement range. (`PM0268`)
- Express spatial extent relative to communication range. (`PM0269`)
- Express spatial extent relative to resource replenishment range. (`PM0270`)
- Replace uniform resolution with finer resolution near critical boundaries. (`PM0271`)
- Coarsen irrelevant regions while retaining detailed interaction zones. (`PM0272`)
- Compare equal-sized windows drawn from different surroundings. (`PM0273`)
- Compare differently sized windows centered on the same event. (`PM0274`)
- Treat the choice of spatial scale as an explanatory variable. (`PM0275`)

## Spatial position

- View the arrangement from the opposite physical side. (`PM0276`)
- View the arrangement from above. (`PM0277`)
- View the arrangement from within an ordinarily externalized component. (`PM0278`)
- Move the observation point to the system's entrance. (`PM0279`)
- Move the observation point to the system's exit. (`PM0280`)
- Move the observation point to the main bottleneck. (`PM0281`)
- Move the observation point to an isolated peripheral location. (`PM0282`)
- Move with the actor instead of observing from a fixed position. (`PM0283`)
- Observe from a fixed position instead of following the actor. (`PM0284`)
- Alternate between several physically separated observation points. (`PM0285`)
- Reconstruct the arrangement from a participant's line of sight. (`PM0286`)
- Reconstruct the arrangement from a location with obstructed visibility. (`PM0287`)
- Restrict observation to what lies within reach. (`PM0288`)
- Restrict observation to what lies along one travel route. (`PM0289`)
- Center the representation on a moving reference object. (`PM0290`)
- Center the representation on an otherwise neglected location. (`PM0291`)
- Remove the assumption that the observer occupies the center. (`PM0292`)
- Compare positions with equal distance but different access. (`PM0293`)
- Compare positions with equal access but different visibility. (`PM0294`)
- Observe the same event before and after crossing a boundary. (`PM0295`)
- View a familiar place from an unfamiliar entry point. (`PM0296`)
- Exchange the locations of observer and observed in a simulation. (`PM0297`)
- Observe through another participant's available instruments. (`PM0298`)
- Represent what each position systematically hides. (`PM0299`)
- Combine partial views while preserving where each originated. (`PM0300`)

## Spatial relations

- Replace straight-line distance with travel time. (`PM0301`)
- Replace travel time with effort required to traverse the route. (`PM0302`)
- Replace geometric proximity with frequency of interaction. (`PM0303`)
- Replace proximity with accessibility under actual restrictions. (`PM0304`)
- Distinguish adjacency from reachable connection. (`PM0305`)
- Distinguish containment from mere overlap. (`PM0306`)
- Distinguish a shared location from a shared functional space. (`PM0307`)
- Represent routes instead of endpoints. (`PM0308`)
- Represent barriers instead of open regions. (`PM0309`)
- Represent corridors instead of total available area. (`PM0310`)
- Represent how access changes with the direction of approach. (`PM0311`)
- Represent one-way access separately from two-way access. (`PM0312`)
- Replace absolute position with position relative to another object. (`PM0313`)
- Replace relative position with membership in a connected region. (`PM0314`)
- Treat an empty interval as separation with practical consequences. (`PM0315`)
- Treat co-location as competition for the same space. (`PM0316`)
- Treat distance as a delay in receiving consequences. (`PM0317`)
- Treat a border as a place of concentrated interaction. (`PM0318`)
- Distinguish physical isolation from informational isolation. (`PM0319`)
- Distinguish centralized geometry from centralized control. (`PM0320`)
- Collapse locations that are functionally interchangeable. (`PM0321`)
- Separate locations that appear geometrically identical but differ functionally. (`PM0322`)
- Represent paths that become available only under certain conditions. (`PM0323`)
- Represent space by the actions available at each position. (`PM0324`)
- Change the spatial relation while preserving the participating objects. (`PM0325`)

## Temporal scope

- Extend the horizon to include delayed consequences. (`PM0326`)
- Shorten the horizon to isolate the immediate transition. (`PM0327`)
- Include preparation before the conventionally defined beginning. (`PM0328`)
- Include maintenance after the conventionally defined completion. (`PM0329`)
- Include recovery after the apparent failure. (`PM0330`)
- Include eventual reversal of an apparent gain. (`PM0331`)
- Include the lifespan of the resources being consumed. (`PM0332`)
- Include the time required for affected parties to respond. (`PM0333`)
- Include consequences for people not yet participating. (`PM0334`)
- Restrict evaluation to the period when intervention remains possible. (`PM0335`)
- Compare conclusions across short, medium, and long horizons. (`PM0336`)
- Replace calendar duration with the number of relevant cycles. (`PM0337`)
- Replace one episode with the sequence of repeated episodes. (`PM0338`)
- Replace an entire history with a critical transition window. (`PM0339`)
- Extend observation until temporary fluctuations can be distinguished. (`PM0340`)
- Shorten observation to expose changes erased by long averaging. (`PM0341`)
- Use different horizons for costs and delayed benefits explicitly. (`PM0342`)
- Align the horizon with the mechanism's actual operating time. (`PM0343`)
- Align the horizon with the decision's irreversibility period. (`PM0344`)
- Allow the horizon to expand as dependencies are discovered. (`PM0345`)
- Treat the end of the observation period as a truncation. (`PM0346`)
- Distinguish unknown later effects from absent later effects. (`PM0347`)
- Represent consequences beyond the chosen horizon separately. (`PM0348`)
- Keep the horizon fixed while moving its start date. (`PM0349`)
- Make sensitivity to the chosen horizon part of the account. (`PM0350`)

## Temporal position

- Interpret the event using only information available beforehand. (`PM0351`)
- Interpret the event from the moment of commitment. (`PM0352`)
- Interpret the event from the moment of first consequence. (`PM0353`)
- Interpret the event from the moment of eventual recovery. (`PM0354`)
- Interpret the present from an imagined completed future. (`PM0355`)
- Interpret the present from an earlier abandoned expectation. (`PM0356`)
- Move the reference date to the first detectable warning. (`PM0357`)
- Move the reference date to the last reversible decision. (`PM0358`)
- Move the reference date to the onset of the underlying process. (`PM0359`)
- Move the reference date to the point of institutional recognition. (`PM0360`)
- Compare the same situation before and after learning its outcome. (`PM0361`)
- Reconstruct what seemed possible at each historical decision point. (`PM0362`)
- Distinguish a contemporary description from a retrospective reconstruction. (`PM0363`)
- Remove knowledge of later success when judging an earlier choice. (`PM0364`)
- Remove knowledge of later failure when judging an earlier choice. (`PM0365`)
- Treat the current arrangement as an earlier generation's future. (`PM0366`)
- Treat the current arrangement as a later generation's inheritance. (`PM0367`)
- Project today's categories backward and mark resulting mismatches. (`PM0368`)
- Apply an earlier category system to the present. (`PM0369`)
- Stand at a future deadline and reason backward to prerequisites. (`PM0370`)
- Stand before a change and preserve the then-open alternatives. (`PM0371`)
- Stand inside an ongoing transition without assuming its destination. (`PM0372`)
- View a familiar history from an intermediate stopping point. (`PM0373`)
- Compare judgments made at several points in the same sequence. (`PM0374`)
- Represent the observer's date as part of every claim. (`PM0375`)

## Temporal structure

- Replace a static snapshot with an ordered event sequence. (`PM0376`)
- Replace an event sequence with simultaneous interacting processes. (`PM0377`)
- Replace uniform time steps with steps at meaningful changes. (`PM0378`)
- Replace clock time with experienced waiting time. (`PM0379`)
- Replace duration with recurrence frequency. (`PM0380`)
- Replace frequency with spacing between occurrences. (`PM0381`)
- Distinguish continuous change from discrete jumps. (`PM0382`)
- Distinguish regular cycles from irregular repetition. (`PM0383`)
- Distinguish a delay from a permanent failure to respond. (`PM0384`)
- Distinguish preparation time from execution time. (`PM0385`)
- Distinguish active work from elapsed time. (`PM0386`)
- Distinguish reversible phases from irreversible transitions. (`PM0387`)
- Represent overlapping commitments as separate timelines. (`PM0388`)
- Represent dependencies that impose a required order. (`PM0389`)
- Represent events whose order can be exchanged. (`PM0390`)
- Reverse the explanatory traversal through a sequence. (`PM0391`)
- Compare the original order with a permuted order. (`PM0392`)
- Synchronize events by a shared trigger instead of calendar date. (`PM0393`)
- Align histories by developmental stage instead of elapsed time. (`PM0394`)
- Represent periods of inactivity as states with consequences. (`PM0395`)
- Treat timing relative to other processes as a variable. (`PM0396`)
- Treat acceleration as distinct from high speed. (`PM0397`)
- Treat slowing change as distinct from stability. (`PM0398`)
- Replace a single timeline with branching possible timelines. (`PM0399`)
- Represent the observation schedule alongside the observed process. (`PM0400`)

## Change and continuity

- Describe an object through what persists while its parts change. (`PM0401`)
- Describe apparent stability through compensating internal changes. (`PM0402`)
- Describe apparent change through a stable underlying relation. (`PM0403`)
- Separate a change in level from a change in rate. (`PM0404`)
- Separate a change in rate from a change in variability. (`PM0405`)
- Separate reversible fluctuation from durable transformation. (`PM0406`)
- Separate replacement of components from change of organization. (`PM0407`)
- Separate growth from expansion of the measurement boundary. (`PM0408`)
- Separate decline from transfer to an unmeasured location. (`PM0409`)
- Treat continuity as something actively maintained. (`PM0410`)
- Treat disruption as exposure of prior dependencies. (`PM0411`)
- Treat an unchanged result as the outcome of changed effort. (`PM0412`)
- Treat an unchanged action as different under changed conditions. (`PM0413`)
- Track the smallest change that alters the available response. (`PM0414`)
- Track changes too small individually to cross a threshold. (`PM0415`)
- Track cumulative drift across repeated minor revisions. (`PM0416`)
- Track discontinuities hidden by smooth aggregate measures. (`PM0417`)
- Track gradual transitions hidden by categorical labels. (`PM0418`)
- Compare what changes with what must remain invariant. (`PM0419`)
- Identify identity-preserving transformations of the represented object. (`PM0420`)
- Describe a transition by the capacities gained and lost. (`PM0421`)
- Describe a transition by the relations created and broken. (`PM0422`)
- Replace before-after comparison with a continuous transformation account. (`PM0423`)
- Replace continuity assumptions with separate accounts of each phase. (`PM0424`)
- Allow several incompatible criteria of continuity to coexist. (`PM0425`)

## Granularity

- Split one action into its decision-relevant subactions. (`PM0426`)
- Merge repeated microactions into a recurring routine. (`PM0427`)
- Split a category at the point where responses must differ. (`PM0428`)
- Merge categories requiring indistinguishable responses in the chosen domain. (`PM0429`)
- Distinguish individual events within an aggregate total. (`PM0430`)
- Distinguish individual participants within a group average. (`PM0431`)
- Distinguish components with different failure modes. (`PM0432`)
- Distinguish process stages with different controlling conditions. (`PM0433`)
- Combine details that share the same causal role. (`PM0434`)
- Combine intervals that contain no relevant state transition. (`PM0435`)
- Replace binary classification with several ordered levels. (`PM0436`)
- Replace several ordered levels with a continuous quantity. (`PM0437`)
- Replace a continuous quantity with action-relevant intervals. (`PM0438`)
- Use finer distinctions near a decision threshold. (`PM0439`)
- Use coarser distinctions where precision cannot change the decision. (`PM0440`)
- Represent some dimensions finely and others coarsely. (`PM0441`)
- Increase detail only where competing interpretations diverge. (`PM0442`)
- Decrease detail to expose a previously hidden regularity. (`PM0443`)
- Track which conclusions survive coarsening. (`PM0444`)
- Track which conclusions require a particular resolution. (`PM0445`)
- Separate measurement resolution from conceptual resolution. (`PM0446`)
- Separate representational detail from evidential support. (`PM0447`)
- Represent unresolved internal variation without pretending it is homogeneous. (`PM0448`)
- Refine a distinction using newly encountered counterexamples. (`PM0449`)
- Restore distinctions removed by an earlier summary. (`PM0450`)

## Abstraction

- Replace named individuals with the roles they occupy. (`PM0451`)
- Replace physical objects with their functional relations. (`PM0452`)
- Replace numerical values with their ordering. (`PM0453`)
- Replace an ordered relation with a simpler membership relation. (`PM0454`)
- Replace specific causes with a shared causal structure. (`PM0455`)
- Replace examples with the invariant relation they instantiate. (`PM0456`)
- Replace a procedure with its input-output relation. (`PM0457`)
- Replace a narrative with its pattern of dependencies. (`PM0458`)
- Replace an institution with its rules for allocating authority. (`PM0459`)
- Replace a conflict with the incompatible requirements producing it. (`PM0460`)
- Replace an object description with its permitted transformations. (`PM0461`)
- Replace a detailed model with its conserved quantities. (`PM0462`)
- Replace materials with capacities required by the task. (`PM0463`)
- Replace an interface with the actions it permits. (`PM0464`)
- Replace a history with the state variables needed for prediction. (`PM0465`)
- Abstract away labels while preserving relational structure. (`PM0466`)
- Abstract away location while preserving adjacency. (`PM0467`)
- Abstract away timing while preserving causal order. (`PM0468`)
- Abstract away magnitude while preserving sign. (`PM0469`)
- Abstract away mechanism while preserving observed constraints. (`PM0470`)
- Retain several abstractions of the same case simultaneously. (`PM0471`)
- Choose abstraction according to the operation to be performed. (`PM0472`)
- Mark distinctions lost during each abstraction step. (`PM0473`)
- Reject an abstraction when it merges operationally different cases. (`PM0474`)
- Apply abstraction to the process of constructing abstractions. (`PM0475`)

## Concreteness

- Replace a general claim with one fully specified case. (`PM0476`)
- Replace a role label with the person's actual available actions. (`PM0477`)
- Replace a goal with an observable state of affairs. (`PM0478`)
- Replace a value word with a choice between concrete alternatives. (`PM0479`)
- Replace a mechanism label with a sequence of interacting events. (`PM0480`)
- Replace a cost estimate with the activities consuming the resources. (`PM0481`)
- Replace a group description with contrasting members. (`PM0482`)
- Replace a trend statement with the observations composing it. (`PM0483`)
- Replace a procedure name with its executable steps. (`PM0484`)
- Replace a constraint label with the condition blocking an action. (`PM0485`)
- Replace an abstract risk with a specified failure path. (`PM0486`)
- Replace a proposed benefit with its recipient and occurrence conditions. (`PM0487`)
- Replace a relationship label with an actual exchange. (`PM0488`)
- Replace a quality judgment with the discriminations supporting it. (`PM0489`)
- Replace a capability claim with a demonstration task. (`PM0490`)
- Instantiate the same abstraction in two dissimilar settings. (`PM0491`)
- Instantiate a rule at its hardest boundary case. (`PM0492`)
- Instantiate a forecast as a dated observable event. (`PM0493`)
- Instantiate a counterfactual with the smallest specified intervention. (`PM0494`)
- Instantiate an ambiguous pronoun with each possible referent. (`PM0495`)
- Restore quantities omitted by a qualitative description. (`PM0496`)
- Restore actors omitted by passive phrasing. (`PM0497`)
- Restore timing omitted by a timeless statement. (`PM0498`)
- Restore local conditions omitted by a general recommendation. (`PM0499`)
- Translate an abstract disagreement into incompatible concrete predictions. (`PM0500`)

## Entity types

- Represent a thing as a recurring event. (`PM0501`)
- Represent an event as a transition between states. (`PM0502`)
- Represent a person as a participant in intersecting relationships. (`PM0503`)
- Represent a group as coordinated individual activities. (`PM0504`)
- Represent a group as an actor with collective capacities. (`PM0505`)
- Represent a resource as a temporarily available capacity. (`PM0506`)
- Represent a rule as a constraint on possible transitions. (`PM0507`)
- Represent a belief as a disposition to interpret and respond. (`PM0508`)
- Represent a preference as an ordering over alternatives. (`PM0509`)
- Represent a habit as a context-sensitive recurring process. (`PM0510`)
- Represent a boundary as an activity of inclusion and exclusion. (`PM0511`)
- Represent a tool as an extension of available operations. (`PM0512`)
- Represent a document as an intervention in another person's understanding. (`PM0513`)
- Represent an institution as maintained patterns of authority and expectation. (`PM0514`)
- Represent an opportunity as a relation between capacity and circumstance. (`PM0515`)
- Represent an obstacle as a relation between target and conditions. (`PM0516`)
- Represent a property as a relation to a measuring procedure. (`PM0517`)
- Represent an absence as a failure of an expected occurrence. (`PM0518`)
- Represent a signal as a difference available to a receiver. (`PM0519`)
- Represent a category as a rule for treating cases together. (`PM0520`)
- Represent an error as divergence under a specified comparison rule. (`PM0521`)
- Represent a solution as a change in reachable states. (`PM0522`)
- Represent a perspective as an arrangement of available distinctions. (`PM0523`)
- Replace substance-based entities with interaction-defined entities. (`PM0524`)
- Allow several entity types for the same encountered matter. (`PM0525`)

## Object identity

- Identify an object by material continuity. (`PM0526`)
- Identify an object by continuity of function. (`PM0527`)
- Identify an object by continuity of organization. (`PM0528`)
- Identify an object by continuity of historical origin. (`PM0529`)
- Identify an object by the role recognized by participants. (`PM0530`)
- Identify an object by a maintained boundary. (`PM0531`)
- Identify an object by a stable pattern of interaction. (`PM0532`)
- Separate numerical identity from similarity of properties. (`PM0533`)
- Separate identity assigned by observers from identity maintained internally. (`PM0534`)
- Treat replacement of all parts as compatible with organizational identity. (`PM0535`)
- Treat unchanged parts in a new arrangement as a new object. (`PM0536`)
- Treat a renamed object as continuous with its predecessor. (`PM0537`)
- Treat an unchanged name as potentially referring to different objects. (`PM0538`)
- Split one presumed object into several interacting objects. (`PM0539`)
- Merge presumed objects whose separation has no operative consequence. (`PM0540`)
- Track identity through branching into descendants. (`PM0541`)
- Track identity through fusion of previously separate objects. (`PM0542`)
- Distinguish a type from each occurrence of that type. (`PM0543`)
- Distinguish an object from its record or identifier. (`PM0544`)
- Distinguish persistence of identity from persistence of value. (`PM0545`)
- Make the identity criterion depend on the question being asked. (`PM0546`)
- Retain incompatible identity criteria without forcing one verdict. (`PM0547`)
- Suspend identity claims where continuity cannot be established. (`PM0548`)
- Treat disputed identity as a substantive part of the situation. (`PM0549`)
- Compare consequences under alternative criteria for being the same thing. (`PM0550`)

## Parts and wholes

- Interpret a part through its contribution to the whole. (`PM0551`)
- Interpret a whole through the interactions among its parts. (`PM0552`)
- Interpret a part independently of its current containing whole. (`PM0553`)
- Move a part into a different hypothetical whole. (`PM0554`)
- Treat the same element as part of several wholes. (`PM0555`)
- Separate physical parts from functional components. (`PM0556`)
- Separate replaceable components from inseparable relations. (`PM0557`)
- Decompose by causal contribution rather than physical location. (`PM0558`)
- Decompose by decision authority rather than organizational chart. (`PM0559`)
- Decompose by failure mode rather than construction sequence. (`PM0560`)
- Decompose by timescale rather than material composition. (`PM0561`)
- Compose components around a shared output requirement. (`PM0562`)
- Compose components around a shared enabling condition. (`PM0563`)
- Represent interactions as explicit components of the whole. (`PM0564`)
- Represent the whole's constraints on each component. (`PM0565`)
- Distinguish summed properties from properties of organization. (`PM0566`)
- Distinguish component improvement from improvement of the whole. (`PM0567`)
- Treat a locally harmful change as potentially globally beneficial. (`PM0568`)
- Treat a locally beneficial change as potentially globally harmful. (`PM0569`)
- Remove a component conceptually and trace compensating changes. (`PM0570`)
- Duplicate a component conceptually and trace new interactions. (`PM0571`)
- Replace hierarchical decomposition with overlapping functional groupings. (`PM0572`)
- Replace one privileged whole with several relevant wholes. (`PM0573`)
- Represent the cost of coordinating parts separately from their outputs. (`PM0574`)
- Treat decomposition itself as a choice that changes available operations. (`PM0575`)

## Properties

- Replace an intrinsic-property account with a relational-property account. (`PM0576`)
- Replace a fixed attribute with a context-dependent disposition. (`PM0577`)
- Replace a binary property with a graded property. (`PM0578`)
- Replace a graded property with a threshold-dependent status. (`PM0579`)
- Separate possession of a capacity from its current exercise. (`PM0580`)
- Separate current condition from enduring tendency. (`PM0581`)
- Separate observed property from inferred property. (`PM0582`)
- Separate measured property from the instrument's response. (`PM0583`)
- Separate a property from the value assigned to it. (`PM0584`)
- Separate average behavior from behavior under exceptional conditions. (`PM0585`)
- Represent a property by the tests that reveal it. (`PM0586`)
- Represent a property by the transformations that preserve it. (`PM0587`)
- Represent a property by the actions it enables. (`PM0588`)
- Represent a property by the constraints it imposes. (`PM0589`)
- Replace one broad attribute with several independent dimensions. (`PM0590`)
- Combine correlated attributes into a provisional common factor. (`PM0591`)
- Allow a property to vary across locations within one object. (`PM0592`)
- Allow a property to vary across phases of one process. (`PM0593`)
- Treat a missing property as unknown rather than absent. (`PM0594`)
- Treat a disputed property as definition-dependent. (`PM0595`)
- Compare objects only on properties relevant to the chosen operation. (`PM0596`)
- Restore properties removed by a convenient stereotype. (`PM0597`)
- Remove properties attributed solely through category membership. (`PM0598`)
- Change which property defines similarity between cases. (`PM0599`)
- Make the attribution procedure part of the property description. (`PM0600`)

## States

- Replace a verbal condition with an explicit state description. (`PM0601`)
- Separate externally visible state from internal operating state. (`PM0602`)
- Separate desired state from expected state. (`PM0603`)
- Separate actual state from the state recorded by observers. (`PM0604`)
- Represent a state by what transitions are available from it. (`PM0605`)
- Represent a state by what histories can lead to it. (`PM0606`)
- Represent a state by the resources required to maintain it. (`PM0607`)
- Represent a state by its sensitivity to disturbance. (`PM0608`)
- Represent a state by which goals it satisfies. (`PM0609`)
- Represent a state by which distinctions remain unresolved. (`PM0610`)
- Split one apparent state into stable and temporary variants. (`PM0611`)
- Split one apparent state according to hidden commitments. (`PM0612`)
- Merge states indistinguishable for the next chosen action. (`PM0613`)
- Distinguish equilibrium from an actively maintained balance. (`PM0614`)
- Distinguish inactivity from readiness to act. (`PM0615`)
- Distinguish readiness from permission to act. (`PM0616`)
- Distinguish blocked progress from completed progress. (`PM0617`)
- Distinguish a paused process from a terminated process. (`PM0618`)
- Represent partial completion explicitly. (`PM0619`)
- Represent incompatible simultaneous state descriptions separately. (`PM0620`)
- Add a previously unrepresented failure state. (`PM0621`)
- Add a recovery state between failure and normal operation. (`PM0622`)
- Add an unknown-state category without assigning a likely state. (`PM0623`)
- Treat the observer's information state separately from the object's state. (`PM0624`)
- Change the state variables when existing ones conceal relevant transitions. (`PM0625`)

## Events

- Define an event by a physical change. (`PM0626`)
- Define an event by a change in available action. (`PM0627`)
- Define an event by a change in recognized status. (`PM0628`)
- Define an event by crossing a decision threshold. (`PM0629`)
- Define an event by an expected occurrence failing to happen. (`PM0630`)
- Separate an event's occurrence from its detection. (`PM0631`)
- Separate an event's detection from its official recognition. (`PM0632`)
- Separate a trigger event from the process it initiates. (`PM0633`)
- Separate a terminating event from gradual loss of activity. (`PM0634`)
- Treat several recorded events as one extended episode. (`PM0635`)
- Split one recorded event into causally distinct occurrences. (`PM0636`)
- Treat repetition as separate events with different histories. (`PM0637`)
- Treat one event as belonging to several interacting sequences. (`PM0638`)
- Represent an event from each participant's available information. (`PM0639`)
- Represent an event by the possibilities it closes. (`PM0640`)
- Represent an event by the possibilities it opens. (`PM0641`)
- Represent an event by the commitments it activates. (`PM0642`)
- Represent an event by the resources it redistributes. (`PM0643`)
- Replace event labels with the changes actually observed. (`PM0644`)
- Distinguish event intensity from event significance. (`PM0645`)
- Distinguish a surprising event from an improbable event. (`PM0646`)
- Distinguish an initiating event from an underlying cause. (`PM0647`)
- Change the event boundary to include immediate preparation. (`PM0648`)
- Change the event boundary to include immediate aftermath. (`PM0649`)
- Represent disputed event boundaries as alternative segmentations. (`PM0650`)

## Processes

- Replace a result description with a transformation sequence. (`PM0651`)
- Replace a named process with its entry and exit conditions. (`PM0652`)
- Distinguish intended process from observed process. (`PM0653`)
- Distinguish prescribed sequence from actual execution order. (`PM0654`)
- Distinguish necessary stages from historically customary stages. (`PM0655`)
- Represent the process through its resource flows. (`PM0656`)
- Represent the process through its changing constraints. (`PM0657`)
- Represent the process through its decisions and branch points. (`PM0658`)
- Represent the process through the information available at each stage. (`PM0659`)
- Represent the process through its failure and recovery paths. (`PM0660`)
- Represent the process through its irreversible commitments. (`PM0661`)
- Represent the process through who controls each transition. (`PM0662`)
- Replace a linear process with interacting concurrent processes. (`PM0663`)
- Replace a single cycle with nested cycles. (`PM0664`)
- Replace automatic progression with explicit transition conditions. (`PM0665`)
- Separate initiation, continuation, interruption, and termination conditions. (`PM0666`)
- Separate productive work from coordination and waiting. (`PM0667`)
- Separate state-changing steps from representational updates. (`PM0668`)
- Treat handoffs as substantive transformations. (`PM0669`)
- Treat rework as part of the process rather than an exception. (`PM0670`)
- Trace inputs that never reach the intended output. (`PM0671`)
- Trace outputs that arise outside the prescribed path. (`PM0672`)
- Make feedback from later stages alter earlier-stage interpretation. (`PM0673`)
- Represent a process as one possible path through alternatives. (`PM0674`)
- Interpret the process by the intermediate results it makes reusable. (`PM0675`)

## Relations

- Replace isolated attributes with relations between entities. (`PM0676`)
- Reverse the direction of a directed relation hypothetically. (`PM0677`)
- Distinguish reciprocal relations from one-sided relations. (`PM0678`)
- Distinguish symmetric relations from mutually present asymmetric relations. (`PM0679`)
- Distinguish transitive relations from relations lacking transitivity. (`PM0680`)
- Distinguish direct relations from relations mediated by another entity. (`PM0681`)
- Distinguish necessary relations from contingent co-occurrence. (`PM0682`)
- Distinguish enabling relations from determining relations. (`PM0683`)
- Distinguish dependence from mere association. (`PM0684`)
- Distinguish competition from incompatible resource use. (`PM0685`)
- Represent the same entities under several relation types. (`PM0686`)
- Replace a pairwise relation with a relation among several participants. (`PM0687`)
- Represent conditions under which a relation exists. (`PM0688`)
- Represent strength of relation separately from its existence. (`PM0689`)
- Represent change in a relation while entities remain unchanged. (`PM0690`)
- Treat the relation as the primary unit of analysis. (`PM0691`)
- Treat an entity as the intersection of its relations. (`PM0692`)
- Replace an undifferentiated connection with a typed connection. (`PM0693`)
- Separate legal, practical, emotional, and informational dependence. (`PM0694`)
- Distinguish relations recognized by participants from those inferred externally. (`PM0695`)
- Represent incompatible relations without reducing them to one dimension. (`PM0696`)
- Expose a relation suppressed by the chosen description. (`PM0697`)
- Remove a relation assumed solely from shared membership. (`PM0698`)
- Compose relations only when their joining conditions are specified. (`PM0699`)
- Examine relations between relations as a new representational level. (`PM0700`)

## Distinctions

- Split cases previously treated as equivalent. (`PM0701`)
- Merge cases whose alleged difference has no selected consequence. (`PM0702`)
- Replace a familiar distinction with one that predicts different responses. (`PM0703`)
- Replace a binary distinction with independent dimensions. (`PM0704`)
- Replace overlapping distinctions with their explicit intersections. (`PM0705`)
- Distinguish the thing from the way it is represented. (`PM0706`)
- Distinguish the result from the criterion used to judge it. (`PM0707`)
- Distinguish a condition from evidence for that condition. (`PM0708`)
- Distinguish a reason from a cause. (`PM0709`)
- Distinguish an explanation from a justification. (`PM0710`)
- Distinguish a limitation from an impossibility. (`PM0711`)
- Distinguish lack of access from lack of existence. (`PM0712`)
- Distinguish lack of expression from lack of capability. (`PM0713`)
- Distinguish rejection of a claim from suspension of judgment. (`PM0714`)
- Distinguish failure to choose from choosing to postpone. (`PM0715`)
- Distinguish an alternative interpretation from an alternative subject. (`PM0716`)
- Distinguish changing a perspective from changing its terminology. (`PM0717`)
- Distinguish discovering a difference from creating one. (`PM0718`)
- Distinguish useful simplification from concealed disagreement. (`PM0719`)
- Distinguish a universal claim from a default expectation. (`PM0720`)
- Distinguish uncertainty about facts from uncertainty about meanings. (`PM0721`)
- Remove a distinction inherited from an irrelevant earlier task. (`PM0722`)
- Recover a distinction erased by aggregation. (`PM0723`)
- Keep a distinction provisional until a separating case appears. (`PM0724`)
- Create a distinction around a previously unexplained divergence. (`PM0725`)

## Categories

- Group cases by causal mechanism instead of appearance. (`PM0726`)
- Group cases by required response instead of shared origin. (`PM0727`)
- Group cases by function instead of material composition. (`PM0728`)
- Group cases by failure mode instead of normal behavior. (`PM0729`)
- Group cases by dependency structure instead of domain vocabulary. (`PM0730`)
- Group cases by developmental stage instead of chronological age. (`PM0731`)
- Group cases by available operations instead of descriptive similarity. (`PM0732`)
- Group cases by evidence quality instead of conclusion content. (`PM0733`)
- Group cases by reversibility instead of estimated benefit. (`PM0734`)
- Group cases by information access instead of institutional title. (`PM0735`)
- Replace a hierarchy with overlapping categories. (`PM0736`)
- Replace overlapping categories with independent classification dimensions. (`PM0737`)
- Replace exhaustive categories with an explicit unclassified remainder. (`PM0738`)
- Replace inherited categories with categories induced from contrasting cases. (`PM0739`)
- Replace fixed categories with context-dependent groupings. (`PM0740`)
- Replace a category center with its boundary cases. (`PM0741`)
- Separate descriptive categories from evaluative categories. (`PM0742`)
- Separate natural variation from administratively imposed categories. (`PM0743`)
- Split a category containing opposed causal mechanisms. (`PM0744`)
- Merge categories separated only by historical terminology. (`PM0745`)
- Add a category for cases violating the current classification. (`PM0746`)
- Remove a category that produces no consequential discrimination. (`PM0747`)
- Allow one case to occupy several categories simultaneously. (`PM0748`)
- Track how a case changes category over time. (`PM0749`)
- Treat the category system itself as a revisable representation. (`PM0750`)

## Membership

- Replace all-or-nothing membership with degrees of fit. (`PM0751`)
- Replace degrees of fit with explicit necessary conditions. (`PM0752`)
- Replace resemblance-based membership with a decision rule. (`PM0753`)
- Replace rule-based membership with comparison to representative cases. (`PM0754`)
- Separate eligibility from actual participation. (`PM0755`)
- Separate formal membership from practical involvement. (`PM0756`)
- Separate self-identification from classification by others. (`PM0757`)
- Separate temporary inclusion from durable membership. (`PM0758`)
- Separate membership by origin from membership by present function. (`PM0759`)
- Distinguish exclusion by rule from exclusion by missing evidence. (`PM0760`)
- Distinguish an unknown member from a known nonmember. (`PM0761`)
- Allow contested membership without forcing immediate classification. (`PM0762`)
- Allow context-specific membership in otherwise stable categories. (`PM0763`)
- Allow a case to satisfy multiple incompatible membership rules. (`PM0764`)
- Treat membership as something achieved through activity. (`PM0765`)
- Treat membership as something maintained through recognition. (`PM0766`)
- Treat membership as something lost when conditions change. (`PM0767`)
- Move the membership threshold and track reclassified cases. (`PM0768`)
- Test membership against the most ambiguous included case. (`PM0769`)
- Test membership against the most similar excluded case. (`PM0770`)
- Replace one global rule with rules for different subcases. (`PM0771`)
- Separate exceptions to membership rules from changes to those rules. (`PM0772`)
- Include the consequences of classification in the membership inquiry. (`PM0773`)
- Track who has authority to grant or deny membership. (`PM0774`)
- Make the membership rule visible in every category-level claim. (`PM0775`)

## Definitions

- Replace a synonym-based definition with discriminating conditions. (`PM0776`)
- Replace a list of examples with a membership rule. (`PM0777`)
- Replace a membership rule with an operational identification procedure. (`PM0778`)
- Replace an operational definition with the construct it approximates. (`PM0779`)
- Replace an essential-property definition with a relational definition. (`PM0780`)
- Replace a static definition with a process-based definition. (`PM0781`)
- Replace a purpose-based definition with a structure-based definition. (`PM0782`)
- Replace a structure-based definition with a capacity-based definition. (`PM0783`)
- Separate stipulated meaning from established usage. (`PM0784`)
- Separate a definition from claims about things satisfying it. (`PM0785`)
- Separate necessary conditions from sufficient conditions. (`PM0786`)
- Separate descriptive meaning from evaluative connotations. (`PM0787`)
- Restrict a definition to the domain where it discriminates correctly. (`PM0788`)
- Extend a definition by explicitly adding a new class of cases. (`PM0789`)
- Split an ambiguous term into several separately defined senses. (`PM0790`)
- Unify terms that make the same operative distinctions. (`PM0791`)
- Replace circular definitions with independently usable distinctions. (`PM0792`)
- Define a term through its contrast with the nearest alternative. (`PM0793`)
- Define a term through allowed and excluded transformations. (`PM0794`)
- Define a term through its role in an inference. (`PM0795`)
- Retain several definitions when they serve different inquiries. (`PM0796`)
- Choose a definition that preserves a previously lost distinction. (`PM0797`)
- Revise a definition after a counterexample defeats its boundary. (`PM0798`)
- Remove a definition that supplies only apparent understanding. (`PM0799`)
- Make the consequences of adopting each definition explicit. (`PM0800`)

## Meaning

- Interpret a statement by its literal asserted content. (`PM0801`)
- Interpret a statement by its practical implications. (`PM0802`)
- Interpret a statement by what it presupposes. (`PM0803`)
- Interpret a statement by what it invites the listener to infer. (`PM0804`)
- Interpret a statement by the alternatives it contrasts with. (`PM0805`)
- Interpret a statement by the question it answers. (`PM0806`)
- Interpret a statement by the action it performs in context. (`PM0807`)
- Interpret a statement by the expectation it creates. (`PM0808`)
- Interpret a statement by the commitment it attributes. (`PM0809`)
- Interpret a statement by what would count against it. (`PM0810`)
- Separate speaker intention from listener uptake. (`PM0811`)
- Separate conventional meaning from context-specific meaning. (`PM0812`)
- Separate referential content from emotional association. (`PM0813`)
- Separate ambiguity of wording from indeterminacy of the subject. (`PM0814`)
- Replace one assumed interpretation with several explicit readings. (`PM0815`)
- Resolve a term differently at each context where its use shifts. (`PM0816`)
- Preserve ambiguity when forced resolution would erase the issue. (`PM0817`)
- Interpret an expression without its customary metaphor. (`PM0818`)
- Interpret a metaphor literally to expose concealed implications. (`PM0819`)
- Reconstruct meaning from the responses the expression licenses. (`PM0820`)
- Reconstruct meaning from cases where the expression becomes inappropriate. (`PM0821`)
- Change the implicit contrast class while preserving the sentence. (`PM0822`)
- Change the presumed question while preserving the sentence. (`PM0823`)
- Change the context of use while preserving the sentence. (`PM0824`)
- Treat meaning as negotiated through subsequent interaction. (`PM0825`)

## Language

- Replace nouns naming processes with verbs specifying activity. (`PM0826`)
- Replace passive phrasing with named actors and actions. (`PM0827`)
- Replace evaluative adjectives with observable differences. (`PM0828`)
- Replace vague quantifiers with explicit ranges or counts. (`PM0829`)
- Replace absolute wording with its actual scope conditions. (`PM0830`)
- Replace weak hedging with a precise uncertainty statement. (`PM0831`)
- Replace technical vocabulary with literal descriptions of operations. (`PM0832`)
- Replace ordinary wording with formal notation where ambiguity matters. (`PM0833`)
- Replace euphemisms with the concrete actions they conceal. (`PM0834`)
- Replace hostile labels with the conduct being alleged. (`PM0835`)
- Replace flattering labels with the performance being claimed. (`PM0836`)
- Replace metaphors of objects with descriptions of relations. (`PM0837`)
- Replace possession language with access or control relations. (`PM0838`)
- Replace identity language with a conditional association where warranted. (`PM0839`)
- Replace a compressed compound term with its distinct components. (`PM0840`)
- Remove customary vocabulary and reconstruct the same distinctions. (`PM0841`)
- Translate the account into another vocabulary without preserving its categories. (`PM0842`)
- Keep the vocabulary fixed while changing the underlying distinctions. (`PM0843`)
- Separate an actor's own terms from the analyst's terms. (`PM0844`)
- Restore omitted subjects, objects, and conditions in elliptical claims. (`PM0845`)
- Rewrite a nominalized dispute as incompatible actions or assertions. (`PM0846`)
- Rewrite inevitability language as specified constraints and alternatives. (`PM0847`)
- Rewrite ability language as conditions for successful performance. (`PM0848`)
- Rewrite a slogan as independently assessable claims. (`PM0849`)
- Treat wording changes as perspective changes only when consequences differ. (`PM0850`)

## Representation

- Replace prose with a table of explicit comparisons. (`PM0851`)
- Replace a table with a graph of dependencies. (`PM0852`)
- Replace a hierarchy with a network of interacting elements. (`PM0853`)
- Replace a network with a sequence of actual traversals. (`PM0854`)
- Replace a timeline with a state-transition representation. (`PM0855`)
- Replace a static diagram with an executable simulation. (`PM0856`)
- Replace an equation with concrete cases satisfying it. (`PM0857`)
- Replace examples with a formal relation connecting them. (`PM0858`)
- Replace a single score with its underlying dimensions. (`PM0859`)
- Replace a dimensional profile with a decision boundary. (`PM0860`)
- Represent uncertainty directly alongside each represented quantity. (`PM0861`)
- Represent missing information as explicit gaps. (`PM0862`)
- Represent alternatives alongside the currently selected arrangement. (`PM0863`)
- Represent excluded cases alongside included cases. (`PM0864`)
- Represent provenance alongside each imported claim. (`PM0865`)
- Represent both the object's state and the observer's knowledge. (`PM0866`)
- Represent dependencies between entries instead of listing them independently. (`PM0867`)
- Represent alternative relation types as independently switchable layers. (`PM0868`)
- Represent contradictions without forcing immediate reconciliation. (`PM0869`)
- Use several coordinated representations of the same subject. (`PM0870`)
- Translate between representations and retain what each translation loses. (`PM0871`)
- Remove decorative structure that contributes no usable distinction. (`PM0872`)
- Add structure only where it enables a previously unavailable operation. (`PM0873`)
- Make the representation editable at the level of its assumptions. (`PM0874`)
- Represent how the representation itself was constructed. (`PM0875`)

## Measurement

- Replace a proxy measure with direct observation where available. (`PM0876`)
- Replace one proxy with several independently informative proxies. (`PM0877`)
- Separate the measured quantity from the intended construct. (`PM0878`)
- Separate measurement error from actual variation. (`PM0879`)
- Separate absence of measurement from a measured zero. (`PM0880`)
- Separate precision of reporting from accuracy of measurement. (`PM0881`)
- Measure the same phenomenon with a different instrument. (`PM0882`)
- Measure the same phenomenon through a different observable consequence. (`PM0883`)
- Measure changes within cases instead of differences between cases. (`PM0884`)
- Measure differences between cases instead of changes within cases. (`PM0885`)
- Measure capacity under standardized conditions instead of ordinary output. (`PM0886`)
- Measure ordinary output instead of performance under ideal conditions. (`PM0887`)
- Measure resource expenditure alongside achieved results. (`PM0888`)
- Measure recovery time alongside failure frequency. (`PM0889`)
- Measure variation alongside average performance. (`PM0890`)
- Measure access to a resource separately from its total amount. (`PM0891`)
- Measure actual use separately from nominal availability. (`PM0892`)
- Replace a single measurement with repeated observations. (`PM0893`)
- Change the measurement interval to expose brief events. (`PM0894`)
- Change the measurement threshold to expose weak signals. (`PM0895`)
- Calibrate the instrument against an independent reference. (`PM0896`)
- Represent instrument limitations as constraints on the conclusion. (`PM0897`)
- Model how measurement changes the behavior being measured. (`PM0898`)
- Compare conclusions under alternative measurement procedures. (`PM0899`)
- Make the choice of what to measure itself examinable. (`PM0900`)

## Units and normalization

- Replace totals with values per person. (`PM0901`)
- Replace totals with values per unit of time. (`PM0902`)
- Replace totals with values per opportunity for occurrence. (`PM0903`)
- Replace totals with values per unit of resource consumed. (`PM0904`)
- Replace totals with values per unit of useful output. (`PM0905`)
- Restore absolute totals beside normalized rates. (`PM0906`)
- Replace percentage change with absolute change. (`PM0907`)
- Replace absolute change with change relative to a baseline. (`PM0908`)
- Replace percentages with natural frequencies under a stated denominator. (`PM0909`)
- Distinguish percentage-point changes from relative percentage changes. (`PM0910`)
- Replace monetary quantities with the activities they can purchase. (`PM0911`)
- Replace time costs with fractions of available time. (`PM0912`)
- Replace physical distance with required effort units. (`PM0913`)
- Normalize by capacity instead of population. (`PM0914`)
- Normalize by exposure instead of calendar duration. (`PM0915`)
- Normalize by starting position instead of final position. (`PM0916`)
- Normalize by a common reference instead of each group's own reference. (`PM0917`)
- Remove normalization that conceals meaningful scale differences. (`PM0918`)
- Use dimensionless ratios to compare differently scaled systems. (`PM0919`)
- Retain original units when normalization erases practical constraints. (`PM0920`)
- Express uncertainty in the same units as the estimated quantity. (`PM0921`)
- Separate unit conversion from a change in the measured construct. (`PM0922`)
- Hold the denominator fixed when comparing changing numerators. (`PM0923`)
- Hold the numerator fixed when comparing denominator effects. (`PM0924`)
- Treat denominator selection as part of the perspective. (`PM0925`)

## Coordinates and reference frames

- Replace absolute values with deviations from a reference value. (`PM0926`)
- Replace Cartesian positions with distances and directions from a center. (`PM0927`)
- Rotate representation axes to expose an obscured relationship. (`PM0928`)
- Change the origin while preserving relative differences. (`PM0929`)
- Rescale one axis to reveal variation previously compressed. (`PM0930`)
- Use a logarithmic axis to compare multiplicative changes. (`PM0931`)
- Use a linear axis to expose additive differences. (`PM0932`)
- Replace elapsed time with progress toward a defined milestone. (`PM0933`)
- Replace calendar position with time since a shared event. (`PM0934`)
- Represent each participant relative to their own initial condition. (`PM0935`)
- Represent all participants relative to one shared condition. (`PM0936`)
- Replace observer-centered coordinates with object-centered coordinates. (`PM0937`)
- Replace object-centered coordinates with relation-centered coordinates. (`PM0938`)
- Separate coordinate artifacts from changes in the represented phenomenon. (`PM0939`)
- Choose axes corresponding to independently variable properties. (`PM0940`)
- Choose axes corresponding to competing practical concerns. (`PM0941`)
- Add a coordinate for a previously hidden condition. (`PM0942`)
- Remove a coordinate irrelevant to the selected discrimination. (`PM0943`)
- Transform coordinates while tracking preserved quantities. (`PM0944`)
- Represent one object in several reference frames simultaneously. (`PM0945`)
- Replace geographic coordinates with positions in an interaction network. (`PM0946`)
- Replace physical coordinates with positions in a space of alternatives. (`PM0947`)
- Replace a fixed reference frame with a moving one. (`PM0948`)
- Keep orientation visible when transformations reverse apparent direction. (`PM0949`)
- Treat reference-frame choice as a possible source of disagreement. (`PM0950`)

## Baselines

- Compare the present with the immediately preceding state. (`PM0951`)
- Compare the present with a longer historical baseline. (`PM0952`)
- Compare the present with the expected state without intervention. (`PM0953`)
- Compare the present with the best feasible alternative. (`PM0954`)
- Compare the present with a minimum acceptable condition. (`PM0955`)
- Compare the present with the actor's original commitment. (`PM0956`)
- Compare the present with the actor's actual starting resources. (`PM0957`)
- Compare the present with a matched case under different conditions. (`PM0958`)
- Compare the present with ordinary variation in the same system. (`PM0959`)
- Compare the present with an explicitly idealized benchmark. (`PM0960`)
- Replace the status quo baseline with a likely future baseline. (`PM0961`)
- Replace an aspirational baseline with an empirically attainable baseline. (`PM0962`)
- Replace an external benchmark with the task's actual requirements. (`PM0963`)
- Replace a moving baseline with a fixed reference period. (`PM0964`)
- Replace a fixed baseline when the underlying conditions have changed. (`PM0965`)
- Use several baselines to expose baseline-dependent judgments. (`PM0966`)
- Separate improvement over baseline from adequacy for the goal. (`PM0967`)
- Separate departure from normality from deterioration. (`PM0968`)
- Separate return to baseline from full recovery of lost opportunities. (`PM0969`)
- Include the cost of maintaining the baseline. (`PM0970`)
- Include the consequences of choosing no deliberate intervention. (`PM0971`)
- Correct a baseline selected because it was unusually favorable. (`PM0972`)
- Correct a baseline selected because it was unusually unfavorable. (`PM0973`)
- Treat disputed baselines as explicit competing assumptions. (`PM0974`)
- Track whether a changed verdict comes solely from baseline replacement. (`PM0975`)

## Comparisons

- Compare matched cases differing in one consequential condition. (`PM0976`)
- Compare dissimilar cases sharing the same relational structure. (`PM0977`)
- Compare the same actor across different situations. (`PM0978`)
- Compare different actors within the same situation. (`PM0979`)
- Compare outcomes under equal resources. (`PM0980`)
- Compare resources required for equal outcomes. (`PM0981`)
- Compare effort under equal difficulty. (`PM0982`)
- Compare difficulty under equal observed effort. (`PM0983`)
- Compare an option with its nearest viable competitor. (`PM0984`)
- Compare an option with a neglected radically different alternative. (`PM0985`)
- Compare a successful case with a near failure. (`PM0986`)
- Compare a failed case with a near success. (`PM0987`)
- Compare typical cases with boundary cases. (`PM0988`)
- Compare stated intentions with revealed choices. (`PM0989`)
- Compare formal rules with actual recurring practices. (`PM0990`)
- Compare predicted consequences with observed consequences. (`PM0991`)
- Compare immediate advantages with cumulative effects. (`PM0992`)
- Compare gains for one party with losses for another. (`PM0993`)
- Compare the same claim under different definitions. (`PM0994`)
- Compare perspectives by the cases they distinguish differently. (`PM0995`)
- Compare perspectives by the actions each makes available. (`PM0996`)
- Compare explanations by where their predictions diverge. (`PM0997`)
- Compare arrangements after equalizing a known confounding difference. (`PM0998`)
- Preserve noncomparability when no shared criterion is justified. (`PM0999`)
- Replace an overall comparison with separate comparisons by condition. (`PM1000`)

## Ordering

- Order cases by magnitude instead of presentation sequence. (`PM1001`)
- Order cases by causal precedence instead of chronology. (`PM1002`)
- Order cases by dependency instead of perceived importance. (`PM1003`)
- Order cases by reversibility instead of desirability. (`PM1004`)
- Order cases by uncertainty instead of expected value. (`PM1005`)
- Order cases by time remaining for useful intervention. (`PM1006`)
- Order cases by information needed before they can be resolved. (`PM1007`)
- Order cases by how strongly they distinguish competing interpretations. (`PM1008`)
- Order cases by consequences for the least advantaged participant. (`PM1009`)
- Order cases by distance from a decision threshold. (`PM1010`)
- Replace a total ranking with a partial order. (`PM1011`)
- Replace a single ranking with criterion-specific rankings. (`PM1012`)
- Replace ordinal ranking with explicit pairwise preferences. (`PM1013`)
- Replace pairwise preferences with a map of preference cycles. (`PM1014`)
- Distinguish ties from unresolved comparisons. (`PM1015`)
- Distinguish lack of superiority from demonstrated equivalence. (`PM1016`)
- Reverse the usual examination order. (`PM1017`)
- Interleave opposing cases instead of presenting them in blocks. (`PM1018`)
- Present edge cases before representative cases. (`PM1019`)
- Present evidence before the proposed conclusion. (`PM1020`)
- Present consequences before the actor's stated intention. (`PM1021`)
- Present dependencies before proposed actions. (`PM1022`)
- Randomize order to expose order-sensitive interpretations. (`PM1023`)
- Hold order fixed while changing the information presented. (`PM1024`)
- Treat ranking instability as information about the ranking rule. (`PM1025`)

## Magnitude and sensitivity

- Replace absolute magnitude with change per unit of input. (`PM1026`)
- Replace average effect with effect near the current operating point. (`PM1027`)
- Replace typical effect with maximum plausible effect. (`PM1028`)
- Replace maximum effect with the conditions required to produce it. (`PM1029`)
- Distinguish large quantities from quantities near a critical threshold. (`PM1030`)
- Distinguish proportional change from disproportionate change. (`PM1031`)
- Distinguish a strong relation from a practically consequential relation. (`PM1032`)
- Represent diminishing returns explicitly. (`PM1033`)
- Represent increasing returns explicitly. (`PM1034`)
- Represent saturation where additional input stops changing output. (`PM1035`)
- Represent thresholds where small changes produce different responses. (`PM1036`)
- Represent dead zones where input changes have no observed effect. (`PM1037`)
- Represent sign reversals across operating conditions. (`PM1038`)
- Separate sensitivity to one variable from sensitivity to joint changes. (`PM1039`)
- Vary one assumption while holding the remaining assumptions fixed. (`PM1040`)
- Vary several assumptions together when their changes are coupled. (`PM1041`)
- Compare sensitivity near typical cases and boundary cases. (`PM1042`)
- Replace a point estimate with a range of resulting outcomes. (`PM1043`)
- Identify which parameter changes alter the preferred interpretation. (`PM1044`)
- Ignore changes too small to alter any selected discrimination. (`PM1045`)
- Restore small changes that accumulate across many repetitions. (`PM1046`)
- Compare equal absolute changes at different starting magnitudes. (`PM1047`)
- Compare equal proportional changes at different starting magnitudes. (`PM1048`)
- Distinguish robustness of direction from robustness of magnitude. (`PM1049`)
- Treat sensitivity to representation choices as part of the result. (`PM1050`)

## Distribution

- Replace the average with the full spread of outcomes. (`PM1051`)
- Replace the mean with the median for a different central comparison. (`PM1052`)
- Replace the center with the worst observed tail. (`PM1053`)
- Replace the center with the best observed tail. (`PM1054`)
- Represent several clusters instead of one overall center. (`PM1055`)
- Separate variation within groups from variation between groups. (`PM1056`)
- Separate outcome frequency from the size of each outcome. (`PM1057`)
- Separate equal totals from equal distributions. (`PM1058`)
- Compare distributions with the same mean but different dispersion. (`PM1059`)
- Compare distributions with the same dispersion but different tails. (`PM1060`)
- Represent asymmetry around the central value. (`PM1061`)
- Represent rare extreme cases without merging them into the average. (`PM1062`)
- Represent the proportion falling below an adequacy threshold. (`PM1063`)
- Represent the proportion exceeding a capacity limit. (`PM1064`)
- Replace individual outcomes with cumulative shares. (`PM1065`)
- Replace cumulative shares with the individual cases they conceal. (`PM1066`)
- Represent concentration among recipients. (`PM1067`)
- Represent concentration over time. (`PM1068`)
- Represent concentration across locations. (`PM1069`)
- Compare unconditional distributions with distributions under specified conditions. (`PM1070`)
- Track how the distribution changes while the average stays constant. (`PM1071`)
- Track who moves between positions in the distribution. (`PM1072`)
- Distinguish persistent inequality from temporary position changes. (`PM1073`)
- Represent uncertainty about the distribution separately from observed spread. (`PM1074`)
- Treat distribution shape as a substantive part of the perspective. (`PM1075`)

## Aggregation

- Disaggregate a total by participant. (`PM1076`)
- Disaggregate a total by time period. (`PM1077`)
- Disaggregate a total by location. (`PM1078`)
- Disaggregate a total by mechanism producing it. (`PM1079`)
- Disaggregate a total by starting condition. (`PM1080`)
- Disaggregate a total by exposure to the relevant opportunity. (`PM1081`)
- Aggregate repeated observations into a pattern of recurrence. (`PM1082`)
- Aggregate local interactions into a system-level flow. (`PM1083`)
- Aggregate individual choices into collective consequences. (`PM1084`)
- Aggregate scattered costs over the full operating period. (`PM1085`)
- Weight cases equally instead of weighting by size. (`PM1086`)
- Weight cases by exposure instead of treating them equally. (`PM1087`)
- Weight cases by relevance to the target population. (`PM1088`)
- Remove weights whose justification does not match the inquiry. (`PM1089`)
- Separate changes in group composition from changes within groups. (`PM1090`)
- Separate aggregate improvement from improvement for every subgroup. (`PM1091`)
- Separate individual association from group-level association. (`PM1092`)
- Retain subgroup disagreements concealed by a pooled result. (`PM1093`)
- Compare pooled and subgroup-specific conclusions side by side. (`PM1094`)
- Represent interactions that prevent simple summation. (`PM1095`)
- Represent common costs separately from additive individual costs. (`PM1096`)
- Avoid double-counting an effect appearing in several groups. (`PM1097`)
- Treat aggregation rules as explicit assumptions. (`PM1098`)
- Track which distinctions disappear at each aggregation step. (`PM1099`)
- Choose aggregation according to the decision actually being made. (`PM1100`)

## Variation

- Treat variation as a subject requiring explanation. (`PM1101`)
- Treat a stable average as compatible with changing individual trajectories. (`PM1102`)
- Separate variation caused by conditions from unexplained residual variation. (`PM1103`)
- Separate variation between individuals from variation within individuals. (`PM1104`)
- Separate predictable cycles from irregular fluctuations. (`PM1105`)
- Separate measurement variation from changes in the underlying process. (`PM1106`)
- Separate variation in opportunity from variation in performance. (`PM1107`)
- Separate variation in exposure from variation in susceptibility. (`PM1108`)
- Represent individual response patterns instead of one common response. (`PM1109`)
- Represent the range of responses under apparently identical conditions. (`PM1110`)
- Track whether the same cases consistently occupy the extremes. (`PM1111`)
- Track whether extreme positions rotate among cases. (`PM1112`)
- Compare variation before and after a shared intervention. (`PM1113`)
- Compare variation under ordinary and stressful conditions. (`PM1114`)
- Represent increasing variability as distinct from changing average performance. (`PM1115`)
- Represent decreasing variability as distinct from overall improvement. (`PM1116`)
- Treat an outlier as a possible new type. (`PM1117`)
- Treat an outlier as a possible measurement failure. (`PM1118`)
- Treat an outlier as evidence about hidden conditions. (`PM1119`)
- Retain several explanations for variation until discriminating evidence appears. (`PM1120`)
- Introduce controlled variation into the imagined situation. (`PM1121`)
- Remove one source of variation in a comparative model. (`PM1122`)
- Examine which differences survive standardization. (`PM1123`)
- Examine which similarities disappear when conditions are varied. (`PM1124`)
- Represent variation in the perspective's own case-by-case performance. (`PM1125`)

## Sampling

- Replace convenient cases with cases selected by an explicit rule. (`PM1126`)
- Add cases from regions the existing sample omits. (`PM1127`)
- Add cases that are difficult for the preferred interpretation. (`PM1128`)
- Add cases where competing interpretations make different predictions. (`PM1129`)
- Add near-boundary cases to test a distinction. (`PM1130`)
- Add mundane cases to counter selection for vividness. (`PM1131`)
- Add failed attempts to counter selection for success. (`PM1132`)
- Add silent participants to counter selection for expressed opinions. (`PM1133`)
- Add departed participants to counter selection for persistence. (`PM1134`)
- Add inaccessible cases as an explicit uncertainty about coverage. (`PM1135`)
- Sample the same population at different times. (`PM1136`)
- Sample different populations under comparable conditions. (`PM1137`)
- Sample entire sequences instead of isolated observations. (`PM1138`)
- Sample interactions instead of individual entities. (`PM1139`)
- Sample opportunities for failure instead of recorded failures alone. (`PM1140`)
- Distinguish the sampled population from the intended population. (`PM1141`)
- Distinguish selection into the system from selection into observation. (`PM1142`)
- Distinguish nonresponse from the response being studied. (`PM1143`)
- Represent the procedure that made each case observable. (`PM1144`)
- Reweight observations to reflect a justified target composition. (`PM1145`)
- Restrict conclusions to the population actually represented. (`PM1146`)
- Use deliberately contrasting cases to discover missing distinctions. (`PM1147`)
- Use random selection to reduce discretionary case choice. (`PM1148`)
- Hold sample membership fixed while changing interpretation. (`PM1149`)
- Treat conclusions that change with sample composition as conditional. (`PM1150`)

## Evidence selection

- Require evidence that separates the leading alternatives. (`PM1151`)
- Include observations that oppose the current belief. (`PM1152`)
- Include observations omitted because they lack a convenient explanation. (`PM1153`)
- Separate direct observations from reports of observations. (`PM1154`)
- Separate independent evidence from repeated versions of one report. (`PM1155`)
- Separate evidence about occurrence from evidence about causation. (`PM1156`)
- Separate evidence about capability from evidence about ordinary performance. (`PM1157`)
- Separate evidence about intention from evidence about effect. (`PM1158`)
- Separate evidence about absence from absence of evidence. (`PM1159`)
- Separate evidence about one case from evidence about generality. (`PM1160`)
- Prefer contemporaneous records for claims about what was then known. (`PM1161`)
- Prefer direct task performance for a narrowly defined capability claim. (`PM1162`)
- Use negative cases to restrict an overbroad generalization. (`PM1163`)
- Use boundary cases to revise an ambiguous criterion. (`PM1164`)
- Use intervention outcomes to examine a proposed causal relation. (`PM1165`)
- Use natural variation to distinguish otherwise similar explanations. (`PM1166`)
- Use a counterexample to defeat a universal claim. (`PM1167`)
- Use a failed prediction to reopen an accepted interpretation. (`PM1168`)
- Retain weak evidence with its limitations visible. (`PM1169`)
- Exclude a datum whose connection to the claim is unspecified. (`PM1170`)
- Restore excluded evidence after discovering its discriminating role. (`PM1171`)
- Change admissibility criteria when the kind of claim changes. (`PM1172`)
- Hold evidence fixed while comparing several interpretations. (`PM1173`)
- Hold interpretation fixed while examining omitted evidence. (`PM1174`)
- Treat the evidence-selection process as an additional object of inquiry. (`PM1175`)

## Evidence interpretation

- Interpret an observation under each live hypothesis. (`PM1176`)
- Interpret an unexpected absence as a possible failed prediction. (`PM1177`)
- Interpret agreement as potentially shared dependence on one source. (`PM1178`)
- Interpret disagreement as potentially different access to the same event. (`PM1179`)
- Interpret repetition as persistence rather than independent confirmation. (`PM1180`)
- Interpret a null result through the observation's detection limits. (`PM1181`)
- Interpret an extreme result through the case-selection procedure. (`PM1182`)
- Interpret an apparent trend through changes in measurement practice. (`PM1183`)
- Interpret a report through the reporter's opportunity to observe. (`PM1184`)
- Interpret a report through incentives affecting what gets reported. (`PM1185`)
- Separate observation from the conclusion attached to it. (`PM1186`)
- Separate evidential relevance from evidential reliability. (`PM1187`)
- Separate reliability of a source from support for this claim. (`PM1188`)
- Separate consistency with a hypothesis from discrimination in its favor. (`PM1189`)
- Separate failure to refute from positive corroboration. (`PM1190`)
- Separate a surprising observation from a decisive observation. (`PM1191`)
- Separate evidence against one option from evidence for another. (`PM1192`)
- Compare how likely an observation is under competing explanations. (`PM1193`)
- Reinterpret previously known evidence after changing a background assumption. (`PM1194`)
- Reinterpret a contradiction as possible variation in scope. (`PM1195`)
- Reinterpret a contradiction as possible variation in terminology. (`PM1196`)
- Reinterpret missing detail as unknown instead of supplying a default. (`PM1197`)
- Reduce evidential weight when observations share a common error source. (`PM1198`)
- Increase evidential diversity by using different observation mechanisms. (`PM1199`)
- Keep the evidential contribution separate from the preferred conclusion. (`PM1200`)

## Source relations

- Trace a claim to its earliest available supporting observation. (`PM1201`)
- Distinguish firsthand observation from retelling. (`PM1202`)
- Distinguish expertise in the subject from general prestige. (`PM1203`)
- Distinguish authority to decide from ability to know. (`PM1204`)
- Distinguish access to information from freedom to disclose it. (`PM1205`)
- Distinguish sincerity from accuracy. (`PM1206`)
- Distinguish accuracy from completeness. (`PM1207`)
- Distinguish independence of authors from independence of their evidence. (`PM1208`)
- Represent shared source ancestry among apparently separate claims. (`PM1209`)
- Represent disagreements within one institution instead of one institutional voice. (`PM1210`)
- Represent changes in a source's position over time. (`PM1211`)
- Represent what a source could not observe. (`PM1212`)
- Represent what a source had reason to emphasize. (`PM1213`)
- Represent what a source had reason to omit. (`PM1214`)
- Compare sources with complementary observational access. (`PM1215`)
- Compare sources exposed to different incentives. (`PM1216`)
- Compare public statements with directly documented actions. (`PM1217`)
- Compare recollections with contemporaneous records. (`PM1218`)
- Read a claim without its author's identity, then restore it. (`PM1219`)
- Read the same claim under an unfamiliar attribution hypothetically. (`PM1220`)
- Separate the messenger's role from the original claimant's role. (`PM1221`)
- Track where qualifications disappear during transmission. (`PM1222`)
- Track where uncertainty becomes apparent certainty during retelling. (`PM1223`)
- Treat a source network as part of the evidence structure. (`PM1224`)
- Revise source weighting separately from revising the claim itself. (`PM1225`)

## Missing information

- Represent an unobserved value as unknown instead of zero. (`PM1226`)
- Represent an omitted option as unavailable to the current comparison. (`PM1227`)
- Represent a missing distinction as an unresolved classification problem. (`PM1228`)
- Represent a missing mechanism as a gap in explanation. (`PM1229`)
- Represent a missing transition as a gap in execution. (`PM1230`)
- Represent a missing record as uncertainty about past activity. (`PM1231`)
- Separate information that does not exist from information not found. (`PM1232`)
- Separate information not found from information not accessible. (`PM1233`)
- Separate inaccessible information from information not interpretable. (`PM1234`)
- Separate uninterpretable information from information not connected to the question. (`PM1235`)
- Separate withheld information from information never recorded. (`PM1236`)
- Separate structurally unknowable details from temporarily unknown details. (`PM1237`)
- Infer bounds from known constraints without filling the missing value. (`PM1238`)
- Retain several completions of an incomplete account. (`PM1239`)
- Search for the missing fact that would change the next action. (`PM1240`)
- Search for the missing relation connecting already known facts. (`PM1241`)
- Search for the missing category needed to describe an observed case. (`PM1242`)
- Remove a conclusion that depends on an invented missing detail. (`PM1243`)
- Expose assumptions silently used to complete the account. (`PM1244`)
- Treat systematic missingness as a possible feature of the process. (`PM1245`)
- Treat random missingness as uncertainty about coverage. (`PM1246`)
- Compare conclusions under favorable and unfavorable missing-data completions. (`PM1247`)
- Ask what evidence would exist if the missing event had occurred. (`PM1248`)
- Change the question to one answerable with available information. (`PM1249`)
- Preserve the original question while obtaining the missing information. (`PM1250`)

## Uncertainty

- Replace a single answer with a bounded range. (`PM1251`)
- Replace a bounded range with several qualitatively different possibilities. (`PM1252`)
- Separate uncertainty about values from uncertainty about the model. (`PM1253`)
- Separate uncertainty about the model from uncertainty about the question. (`PM1254`)
- Separate ignorance from observed variability. (`PM1255`)
- Separate ambiguity of interpretation from unpredictability of events. (`PM1256`)
- Separate reducible uncertainty from uncertainty left by available methods. (`PM1257`)
- Separate disagreement among observers from uncertainty within one observer. (`PM1258`)
- Attach uncertainty to individual claims instead of the whole account. (`PM1259`)
- Represent dependencies among uncertain claims. (`PM1260`)
- Preserve uncertainty that cannot affect the selected decision. (`PM1261`)
- Prioritize uncertainty that can reverse the selected decision. (`PM1262`)
- Replace vague caution with specified unknown conditions. (`PM1263`)
- Replace apparent certainty with the alternatives it prematurely excluded. (`PM1264`)
- Treat one uncertain assumption as certain within a declared thought experiment. (`PM1265`)
- Treat an accepted certainty as revisable for a bounded exploration. (`PM1266`)
- Represent confidence intervals separately from prediction intervals when used. (`PM1267`)
- Represent uncertainty about a probability separately from that probability. (`PM1268`)
- Propagate unresolved assumptions into downstream conclusions. (`PM1269`)
- Stop propagating uncertainty through an inference that does not depend on it. (`PM1270`)
- Distinguish robustness across possibilities from confidence in one possibility. (`PM1271`)
- Distinguish lack of confidence from evidence of falsehood. (`PM1272`)
- Use separate accounts for cases where uncertainty resolves differently. (`PM1273`)
- Make tolerance for uncertainty depend on the contemplated action. (`PM1274`)
- Treat the management of uncertainty as part of the perspective. (`PM1275`)

## Probability

- Replace a possibility claim with an explicit probability estimate. (`PM1276`)
- Replace a precise probability with a justified probability range. (`PM1277`)
- Replace probability language with natural-frequency scenarios. (`PM1278`)
- Condition a probability on newly specified information. (`PM1279`)
- Remove a conditioning assumption and reconsider the broader population. (`PM1280`)
- Distinguish the probability of evidence from the probability of a hypothesis. (`PM1281`)
- Distinguish joint occurrence from occurrence of either event. (`PM1282`)
- Distinguish unconditional risk from risk within an exposed subgroup. (`PM1283`)
- Distinguish cumulative occurrence from occurrence in one trial. (`PM1284`)
- Distinguish independent repetitions from dependent repetitions. (`PM1285`)
- Include the reference-class frequency before interpreting an individual signal. (`PM1286`)
- Compare likelihoods under alternative explanations of the same evidence. (`PM1287`)
- Update a prior estimate after a discriminating observation. (`PM1288`)
- Retain alternative priors when the initial evidence is weak. (`PM1289`)
- Change the reference class and track the resulting estimate. (`PM1290`)
- Represent dependence between events instead of multiplying independent probabilities. (`PM1291`)
- Represent mutually exclusive outcomes explicitly. (`PM1292`)
- Represent overlapping outcomes without counting their overlap twice. (`PM1293`)
- Replace the most likely outcome with the whole outcome distribution. (`PM1294`)
- Separate a rare outcome's probability from its consequence severity. (`PM1295`)
- Separate uncertainty in frequency from uncertainty in case membership. (`PM1296`)
- Estimate probabilities separately for distinct operating regimes. (`PM1297`)
- Treat a probability forecast as dated and information-dependent. (`PM1298`)
- Compare forecast confidence with observed forecasting performance. (`PM1299`)
- Suspend numerical probability when the event space is not specified. (`PM1300`)

## Belief strength

- Change a belief from accepted to provisionally accepted. (`PM1301`)
- Change a belief from provisional to unresolved. (`PM1302`)
- Change a belief from unresolved to tentatively rejected. (`PM1303`)
- Change a belief from rejected to worth reconsidering. (`PM1304`)
- Separate believing a claim from acting as though it holds. (`PM1305`)
- Separate confidence in a conclusion from confidence in its explanation. (`PM1306`)
- Separate confidence in direction from confidence in magnitude. (`PM1307`)
- Separate confidence in one case from confidence in generalization. (`PM1308`)
- Assign different strengths to different components of a compound belief. (`PM1309`)
- Reduce confidence when a supporting premise loses support. (`PM1310`)
- Reduce confidence when independent alternatives explain the same evidence. (`PM1311`)
- Increase confidence after a successful discriminating prediction. (`PM1312`)
- Increase confidence after relevant objections are answered. (`PM1313`)
- Preserve belief strength when new information is nondiscriminating. (`PM1314`)
- Restore uncertainty hidden by repeated confident phrasing. (`PM1315`)
- Treat familiarity with a claim separately from support for it. (`PM1316`)
- Treat fluency of an explanation separately from its adequacy. (`PM1317`)
- Treat social agreement separately from independent justification. (`PM1318`)
- Set an explicit threshold for provisional use of a belief. (`PM1319`)
- Set a higher threshold for irreversible reliance on a belief. (`PM1320`)
- Retain a belief while suspending one disputed consequence. (`PM1321`)
- Reject one consequence without discarding the whole framework. (`PM1322`)
- Adopt an opponent's confidence level temporarily within a simulation. (`PM1323`)
- Compare conclusions under several permissible confidence assignments. (`PM1324`)
- Make belief revision responsive to evidence rather than previous expression. (`PM1325`)

## Assumptions

- Remove one assumption and reconstruct the resulting interpretation. (`PM1326`)
- Negate one assumption within a thought experiment. (`PM1327`)
- Replace one assumption with a weaker condition. (`PM1328`)
- Replace one assumption with a stronger condition. (`PM1329`)
- Replace a fixed assumption with a variable. (`PM1330`)
- Replace an implicit assumption with an explicit branch. (`PM1331`)
- Assume the preferred claim is right and derive consequences. (`PM1332`)
- Assume the preferred claim is wrong and derive alternatives. (`PM1333`)
- Assume the current goal has been misidentified. (`PM1334`)
- Assume the current subject has been misidentified. (`PM1335`)
- Assume the stated constraint can be changed. (`PM1336`)
- Assume the apparent exception is representative. (`PM1337`)
- Assume the apparent default is exceptional. (`PM1338`)
- Assume a missing capacity already exists but is inaccessible. (`PM1339`)
- Assume an accessible capacity cannot be activated reliably. (`PM1340`)
- Assume the mechanism differs despite similar outcomes. (`PM1341`)
- Assume the mechanism is shared despite different appearances. (`PM1342`)
- Keep the target fixed while replacing a means assumption. (`PM1343`)
- Keep the evidence fixed while replacing a background assumption. (`PM1344`)
- Keep the question fixed while replacing its presumed answer space. (`PM1345`)
- Vary assumptions jointly when they depend on one another. (`PM1346`)
- Expose assumptions required to make an inference valid. (`PM1347`)
- Remove assumptions that contribute nothing to the selected conclusion. (`PM1348`)
- Retain contradictory assumption sets as separate exploratory branches. (`PM1349`)
- Treat the choice of assumptions as itself open to modification. (`PM1350`)

## Presuppositions

- Question whether the named object exists as described. (`PM1351`)
- Question whether the question has a single answer. (`PM1352`)
- Question whether the offered alternatives exhaust the possibilities. (`PM1353`)
- Question whether the apparent problem requires solving. (`PM1354`)
- Question whether the proposed improvement is an improvement. (`PM1355`)
- Question whether the current distinction has practical consequences. (`PM1356`)
- Question whether the participants mean the same thing. (`PM1357`)
- Question whether the relevant unit is an individual. (`PM1358`)
- Question whether the relevant process has a definite beginning. (`PM1359`)
- Question whether the relevant outcome has a definite end. (`PM1360`)
- Question whether the supposed background is stable. (`PM1361`)
- Question whether the observer is independent of the situation. (`PM1362`)
- Question whether a successful explanation must identify an intention. (`PM1363`)
- Question whether disagreement indicates somebody's error. (`PM1364`)
- Question whether inconsistency must be resolved immediately. (`PM1365`)
- Question whether all valuable differences can share one measure. (`PM1366`)
- Question whether the available evidence was selected neutrally. (`PM1367`)
- Question whether the stated goal belongs to the affected people. (`PM1368`)
- Question whether the category predates the practices it organizes. (`PM1369`)
- Question whether a stated inability is a lack of capability. (`PM1370`)
- Question whether continuation is preferable to stopping. (`PM1371`)
- Question whether a perspective must remain internally unchanged across contexts. (`PM1372`)
- Question whether understanding must precede useful action. (`PM1373`)
- Question whether the next step must follow the existing framework. (`PM1374`)
- Treat the question's presuppositions as replaceable parts of its construction. (`PM1375`)

## Logic

- Separate premises from conclusions in a compressed argument. (`PM1376`)
- Separate logical consequence from empirical expectation. (`PM1377`)
- Separate contradiction from mere difference in emphasis. (`PM1378`)
- Separate contraries from exhaustive negations. (`PM1379`)
- Replace an implication with its converse and test it separately. (`PM1380`)
- Use classical contraposition to reason from a false consequent to a false antecedent. (`PM1381`)
- Negate a compound claim while preserving its logical structure. (`PM1382`)
- Separate conjunction from disjunction in an ambiguous statement. (`PM1383`)
- Make exclusive and inclusive readings of either-or explicit. (`PM1384`)
- Replace a universal rule with a defeasible default. (`PM1385`)
- Replace a default with explicit exception conditions. (`PM1386`)
- Represent inference dependencies as a graph. (`PM1387`)
- Remove an unsupported premise and identify which conclusions survive. (`PM1388`)
- Add an alternative premise yielding the same conclusion. (`PM1389`)
- Construct a countermodel where the premises hold and conclusion fails. (`PM1390`)
- Distinguish validity of an inference from truth of its premises. (`PM1391`)
- Distinguish inconsistency in statements from inconsistency in actions. (`PM1392`)
- Localize a contradiction instead of discarding every connected claim. (`PM1393`)
- Permit unresolved contradictions without inferring arbitrary conclusions. (`PM1394`)
- Replace binary truth assignment with an explicit unknown status. (`PM1395`)
- Separate truth evaluation from permission to assert. (`PM1396`)
- Restrict an inference rule to its stated domain. (`PM1397`)
- Track where an argument changes the meaning of a term. (`PM1398`)
- Compare the same premises under different declared inference rules. (`PM1399`)
- Treat inference rules themselves as revisable objects of examination. (`PM1400`)

## Quantification

- Replace all with some and examine the weaker claim. (`PM1401`)
- Replace some with all and seek a separating counterexample. (`PM1402`)
- Replace none with not all to expose scope differences. (`PM1403`)
- Replace every case with a typical case explicitly. (`PM1404`)
- Replace a typical-case claim with a claim about frequency. (`PM1405`)
- Replace often with a specified proportion and population. (`PM1406`)
- Replace always with a stated time domain. (`PM1407`)
- Replace everywhere with a stated spatial domain. (`PM1408`)
- Distinguish one solution for everyone from a solution for each. (`PM1409`)
- Distinguish one cause of every event from causes for each event. (`PM1410`)
- Distinguish every option meeting some criterion from one shared criterion. (`PM1411`)
- Distinguish no evidence for a claim from evidence for its negation. (`PM1412`)
- Move the scope of a negation and compare meanings. (`PM1413`)
- Move the scope of a condition and compare meanings. (`PM1414`)
- Make nested quantifier order explicit. (`PM1415`)
- Restrict a generalization to directly examined cases. (`PM1416`)
- Extend a generalization to unobserved cases as a separate inference. (`PM1417`)
- Separate existence from uniqueness. (`PM1418`)
- Separate uniqueness from optimality. (`PM1419`)
- Separate existence in principle from availability in practice. (`PM1420`)
- Represent exceptions without abandoning the entire quantified claim. (`PM1421`)
- Represent the denominator underlying a frequency claim. (`PM1422`)
- Separate finite observations from an unbounded generalization. (`PM1423`)
- Ask which case would witness an existential claim. (`PM1424`)
- Separate claims about arbitrarily many cases from claims about every case. (`PM1425`)

## Possibility and necessity

- Separate logical possibility from physical possibility. (`PM1426`)
- Separate physical possibility from technical feasibility. (`PM1427`)
- Separate technical feasibility from practical availability. (`PM1428`)
- Separate practical availability from institutional permission. (`PM1429`)
- Separate permission from willingness to act. (`PM1430`)
- Separate possible outcomes from reachable outcomes under present conditions. (`PM1431`)
- Separate necessity for one method from necessity for the goal. (`PM1432`)
- Distinguish necessity across possible worlds from necessity within one model. (`PM1433`)
- Separate inevitability from high probability. (`PM1434`)
- Separate inability now from impossibility under every relevant condition. (`PM1435`)
- Treat an assumed impossibility as a condition-dependent limitation. (`PM1436`)
- Treat an assumed necessity as a removable design choice. (`PM1437`)
- Treat a remote possibility as the starting condition of an inquiry. (`PM1438`)
- Treat an ordinary possibility as unavailable within a constrained exploration. (`PM1439`)
- Expand the possible worlds by relaxing one constraint. (`PM1440`)
- Contract the possible worlds by adding one constraint. (`PM1441`)
- Distinguish what could happen from what could have happened. (`PM1442`)
- Distinguish what must happen from what should happen. (`PM1443`)
- Distinguish what is allowed from what is enabled. (`PM1444`)
- Represent possibility relative to a specified actor's capacities. (`PM1445`)
- Represent possibility relative to a specified time horizon. (`PM1446`)
- Represent necessity relative to a declared target and conditions. (`PM1447`)
- Preserve several incompatible possible continuations. (`PM1448`)
- Examine consequences common to every retained possibility. (`PM1449`)
- Examine possibilities excluded by the perspective's own vocabulary. (`PM1450`)

## Causation

- Replace temporal succession with a separately justified causal relation. (`PM1451`)
- Replace correlation with competing causal explanations. (`PM1452`)
- Replace a single cause with jointly necessary contributing conditions. (`PM1453`)
- Replace many listed causes with their interaction structure. (`PM1454`)
- Separate initiating causes from sustaining causes. (`PM1455`)
- Separate enabling conditions from triggering events. (`PM1456`)
- Separate background conditions from difference-making interventions. (`PM1457`)
- Separate direct effects from effects through intermediaries. (`PM1458`)
- Separate common causes from influence between observed variables. (`PM1459`)
- Separate reverse causation from the originally proposed direction. (`PM1460`)
- Separate selection effects from effects within the selected group. (`PM1461`)
- Separate individual causal pathways from aggregate associations. (`PM1462`)
- Trace the same outcome backward through alternative causal paths. (`PM1463`)
- Trace one intervention forward through several affected processes. (`PM1464`)
- Represent feedback where effects alter their own causes. (`PM1465`)
- Represent causes whose effects depend on operating conditions. (`PM1466`)
- Represent interactions where combined effects differ from separate effects. (`PM1467`)
- Represent causal redundancy where several paths can produce the outcome. (`PM1468`)
- Represent causes sufficient only alongside unmentioned background conditions. (`PM1469`)
- Change the contrast outcome to change the causal question. (`PM1470`)
- Change the intervention while preserving the observational description. (`PM1471`)
- Compare observational expectations with intervention expectations. (`PM1472`)
- Treat absent prevention as distinct from active production. (`PM1473`)
- Distinguish causal contribution from moral responsibility. (`PM1474`)
- Treat causal framing as dependent on the chosen explanatory target. (`PM1475`)

## Mechanisms

- Replace a mechanism name with its interacting components. (`PM1476`)
- Replace a causal arrow with intervening events. (`PM1477`)
- Represent the mechanism through material transfers. (`PM1478`)
- Represent the mechanism through energy transfers. (`PM1479`)
- Represent the mechanism through information-dependent state changes. (`PM1480`)
- Represent the mechanism through resource allocation changes. (`PM1481`)
- Represent the mechanism through incentives and responses. (`PM1482`)
- Represent the mechanism through constraints on available transitions. (`PM1483`)
- Separate the mechanism's trigger from its operating conditions. (`PM1484`)
- Separate activation from amplification. (`PM1485`)
- Separate transmission from transformation. (`PM1486`)
- Separate accumulation from threshold crossing. (`PM1487`)
- Separate storage from later retrieval. (`PM1488`)
- Separate detection from response selection. (`PM1489`)
- Separate response selection from execution. (`PM1490`)
- Separate execution from successful effect. (`PM1491`)
- Represent where a mechanism can be interrupted. (`PM1492`)
- Represent where a mechanism can be bypassed. (`PM1493`)
- Represent where alternative mechanisms can compensate. (`PM1494`)
- Represent where no alternative can compensate. (`PM1495`)
- Compare different mechanisms producing the same observed result. (`PM1496`)
- Compare the same mechanism under different surrounding conditions. (`PM1497`)
- Explain mechanism failure through a missing required interaction. (`PM1498`)
- Retain an unexplained link as a gap rather than a label. (`PM1499`)
- Change mechanism granularity until competing accounts make different predictions. (`PM1500`)

## Explanation

- Explain an outcome through the mechanism producing it. (`PM1501`)
- Explain an outcome through the constraints excluding alternatives. (`PM1502`)
- Explain an outcome through the history making it reachable. (`PM1503`)
- Explain an outcome through the function it serves. (`PM1504`)
- Explain an outcome through the selection process retaining it. (`PM1505`)
- Explain an action through the actor's reasons. (`PM1506`)
- Explain an action through situational pressures independent of stated reasons. (`PM1507`)
- Explain a pattern through interactions among individually simple processes. (`PM1508`)
- Explain stability through compensating changes. (`PM1509`)
- Explain variation through differences in initial conditions. (`PM1510`)
- Replace a just-so account with discriminating predictions. (`PM1511`)
- Replace a description of sequence with an account of dependence. (`PM1512`)
- Replace one encompassing explanation with condition-specific explanations. (`PM1513`)
- Replace separate explanations with a shared mechanism where justified. (`PM1514`)
- Separate explanation of occurrence from explanation of timing. (`PM1515`)
- Separate explanation of timing from explanation of magnitude. (`PM1516`)
- Separate explanation of persistence from explanation of origin. (`PM1517`)
- Separate explanation of one case from explanation of a pattern. (`PM1518`)
- Separate understanding how from knowing why this rather than that. (`PM1519`)
- Change the contrast case to expose a different explanatory demand. (`PM1520`)
- Change the explanatory audience's assumed knowledge. (`PM1521`)
- Stop explanation at a different explicitly accepted background condition. (`PM1522`)
- Reopen a background condition previously treated as explanatory bedrock. (`PM1523`)
- Retain competing explanations with different unresolved predictions. (`PM1524`)
- Treat explanatory satisfaction as distinct from explanatory adequacy. (`PM1525`)

## Counterfactuals

- Remove one event and trace the remaining possible sequence. (`PM1526`)
- Add one event and trace its downstream consequences. (`PM1527`)
- Change the timing of an event while preserving its content. (`PM1528`)
- Change the actor while preserving the action and circumstances. (`PM1529`)
- Change the setting while preserving the actor and action. (`PM1530`)
- Change the outcome while searching for the smallest sufficient alteration. (`PM1531`)
- Keep the outcome fixed while replacing the causal path. (`PM1532`)
- Keep resources fixed while replacing their allocation. (`PM1533`)
- Keep the goal fixed while replacing all customary methods. (`PM1534`)
- Keep the question fixed while replacing the available categories. (`PM1535`)
- Keep the evidence fixed while imagining an alternative true explanation. (`PM1536`)
- Assume the intervention never occurred and reconstruct the likely baseline. (`PM1537`)
- Assume the intervention occurred earlier and track timing effects. (`PM1538`)
- Assume the intervention occurred later and track lost opportunities. (`PM1539`)
- Assume perfect information while preserving limited control. (`PM1540`)
- Assume perfect control while preserving limited information. (`PM1541`)
- Assume unlimited resources while preserving the same physical constraints. (`PM1542`)
- Assume the main constraint is inverted in an imaginary case. (`PM1543`)
- Assume every participant knows what the others know. (`PM1544`)
- Assume nobody knows the eventual outcome. (`PM1545`)
- Assume the favored option fails despite correct execution. (`PM1546`)
- Assume the rejected option succeeds and reconstruct enabling conditions. (`PM1547`)
- Distinguish minimally changed worlds from radically redesigned worlds. (`PM1548`)
- Retain multiple counterfactual outcomes when the intervention underdetermines them. (`PM1549`)
- Treat counterfactual dependence separately from historical causal attribution. (`PM1550`)

## Constraints

- Replace an assumed hard constraint with a negotiable condition. (`PM1551`)
- Replace a negotiable preference with a hard constraint experimentally. (`PM1552`)
- Separate physical constraints from institutional rules. (`PM1553`)
- Separate institutional rules from customary practice. (`PM1554`)
- Separate customary practice from personal reluctance. (`PM1555`)
- Separate resource limits from allocation decisions. (`PM1556`)
- Separate present constraints from constraints expected to change. (`PM1557`)
- Separate constraints on outcomes from constraints on methods. (`PM1558`)
- Separate constraints on one actor from constraints on the whole system. (`PM1559`)
- Separate constraints that bind now from constraints that bind later. (`PM1560`)
- Remove one constraint and reconstruct the feasible alternatives. (`PM1561`)
- Add one constraint to expose hidden dependencies. (`PM1562`)
- Invert a constraint inside a declared thought experiment. (`PM1563`)
- Transfer a constraint from the result to the process. (`PM1564`)
- Transfer a constraint from the process to the result. (`PM1565`)
- Relax a constraint by a specified amount. (`PM1566`)
- Tighten a constraint until the current interpretation fails. (`PM1567`)
- Represent interacting constraints instead of listing them independently. (`PM1568`)
- Represent who can modify each constraint. (`PM1569`)
- Represent the cost of changing each constraint. (`PM1570`)
- Represent exceptions and waiver conditions explicitly. (`PM1571`)
- Treat a constraint as an output of another process. (`PM1572`)
- Treat a useful constraint as an enabling structure. (`PM1573`)
- Keep necessary constraints while removing inherited ones. (`PM1574`)
- Make the legitimacy of an imposed constraint a separate question. (`PM1575`)

## Feasibility

- Replace imagined possibility with an executable path from the present. (`PM1576`)
- Replace one-step reachability with a sequence of feasible transitions. (`PM1577`)
- Separate technical feasibility from organizational feasibility. (`PM1578`)
- Separate organizational feasibility from participant willingness. (`PM1579`)
- Separate participant willingness from available authority. (`PM1580`)
- Separate resource sufficiency from resource availability at the required time. (`PM1581`)
- Separate feasibility once from feasibility under repeated use. (`PM1582`)
- Separate demonstration feasibility from ordinary operating feasibility. (`PM1583`)
- Separate individual feasibility from simultaneous collective feasibility. (`PM1584`)
- Separate feasibility under ideal conditions from feasibility under disturbance. (`PM1585`)
- Represent prerequisites missing from the current state. (`PM1586`)
- Represent intermediate states that create later capabilities. (`PM1587`)
- Represent temporary infeasibility resolved by sequencing. (`PM1588`)
- Represent mutually incompatible commitments blocking an otherwise feasible option. (`PM1589`)
- Represent bottlenecks that dominate available capacity. (`PM1590`)
- Represent substitutions that remove dependence on one scarce input. (`PM1591`)
- Represent partial solutions that make the remaining task feasible. (`PM1592`)
- Replace yes-no feasibility with feasible regions under varying conditions. (`PM1593`)
- Replace a single feasible path with alternative feasible paths. (`PM1594`)
- Compare an option's feasibility before and after environmental change. (`PM1595`)
- Treat inaccessible existing capacity as distinct from absent capacity. (`PM1596`)
- Treat execution risk separately from formal feasibility. (`PM1597`)
- Include setup, maintenance, and recovery in the feasibility account. (`PM1598`)
- Retain infeasible options as possibilities conditional on specific changes. (`PM1599`)
- Make feasibility claims relative to a named actor and time. (`PM1600`)

## Alternatives

- Add the option of doing nothing deliberately. (`PM1601`)
- Add the option of stopping an ongoing activity. (`PM1602`)
- Add the option of postponing until a condition changes. (`PM1603`)
- Add the option of obtaining information before choosing. (`PM1604`)
- Add the option of changing the question being decided. (`PM1605`)
- Add the option of changing who decides. (`PM1606`)
- Add the option of dividing the decision into parts. (`PM1607`)
- Add the option of combining compatible parts of existing options. (`PM1608`)
- Add the option of reversing a previous commitment. (`PM1609`)
- Add the option of modifying the environment instead of the actor. (`PM1610`)
- Add the option of changing the goal instead of the method. (`PM1611`)
- Add an alternative from a different domain's operating logic. (`PM1612`)
- Add an alternative that violates an untested customary restriction. (`PM1613`)
- Add a deliberately extreme alternative to expose hidden dimensions. (`PM1614`)
- Add intermediate alternatives between two apparent extremes. (`PM1615`)
- Split one option into variants with different risks. (`PM1616`)
- Split one option into variants with different beneficiaries. (`PM1617`)
- Merge options whose practical consequences are equivalent in scope. (`PM1618`)
- Remove dominated options under the explicitly chosen criteria. (`PM1619`)
- Restore a rejected option after its rejection condition changes. (`PM1620`)
- Separate unavailable alternatives from undesirable alternatives. (`PM1621`)
- Separate unimagined alternatives from evaluated and rejected alternatives. (`PM1622`)
- Represent uncertainty about the completeness of the option set. (`PM1623`)
- Generate alternatives before choosing the comparison criteria. (`PM1624`)
- Revise the option set when a new distinction becomes available. (`PM1625`)

## Resources

- Represent money as access to particular future activities. (`PM1626`)
- Represent time as windows in which actions remain possible. (`PM1627`)
- Represent attention as capacity to distinguish and respond. (`PM1628`)
- Represent knowledge as available results usable in later operations. (`PM1629`)
- Represent relationships as channels for coordination and access. (`PM1630`)
- Represent infrastructure as maintained access to repeated capabilities. (`PM1631`)
- Represent trust as reduced need for repeated verification. (`PM1632`)
- Represent flexibility as access to alternative future allocations. (`PM1633`)
- Separate resource quantity from resource quality. (`PM1634`)
- Separate ownership from access. (`PM1635`)
- Separate access from control over use. (`PM1636`)
- Separate nominal availability from readiness for immediate use. (`PM1637`)
- Separate renewable resources from resources depleted by use. (`PM1638`)
- Separate shareable resources from rival uses of the same resource. (`PM1639`)
- Separate storable resources from opportunities that expire. (`PM1640`)
- Separate interchangeable resources from inputs with unique functions. (`PM1641`)
- Represent hidden resources embedded in unused capacities. (`PM1642`)
- Represent resources consumed by coordination and maintenance. (`PM1643`)
- Represent resources freed by stopping an existing activity. (`PM1644`)
- Represent one resource's dependence on another resource's availability. (`PM1645`)
- Represent conversion possibilities among resource types. (`PM1646`)
- Represent losses incurred during resource conversion. (`PM1647`)
- Treat an apparent waste product as a possible input elsewhere. (`PM1648`)
- Treat an apparent abundance as unusable without complementary resources. (`PM1649`)
- Change the resource inventory when a new use becomes conceivable. (`PM1650`)

## Allocation

- Replace total resources with their distribution among uses. (`PM1651`)
- Replace stated priorities with actual resource commitments. (`PM1652`)
- Replace nominal budgets with resources available at each stage. (`PM1653`)
- Separate allocation decisions from their resulting consumption. (`PM1654`)
- Separate intentional allocation from allocation produced by defaults. (`PM1655`)
- Separate current allocation from cumulative historical allocation. (`PM1656`)
- Separate allocation to execution from allocation to learning. (`PM1657`)
- Separate allocation to production from allocation to maintenance. (`PM1658`)
- Separate allocation to exploration from allocation to exploitation. (`PM1659`)
- Separate allocation to prevention from allocation to recovery. (`PM1660`)
- Represent opportunity costs as displaced alternative uses. (`PM1661`)
- Represent coordination costs between separately beneficial uses. (`PM1662`)
- Represent bottlenecks created by unbalanced allocations. (`PM1663`)
- Represent idle capacity caused by missing complementary inputs. (`PM1664`)
- Reallocate a fixed budget hypothetically across competing uses. (`PM1665`)
- Hold allocations fixed while varying total resources. (`PM1666`)
- Hold outcomes fixed while comparing resource requirements. (`PM1667`)
- Replace equal allocation with allocation by unmet need. (`PM1668`)
- Replace allocation by need with allocation by marginal effect. (`PM1669`)
- Replace allocation by marginal effect with guaranteed minimum provision. (`PM1670`)
- Treat automatic recurring allocations as revisable commitments. (`PM1671`)
- Treat allocation rules as part of the system's causal structure. (`PM1672`)
- Trace how earlier allocations constrain later allocations. (`PM1673`)
- Trace how results of allocations change subsequent allocation rules. (`PM1674`)
- Interpret a perspective through the allocations it makes seem relevant. (`PM1675`)

## Value

- Separate what is valued from the measure used to represent it. (`PM1676`)
- Separate intrinsic value claims from instrumental value claims. (`PM1677`)
- Separate value to the chooser from value to affected people. (`PM1678`)
- Separate experienced value from anticipated value. (`PM1679`)
- Separate stated value from value revealed by tradeoffs. (`PM1680`)
- Separate enduring value from temporary salience. (`PM1681`)
- Separate value of an outcome from value of achieving it personally. (`PM1682`)
- Separate value of possession from value of access. (`PM1683`)
- Separate value of certainty from expected value of outcomes. (`PM1684`)
- Separate value of preserving options from value of immediate use. (`PM1685`)
- Replace one beneficiary's valuation with another beneficiary's valuation. (`PM1686`)
- Replace present valuation with a plausible future valuation. (`PM1687`)
- Replace market price with consequences for a specified need. (`PM1688`)
- Replace prestige with the capacities or experiences actually provided. (`PM1689`)
- Replace scarcity-based valuation with use-based valuation. (`PM1690`)
- Replace aggregate value with its distribution among recipients. (`PM1691`)
- Recognize a previously unrepresented source of value. (`PM1692`)
- Remove value attributed solely through sunk effort. (`PM1693`)
- Allow several values without forcing a common numerical scale. (`PM1694`)
- Treat conflicting values as distinct commitments requiring a choice. (`PM1695`)
- Treat a value hierarchy as revisable after concrete comparison. (`PM1696`)
- Interpret aversion as protection of a valued condition. (`PM1697`)
- Interpret indifference as equivalence under a particular concern. (`PM1698`)
- Represent value that depends on sequence or context. (`PM1699`)
- Examine how adopting a perspective changes what appears valuable. (`PM1700`)

## Evaluation

- Replace relative ranking with adequacy against a requirement. (`PM1701`)
- Replace adequacy with degree of improvement beyond the requirement. (`PM1702`)
- Replace average performance with performance in critical cases. (`PM1703`)
- Replace peak performance with reliability across ordinary cases. (`PM1704`)
- Replace output quantity with usefulness to the intended recipient. (`PM1705`)
- Replace elegance with correctness under the stated conditions. (`PM1706`)
- Replace novelty with the consequential difference produced. (`PM1707`)
- Replace agreement with explanatory or predictive adequacy. (`PM1708`)
- Replace confidence of presentation with evidential support. (`PM1709`)
- Replace ease of use with total effort over repeated use. (`PM1710`)
- Replace initial cost with total cost over the relevant horizon. (`PM1711`)
- Replace isolated success with reproducibility under changed conditions. (`PM1712`)
- Replace intention-based evaluation with consequence-based evaluation. (`PM1713`)
- Replace consequence-based evaluation with compliance with a chosen constraint. (`PM1714`)
- Separate correctness from completeness. (`PM1715`)
- Separate completeness from relevance. (`PM1716`)
- Separate relevance from practical usability. (`PM1717`)
- Separate practical usability from ease of presentation. (`PM1718`)
- Use different criteria for different failure consequences. (`PM1719`)
- Make criterion weights explicit rather than implied by one score. (`PM1720`)
- Allow a veto condition that overrides compensating advantages. (`PM1721`)
- Retain incomparability where criteria conflict without an accepted tradeoff. (`PM1722`)
- Hold criteria fixed while comparing altered perspectives. (`PM1723`)
- Hold the perspective fixed while testing criterion changes. (`PM1724`)
- Track whether apparent improvement results from changing the grading rule. (`PM1725`)

## Tradeoffs

- Represent a gain together with the sacrifice enabling it. (`PM1726`)
- Represent immediate benefit together with delayed cost. (`PM1727`)
- Represent one participant's benefit together with another's burden. (`PM1728`)
- Represent increased precision together with measurement effort. (`PM1729`)
- Represent greater detail together with reduced navigability. (`PM1730`)
- Represent flexibility together with coordination difficulty. (`PM1731`)
- Represent standardization together with lost local adaptation. (`PM1732`)
- Represent speed together with reduced opportunity for correction. (`PM1733`)
- Represent redundancy together with unused ordinary capacity. (`PM1734`)
- Represent transparency together with exposure of sensitive information. (`PM1735`)
- Represent stability together with reduced responsiveness. (`PM1736`)
- Represent exploration together with forgone immediate production. (`PM1737`)
- Represent central control together with lost local discretion. (`PM1738`)
- Represent simplicity together with distinctions removed. (`PM1739`)
- Represent comprehensiveness together with attention required. (`PM1740`)
- Separate unavoidable tradeoffs from tradeoffs imposed by current design. (`PM1741`)
- Search for alternatives improving one criterion without worsening another. (`PM1742`)
- Replace a single optimum with a frontier of nondominated options. (`PM1743`)
- Make the acceptable exchange rate between criteria explicit. (`PM1744`)
- Refuse compensation across criteria treated as nonexchangeable. (`PM1745`)
- Compare tradeoffs at different scales of deployment. (`PM1746`)
- Compare tradeoffs before and after a prerequisite is satisfied. (`PM1747`)
- Include the cost of delaying the tradeoff decision. (`PM1748`)
- Treat an apparent compromise as a possible failure on both criteria. (`PM1749`)
- Treat the framing of the tradeoff as itself revisable. (`PM1750`)

## Risk

- Separate probability of harm from severity of harm. (`PM1751`)
- Separate expected loss from the worst credible loss. (`PM1752`)
- Separate reversible harm from irreversible harm. (`PM1753`)
- Separate individual risk from cumulative repeated exposure. (`PM1754`)
- Separate independent failures from failures sharing a common cause. (`PM1755`)
- Separate risk borne by the chooser from risk imposed on others. (`PM1756`)
- Separate voluntary exposure from exposure without meaningful choice. (`PM1757`)
- Separate known risks from uncertainty about unrecognized failure modes. (`PM1758`)
- Separate the chance of failure from the chance of failed recovery. (`PM1759`)
- Separate ordinary operating risk from transition risk. (`PM1760`)
- Represent the sequence required for a severe outcome. (`PM1761`)
- Represent barriers interrupting each failure path. (`PM1762`)
- Represent conditions under which several barriers fail together. (`PM1763`)
- Represent risk transferred outside the chosen system boundary. (`PM1764`)
- Represent risk created by delaying an intervention. (`PM1765`)
- Represent risk created by adopting the proposed remedy. (`PM1766`)
- Replace one risk score with distinct failure scenarios. (`PM1767`)
- Replace frequent minor failures with attention to rare severe failures. (`PM1768`)
- Replace rare severe failures with attention to cumulative ordinary losses. (`PM1769`)
- Compare risks against an explicitly specified alternative. (`PM1770`)
- Compare risks over equal exposure periods. (`PM1771`)
- Include uncertainty in both probability and consequence estimates. (`PM1772`)
- Change acceptable risk according to reversibility and affected parties. (`PM1773`)
- Treat robustness across scenarios separately from expected performance. (`PM1774`)
- Interpret a perspective by the failure paths it makes visible. (`PM1775`)

## Error

- Treat an error as evidence of a missing distinction. (`PM1776`)
- Treat an error as evidence of a wrong transformation. (`PM1777`)
- Treat an error as evidence of a wrong operation order. (`PM1778`)
- Treat an error as evidence of a misunderstood goal. (`PM1779`)
- Treat an error as evidence of inadequate information access. (`PM1780`)
- Treat an error as evidence of failed execution despite correct selection. (`PM1781`)
- Treat an error as evidence of a misleading evaluation rule. (`PM1782`)
- Separate false positives from false negatives. (`PM1783`)
- Separate random deviations from systematic bias. (`PM1784`)
- Separate local mistakes from defects in the general procedure. (`PM1785`)
- Separate detectable errors from errors the process cannot reveal. (`PM1786`)
- Separate corrected errors from errors whose consequences persist. (`PM1787`)
- Separate error in the result from error in its explanation. (`PM1788`)
- Separate error in measurement from error in interpretation. (`PM1789`)
- Separate overgeneralization from undergeneralization. (`PM1790`)
- Separate omission from incorrect inclusion. (`PM1791`)
- Separate premature rejection from premature acceptance. (`PM1792`)
- Separate overconfidence from incorrect belief content. (`PM1793`)
- Trace the earliest point where the error became preventable. (`PM1794`)
- Trace the latest point where the error remained recoverable. (`PM1795`)
- Compare the cost of different error types explicitly. (`PM1796`)
- Change the decision threshold to redistribute error types. (`PM1797`)
- Retain a useful method while isolating its known failure conditions. (`PM1798`)
- Replace blame for an error with an account of its production. (`PM1799`)
- Use recurring errors to revise the perspective's own categories. (`PM1800`)

## Exceptions

- Treat an exception as a boundary on the rule's scope. (`PM1801`)
- Treat an exception as evidence for a missing condition. (`PM1802`)
- Treat an exception as a distinct mechanism requiring separate treatment. (`PM1803`)
- Treat an exception as a possible observation error. (`PM1804`)
- Treat an exception as a case selected by a different process. (`PM1805`)
- Treat an exception as the first member of an unrecognized class. (`PM1806`)
- Treat a supposed exception as evidence that the rule is wrong. (`PM1807`)
- Treat repeated exceptions as a possible ordinary operating regime. (`PM1808`)
- Separate genuine exceptions from ambiguous membership cases. (`PM1809`)
- Separate exceptions to a prediction from exceptions to a definition. (`PM1810`)
- Separate permitted exceptions from unacknowledged rule violations. (`PM1811`)
- Separate rare cases from cases that contradict the rule. (`PM1812`)
- Compare the exception with its nearest conforming case. (`PM1813`)
- Compare several exceptions for a shared hidden feature. (`PM1814`)
- Revise the rule minimally to accommodate a supported exception. (`PM1815`)
- Replace the rule when local repairs destroy its discrimination. (`PM1816`)
- Preserve the rule while representing unresolved exceptions explicitly. (`PM1817`)
- Change which case is treated as normal and which as exceptional. (`PM1818`)
- Generate a boundary case designed to defeat an overbroad rule. (`PM1819`)
- Track the cost of adding each exception clause. (`PM1820`)
- Track who benefits from being classified as an exception. (`PM1821`)
- Track whether exceptions arise under particular operating conditions. (`PM1822`)
- Use exceptions to expose the rule's unspoken purpose. (`PM1823`)
- Use exceptions to distinguish a descriptive rule from an enforced rule. (`PM1824`)
- Make exception handling part of the operative perspective. (`PM1825`)

## Conflict and contradiction

- Separate conflicting claims from conflicting preferences. (`PM1826`)
- Separate conflicting preferences from incompatible actions. (`PM1827`)
- Separate incompatible actions from competition for one resource. (`PM1828`)
- Separate logical contradiction from different scopes of application. (`PM1829`)
- Separate contradiction in terms from a shift in meanings. (`PM1830`)
- Separate disagreement about facts from disagreement about criteria. (`PM1831`)
- Separate disagreement about criteria from disagreement about authority. (`PM1832`)
- Separate disagreement about goals from disagreement about means. (`PM1833`)
- Represent each conflicting position in its own strongest terms. (`PM1834`)
- Identify the smallest proposition on which the positions diverge. (`PM1835`)
- Identify commitments shared despite the visible disagreement. (`PM1836`)
- Retain contradictory accounts under separate assumptions. (`PM1837`)
- Allow unresolved conflict to guide further evidence collection. (`PM1838`)
- Replace forced consensus with explicit conditional conclusions. (`PM1839`)
- Replace a compromise with separate solutions for distinct subproblems. (`PM1840`)
- Replace a binary dispute with several independent dimensions. (`PM1841`)
- Treat a contradiction as evidence of a defective representation. (`PM1842`)
- Treat a contradiction as evidence of genuinely incompatible requirements. (`PM1843`)
- Treat a contradiction as information about different observation positions. (`PM1844`)
- Change the unit of analysis until the apparent conflict separates. (`PM1845`)
- Change the time horizon to expose a temporal conflict. (`PM1846`)
- Change the beneficiary to expose a distributional conflict. (`PM1847`)
- Represent tradeoffs hidden by claims of universal agreement. (`PM1848`)
- Preserve productive tension instead of immediately synthesizing positions. (`PM1849`)
- Examine whether resolving the conflict would destroy a useful distinction. (`PM1850`)

## Agency

- Replace a trait account with the actor's available choices. (`PM1851`)
- Replace an individual-choice account with interacting decisions. (`PM1852`)
- Replace an intentional account with an automatic process account. (`PM1853`)
- Replace an automatic-process account with possible deliberate intervention points. (`PM1854`)
- Separate capacity to act from authority to act. (`PM1855`)
- Separate authority to act from practical control of implementation. (`PM1856`)
- Separate intention from effort. (`PM1857`)
- Separate effort from successful influence. (`PM1858`)
- Separate choosing an action from choosing the available options. (`PM1859`)
- Separate initiating a process from controlling its continuation. (`PM1860`)
- Represent degrees of agency instead of all-or-nothing agency. (`PM1861`)
- Represent agency distributed across several participants. (`PM1862`)
- Represent agency constrained by dependencies on others. (`PM1863`)
- Represent collective capacity absent from any individual alone. (`PM1864`)
- Represent an actor's ability to refuse or exit. (`PM1865`)
- Represent an actor's ability to change the rules. (`PM1866`)
- Represent an actor's ability to redirect attention. (`PM1867`)
- Represent an actor's ability to revise their own commitments. (`PM1868`)
- View an apparent object as a participant with responses. (`PM1869`)
- View an apparent decision-maker as implementing another's selection. (`PM1870`)
- Treat a default as an allocation of decision power. (`PM1871`)
- Treat nonintervention as a possible exercise of agency. (`PM1872`)
- Treat compelled compliance separately from chosen agreement. (`PM1873`)
- Compare action possibilities before and after perspective change. (`PM1874`)
- Locate agency at the level where alternatives are actually resolved. (`PM1875`)

## Control

- Separate observing a variable from being able to change it. (`PM1876`)
- Separate direct control from indirect influence. (`PM1877`)
- Separate influence from reliable outcome control. (`PM1878`)
- Separate control of inputs from control of resulting outputs. (`PM1879`)
- Separate starting a process from stopping it. (`PM1880`)
- Separate stopping a process from reversing its effects. (`PM1881`)
- Separate nominal control from control exercised in practice. (`PM1882`)
- Separate centralized authority from distributed operational control. (`PM1883`)
- Represent controllable variables separately from external conditions. (`PM1884`)
- Represent control delays between action and observed response. (`PM1885`)
- Represent limits beyond which control inputs stop being effective. (`PM1886`)
- Represent disturbances the available controls cannot compensate for. (`PM1887`)
- Represent competing controllers acting on the same process. (`PM1888`)
- Represent who can override whose decision. (`PM1889`)
- Represent conditions that transfer control between participants. (`PM1890`)
- Replace open-loop action with feedback-dependent action in the model. (`PM1891`)
- Replace continuous intervention with event-triggered intervention. (`PM1892`)
- Replace rigid control with a range of acceptable states. (`PM1893`)
- Replace control of behavior with control of enabling conditions. (`PM1894`)
- Replace control of outcomes with preservation of correction opportunities. (`PM1895`)
- Treat increased observability as distinct from increased control. (`PM1896`)
- Treat lost control as distinct from ordinary variability. (`PM1897`)
- Locate the smallest missing control operation preventing reliable execution. (`PM1898`)
- Include interruptibility in the description of a usable capability. (`PM1899`)
- Treat the control policy itself as a possible object of revision. (`PM1900`)

## Responsibility

- Separate causal contribution from duty to prevent. (`PM1901`)
- Separate duty to prevent from capacity to prevent. (`PM1902`)
- Separate responsibility for a decision from responsibility for implementation. (`PM1903`)
- Separate responsibility for intent from responsibility for foreseeable effects. (`PM1904`)
- Separate individual responsibility from shared institutional responsibility. (`PM1905`)
- Separate fault for an outcome from responsibility for repair. (`PM1906`)
- Separate accountability to explain from liability for consequences. (`PM1907`)
- Separate assigned responsibility from actual decision authority. (`PM1908`)
- Separate authority held from authority meaningfully exercisable. (`PM1909`)
- Represent responsibility distributed across a sequence of handoffs. (`PM1910`)
- Represent responsibility for maintaining the enabling conditions. (`PM1911`)
- Represent responsibility for detecting foreseeable failures. (`PM1912`)
- Represent responsibility for communicating known limitations. (`PM1913`)
- Represent responsibility for correcting an inherited defect. (`PM1914`)
- Represent responsibility for harms displaced beyond formal boundaries. (`PM1915`)
- Evaluate a decision using information available when it was made. (`PM1916`)
- Evaluate later conduct using information learned after the decision. (`PM1917`)
- Compare responsibility under alternative role assignments. (`PM1918`)
- Compare responsibility before and after a valid delegation. (`PM1919`)
- Treat omission separately from active production of an outcome. (`PM1920`)
- Treat compelled action separately from freely chosen action. (`PM1921`)
- Locate gaps where everyone assumes someone else is responsible. (`PM1922`)
- Locate overlaps where responsibility is shared but coordination is absent. (`PM1923`)
- Replace a blame-centered account with a repair-allocation account. (`PM1924`)
- Make the responsibility criterion explicit before assigning responsibility. (`PM1925`)

## Stakeholders

- View the arrangement from the direct recipient's position. (`PM1926`)
- View the arrangement from the person performing the work. (`PM1927`)
- View the arrangement from the person funding the work. (`PM1928`)
- View the arrangement from the person maintaining the result. (`PM1929`)
- View the arrangement from the person excluded from participation. (`PM1930`)
- View the arrangement from the person bearing indirect costs. (`PM1931`)
- View the arrangement from a future recipient's position. (`PM1932`)
- View the arrangement from a participant unable to exit. (`PM1933`)
- View the arrangement from a participant free to leave. (`PM1934`)
- View the arrangement from a newcomer without background knowledge. (`PM1935`)
- View the arrangement from an experienced participant facing accumulated burdens. (`PM1936`)
- View the arrangement from the person handling exceptional cases. (`PM1937`)
- View the arrangement from the person repairing failures. (`PM1938`)
- View the arrangement from the person whose work remains invisible. (`PM1939`)
- Separate beneficiaries from decision-makers. (`PM1940`)
- Separate nominal representatives from the people they represent. (`PM1941`)
- Separate expressed interests from unexpressed affected interests. (`PM1942`)
- Separate immediate stakeholders from indirectly affected groups. (`PM1943`)
- Represent differences within an apparently unified stakeholder group. (`PM1944`)
- Represent conflicts between one stakeholder's short and long horizons. (`PM1945`)
- Add a stakeholder omitted by the original system boundary. (`PM1946`)
- Remove a presumed stakeholder after checking actual exposure. (`PM1947`)
- Weight stakeholder accounts by relevant access rather than status. (`PM1948`)
- Compare what each stakeholder can know, change, gain, and lose. (`PM1949`)
- Treat stakeholder selection as a consequential perspective choice. (`PM1950`)

## Self and other

- Describe your action using the terms applied to another's action. (`PM1951`)
- Describe another's action using the context granted to your own. (`PM1952`)
- Exchange the roles of actor and recipient in imagination. (`PM1953`)
- Exchange the roles of evaluator and evaluated in imagination. (`PM1954`)
- Adopt the other person's stated goal without accepting their factual beliefs. (`PM1955`)
- Adopt the other person's factual beliefs without adopting their goal. (`PM1956`)
- Reconstruct the other person's available alternatives at the decision point. (`PM1957`)
- Reconstruct what the other person expects you to do next. (`PM1958`)
- Separate your intention from the effect experienced by the other. (`PM1959`)
- Separate the other's intention from the effect you experience. (`PM1960`)
- Separate disagreement with you from hostility toward you. (`PM1961`)
- Separate agreement with you from independent understanding. (`PM1962`)
- Treat your own interpretation as one situated account. (`PM1963`)
- Treat another's interpretation as internally organized rather than merely mistaken. (`PM1964`)
- Observe your reaction as an event requiring interpretation. (`PM1965`)
- View your current choice from your earlier stated commitments. (`PM1966`)
- View your earlier commitment from your current circumstances. (`PM1967`)
- Describe yourself in third person to expose asymmetric standards. (`PM1968`)
- Describe another in first person to reconstruct their practical situation. (`PM1969`)
- Retain the other's differences instead of assimilating them to yourself. (`PM1970`)
- Represent mutual misunderstandings as interacting perspective differences. (`PM1971`)
- Distinguish imagining another's experience from evidence about that experience. (`PM1972`)
- Let the other's response revise your model of their perspective. (`PM1973`)
- Let the other's model of you revise your self-description. (`PM1974`)
- Compare which distinctions become available after exchanging personal positions. (`PM1975`)

## Knowledge access

- Restrict the account to what one participant can observe directly. (`PM1976`)
- Add information available only to another participant. (`PM1977`)
- Remove hindsight unavailable at the original decision. (`PM1978`)
- Distinguish public knowledge from privately held knowledge. (`PM1979`)
- Distinguish shared information from knowledge that it is shared. (`PM1980`)
- Distinguish knowing a rule from knowing when it applies. (`PM1981`)
- Distinguish recognizing an answer from generating it independently. (`PM1982`)
- Distinguish possessing a record from being able to find it. (`PM1983`)
- Distinguish finding information from recognizing its relevance. (`PM1984`)
- Distinguish understanding information from being able to act on it. (`PM1985`)
- Represent information barriers between roles. (`PM1986`)
- Represent delays in receiving updated information. (`PM1987`)
- Represent unequal ability to verify the same claim. (`PM1988`)
- Represent what each actor falsely assumes others know. (`PM1989`)
- Represent what each actor falsely assumes others do not know. (`PM1990`)
- Represent information lost during organizational handoffs. (`PM1991`)
- Represent hidden expertise outside formally recognized roles. (`PM1992`)
- View a procedure from the position of someone missing one prerequisite. (`PM1993`)
- View a procedure from the position of someone holding tacit knowledge. (`PM1994`)
- Remove expert shortcuts to reconstruct the novice's available inferences. (`PM1995`)
- Restore expert distinctions concealed by a simplified account. (`PM1996`)
- Change access to information while preserving the actor's capacities. (`PM1997`)
- Change interpretive capacities while preserving the available information. (`PM1998`)
- Combine complementary access without pretending any participant knows everything. (`PM1999`)
- Treat knowledge access as a variable shaping reasonable interpretation. (`PM2000`)

## Embodiment

- Interpret the environment through actions possible from the current posture. (`PM2001`)
- Interpret the environment through the actor's actual movement range. (`PM2002`)
- Interpret an obstacle relative to the actor's bodily capacities. (`PM2003`)
- Interpret a tool through the movements required to use it. (`PM2004`)
- Interpret a task through the fatigue its execution accumulates. (`PM2005`)
- Interpret a space through the effort required to navigate it. (`PM2006`)
- Interpret a sequence through coordination demands on the body. (`PM2007`)
- Interpret a delay through the discomfort of physically waiting. (`PM2008`)
- Separate knowing a movement from being able to perform it. (`PM2009`)
- Separate describing an experience from undergoing its bodily demands. (`PM2010`)
- Separate bodily discomfort from the judgment attached to it. (`PM2011`)
- Separate limits imposed by the body from limits imposed by design. (`PM2012`)
- Compare the same task from seated and standing positions. (`PM2013`)
- Compare the same environment with different reach assumptions. (`PM2014`)
- Compare the same activity before and after ordinary physical rest. (`PM2015`)
- Compare imagined movement with safely performed movement. (`PM2016`)
- Shift from visual description to the felt sequence of actions. (`PM2017`)
- Shift from detached observation to ordinary direct participation. (`PM2018`)
- Attend to balance, resistance, and effort during a familiar action. (`PM2019`)
- Represent tools as changing the body's effective action range. (`PM2020`)
- Represent bodily vulnerability as relevant to the situation's meaning. (`PM2021`)
- Represent dependence on assistance without erasing the person's agency. (`PM2022`)
- Represent bodily capacities as varying across conditions and time. (`PM2023`)
- Replace an assumed standard body with the actual user's capacities. (`PM2024`)
- Make bodily position part of the account of what appears possible. (`PM2025`)

## Sensation

- Inspect the same object through touch instead of sight where safe. (`PM2026`)
- Inspect an event through sound instead of its visual record. (`PM2027`)
- Inspect a process through its rhythm instead of its verbal description. (`PM2028`)
- Separate raw sensory differences from the labels assigned to them. (`PM2029`)
- Separate signal intensity from interpreted importance. (`PM2030`)
- Separate a sensory absence from inability to detect the signal. (`PM2031`)
- Attend to peripheral sensations previously excluded from awareness. (`PM2032`)
- Attend to changes in sensation rather than constant stimulation. (`PM2033`)
- Attend to constancy beneath changing sensory appearances. (`PM2034`)
- Compare the same object under different ordinary lighting. (`PM2035`)
- Compare the same sound within different surrounding sounds. (`PM2036`)
- Compare a detail in isolation and within the full scene. (`PM2037`)
- Remove one sensory channel from an imagined reconstruction. (`PM2038`)
- Add a complementary sensory channel to an observation. (`PM2039`)
- Translate a visible pattern into an audible pattern. (`PM2040`)
- Translate a temporal pattern into a spatial arrangement. (`PM2041`)
- Translate a quantitative difference into an accessible sensory contrast. (`PM2042`)
- Represent what the available senses systematically cannot detect. (`PM2043`)
- Use an instrument to reveal a previously inaccessible signal. (`PM2044`)
- Distinguish instrument output from direct sensory experience. (`PM2045`)
- Reduce competing stimulation to expose a subtle difference. (`PM2046`)
- Increase contextual stimulation to examine real-world detectability. (`PM2047`)
- Track how repeated exposure changes what is noticed. (`PM2048`)
- Treat sensory uncertainty separately from conceptual uncertainty. (`PM2049`)
- Let an unexpected sensation revise the object's assumed identity. (`PM2050`)

## Emotion

- Interpret anger as an appraisal of a violated expectation. (`PM2051`)
- Interpret fear as an appraisal of threatened possibilities. (`PM2052`)
- Interpret relief as a change in expected demands. (`PM2053`)
- Interpret disappointment through the expectation that preceded it. (`PM2054`)
- Interpret envy through the comparison it makes salient. (`PM2055`)
- Interpret guilt through the responsibility being attributed. (`PM2056`)
- Interpret shame through the imagined audience and standard. (`PM2057`)
- Interpret pride through the achievement and comparison being claimed. (`PM2058`)
- Interpret boredom through the absence of a compelling available activity. (`PM2059`)
- Interpret curiosity through an unresolved consequential difference. (`PM2060`)
- Separate an emotion from the factual claim accompanying it. (`PM2061`)
- Separate emotional intensity from the importance of the underlying issue. (`PM2062`)
- Separate immediate feeling from the action selected in response. (`PM2063`)
- Separate your feeling from your estimate of another's feeling. (`PM2064`)
- View the same event after the immediate emotional peak passes. (`PM2065`)
- Imagine the event with a different expectation of its outcome. (`PM2066`)
- Imagine the event affecting someone toward whom you feel differently. (`PM2067`)
- Replace a threat reading with a challenge reading provisionally. (`PM2068`)
- Replace a challenge reading with a loss reading provisionally. (`PM2069`)
- Replace a personal insult reading with a situational-friction reading. (`PM2070`)
- Restore an emotion excluded by an overly detached account. (`PM2071`)
- Attend to mixed emotions without selecting one as the real emotion. (`PM2072`)
- Represent emotional changes across stages of the same event. (`PM2073`)
- Treat emotional response as evidence about appraisal, not proof of facts. (`PM2074`)
- Change the appraisal and observe whether the experienced meaning changes. (`PM2075`)

## Desire and aversion

- Separate wanting an object from wanting the experience associated with it. (`PM2076`)
- Separate wanting an outcome from wanting recognition for achieving it. (`PM2077`)
- Separate wanting access from wanting exclusive possession. (`PM2078`)
- Separate wanting relief from wanting the proposed remedy. (`PM2079`)
- Separate attraction to novelty from value under repeated exposure. (`PM2080`)
- Separate aversion to effort from aversion to the intended result. (`PM2081`)
- Separate aversion to uncertainty from aversion to a particular outcome. (`PM2082`)
- Separate rejection of a method from rejection of the goal. (`PM2083`)
- Separate current desire from a commitment chosen under other conditions. (`PM2084`)
- Separate another's expressed preference from your assumption about their need. (`PM2085`)
- Imagine the desired outcome already secured and reconsider the next concern. (`PM2086`)
- Imagine the desired outcome unavailable and identify remaining valued possibilities. (`PM2087`)
- Imagine achieving the goal without receiving recognition. (`PM2088`)
- Imagine receiving recognition without achieving the goal. (`PM2089`)
- Compare desire before and after obtaining relevant information. (`PM2090`)
- Compare desire before and after ordinary satisfaction of a competing need. (`PM2091`)
- Compare aversion to the task with aversion to its framing. (`PM2092`)
- Treat desire as revisable rather than a fixed input. (`PM2093`)
- Treat aversion as a possible warning about an unrepresented cost. (`PM2094`)
- Treat aversion as a learned association open to reconsideration. (`PM2095`)
- Shift from obtaining more to using what is already available. (`PM2096`)
- Shift from avoiding discomfort to accepting a chosen temporary cost. (`PM2097`)
- Shift from immediate gratification to preserving a future option. (`PM2098`)
- Allow conflicting desires to remain separately represented. (`PM2099`)
- Examine which desires the current perspective makes intelligible. (`PM2100`)

## Identity and commitment

- Separate a belief from the identity attached to holding it. (`PM2101`)
- Separate a project from the self-description built around it. (`PM2102`)
- Separate a past commitment from a present endorsement. (`PM2103`)
- Separate loyalty to a person from agreement with a claim. (`PM2104`)
- Separate belonging to a group from accepting every group position. (`PM2105`)
- Separate competence from the need to appear immediately correct. (`PM2106`)
- Separate consistency of values from consistency of particular decisions. (`PM2107`)
- Separate changing your mind from denying your earlier position. (`PM2108`)
- Separate abandoning a method from abandoning its intended purpose. (`PM2109`)
- Separate criticism of a result from rejection of its creator. (`PM2110`)
- Adopt a role temporarily without making it a permanent identity. (`PM2111`)
- Describe your identity through revisable commitments instead of fixed traits. (`PM2112`)
- Describe your identity through recurring relations instead of achievements. (`PM2113`)
- View an old commitment as a historical response to old conditions. (`PM2114`)
- View a new commitment from the obligations it would create. (`PM2115`)
- Suspend the need to defend a publicly stated position. (`PM2116`)
- Retain accountability for an earlier claim while revising it. (`PM2117`)
- Imagine holding the same evidence without having announced a conclusion. (`PM2118`)
- Imagine entering the project without its accumulated prestige. (`PM2119`)
- Imagine a successor continuing the work with different methods. (`PM2120`)
- Treat commitment strength as adjustable to evidence and reversibility. (`PM2121`)
- Treat identity protection as one influence on interpretation. (`PM2122`)
- Restore possibilities excluded because they seem unlike you. (`PM2123`)
- Compare what a role permits with what it requires. (`PM2124`)
- Let a revised perspective change the identity through which it is viewed. (`PM2125`)

## Social position

- View the situation from a newcomer lacking informal access. (`PM2126`)
- View the situation from an insider possessing informal access. (`PM2127`)
- View the situation from a subordinate exposed to evaluation. (`PM2128`)
- View the situation from a supervisor responsible for coordination. (`PM2129`)
- View the situation from someone dependent on continued approval. (`PM2130`)
- View the situation from someone able to refuse without serious loss. (`PM2131`)
- View the situation from a majority position treated as ordinary. (`PM2132`)
- View the situation from a minority position repeatedly required to explain. (`PM2133`)
- View the situation from someone whose work is publicly credited. (`PM2134`)
- View the situation from someone whose contribution is routinely absorbed. (`PM2135`)
- Separate formal rank from practical influence. (`PM2136`)
- Separate visibility from importance to the group's functioning. (`PM2137`)
- Separate social acceptance from accuracy of the accepted account. (`PM2138`)
- Separate status loss from substantive harm to the project. (`PM2139`)
- Separate politeness conventions from agreement with the content. (`PM2140`)
- Represent unequal costs of speaking honestly. (`PM2141`)
- Represent unequal consequences of making the same mistake. (`PM2142`)
- Represent unequal access to the benefit of the doubt. (`PM2143`)
- Represent informal obligations absent from formal role descriptions. (`PM2144`)
- Represent dependence on reputation when interpreting public statements. (`PM2145`)
- Exchange social positions while preserving the same conduct hypothetically. (`PM2146`)
- Compare interpretations before and after learning a participant's status. (`PM2147`)
- Remove status cues to inspect the underlying claim. (`PM2148`)
- Restore social context when anonymity conceals relevant pressures. (`PM2149`)
- Treat social position as a condition of access and consequence. (`PM2150`)

## Power and institutions

- Interpret a rule through who can enforce it. (`PM2151`)
- Interpret a rule through who can challenge it. (`PM2152`)
- Interpret a rule through who can change it. (`PM2153`)
- Interpret a procedure through who controls admission. (`PM2154`)
- Interpret a metric through who defines acceptable performance. (`PM2155`)
- Interpret a dispute through who determines the recognized vocabulary. (`PM2156`)
- Interpret a default through who benefits when nobody intervenes. (`PM2157`)
- Interpret silence through possible costs of dissent. (`PM2158`)
- Interpret consent through available alternatives to agreement. (`PM2159`)
- Interpret representation through accountability to those represented. (`PM2160`)
- Separate authority on paper from enforceable authority. (`PM2161`)
- Separate rule compliance from achievement of the rule's purpose. (`PM2162`)
- Separate institutional survival from fulfillment of institutional goals. (`PM2163`)
- Separate public justification from incentives sustaining the arrangement. (`PM2164`)
- Separate enforcement capacity from legitimacy. (`PM2165`)
- Represent agenda-setting power alongside final decision power. (`PM2166`)
- Represent gatekeeping power over information and participation. (`PM2167`)
- Represent the ability to externalize costs onto weaker parties. (`PM2168`)
- Represent the ability to delay decisions until alternatives disappear. (`PM2169`)
- Represent how exceptions are distributed across social positions. (`PM2170`)
- Compare formal procedures with informal routes to outcomes. (`PM2171`)
- Compare the institution's viewpoint with that of people outside it. (`PM2172`)
- Treat an apparently natural limit as an institutional arrangement. (`PM2173`)
- Treat an institution as a maintained coordination mechanism. (`PM2174`)
- Make the distribution of interpretive authority part of the perspective. (`PM2175`)

## Culture and convention

- Treat a familiar practice as a local convention. (`PM2176`)
- Treat an unfamiliar practice as an internally intelligible arrangement. (`PM2177`)
- Separate what is customary from what is necessary. (`PM2178`)
- Separate what is customary from what is valued. (`PM2179`)
- Separate explicit rules from tacit expectations. (`PM2180`)
- Separate shared vocabulary from shared interpretation. (`PM2181`)
- Separate a ritual's stated meaning from its coordination effects. (`PM2182`)
- Separate a symbol's historical meaning from its present use. (`PM2183`)
- Interpret a practice through the problems it originally addressed. (`PM2184`)
- Interpret a practice through the conditions sustaining it now. (`PM2185`)
- Compare the same action under different conventions of politeness. (`PM2186`)
- Compare the same obligation under different kinship assumptions. (`PM2187`)
- Compare the same exchange under different ownership conventions. (`PM2188`)
- Compare the same achievement under different status systems. (`PM2189`)
- Compare the same uncertainty under different standards of evidence. (`PM2190`)
- Translate a practice into ordinary functional terms. (`PM2191`)
- Restore meanings lost by an exclusively functional translation. (`PM2192`)
- View your own convention through an unfamiliar observer's questions. (`PM2193`)
- View another convention without requiring resemblance to your own. (`PM2194`)
- Identify variation within a supposedly uniform culture. (`PM2195`)
- Represent cultural borrowing as transformation rather than exact copying. (`PM2196`)
- Represent conventions as changing through repeated use. (`PM2197`)
- Treat a contested norm as several competing interpretations. (`PM2198`)
- Suspend the assumption that one convention must fit every context. (`PM2199`)
- Examine how a convention determines which perspectives appear respectable. (`PM2200`)

## Communication

- Interpret an utterance as a request rather than a factual report. (`PM2201`)
- Interpret an utterance as a commitment rather than a prediction. (`PM2202`)
- Interpret an utterance as a warning rather than a threat. (`PM2203`)
- Interpret an utterance as a threat rather than a neutral prediction. (`PM2204`)
- Interpret an utterance as a question rather than a challenge. (`PM2205`)
- Interpret an utterance as a challenge rather than a request for information. (`PM2206`)
- Interpret silence as unresolved instead of automatic agreement. (`PM2207`)
- Interpret brevity through context instead of presumed indifference. (`PM2208`)
- Interpret repetition as a failed attempt to establish a distinction. (`PM2209`)
- Interpret correction as specification of an unmet requirement. (`PM2210`)
- Separate what was said from what was successfully communicated. (`PM2211`)
- Separate intended tone from experienced tone. (`PM2212`)
- Separate disagreement with content from failure to understand it. (`PM2213`)
- Separate communicative success from factual correctness. (`PM2214`)
- Separate disclosure of information from creation of common understanding. (`PM2215`)
- Change the medium while preserving the intended message. (`PM2216`)
- Change the sequence of disclosure while preserving the information. (`PM2217`)
- Replace explanation with a demonstration of the relevant distinction. (`PM2218`)
- Replace assertion with a case the recipient can inspect. (`PM2219`)
- Replace persuasion with a jointly examinable comparison. (`PM2220`)
- Make the expected response explicit in interpreting an utterance. (`PM2221`)
- Represent conversational history as part of the message's meaning. (`PM2222`)
- Represent constraints on what the speaker can say openly. (`PM2223`)
- Let the listener's actual response revise the inferred communicative act. (`PM2224`)
- Interpret communication by the continuations it makes available. (`PM2225`)

## Audience

- Write for someone encountering the subject for the first time. (`PM2226`)
- Write for someone who already accepts the main conclusion. (`PM2227`)
- Write for someone who understands the facts but disputes the criterion. (`PM2228`)
- Write for someone who accepts the criterion but disputes the facts. (`PM2229`)
- Write for someone who must implement the result. (`PM2230`)
- Write for someone who must evaluate the result independently. (`PM2231`)
- Write for someone who bears the cost of an error. (`PM2232`)
- Write for someone who has little time to identify the decision. (`PM2233`)
- Write for someone seeking the strongest objection. (`PM2234`)
- Write for someone whose vocabulary differs from yours. (`PM2235`)
- Replace assumed shared background with explicitly supplied prerequisites. (`PM2236`)
- Remove prerequisites the audience already demonstrably understands. (`PM2237`)
- Replace broad reassurance with the distinction resolving the audience's concern. (`PM2238`)
- Replace detailed derivation with the exact result needed for action. (`PM2239`)
- Restore derivation when the audience must assess validity. (`PM2240`)
- Change examples to match the audience's actual decision context. (`PM2241`)
- Change examples to break an audience's habitual domain association. (`PM2242`)
- Separate what the audience wants from what they need to determine. (`PM2243`)
- Separate audience discomfort from evidence that the claim is wrong. (`PM2244`)
- Separate accessibility from dilution of substantive distinctions. (`PM2245`)
- Predict the audience's strongest logical continuation before choosing content. (`PM2246`)
- Revise the audience model after an unexpected response. (`PM2247`)
- Treat several audiences as having different information needs. (`PM2248`)
- Keep the claim fixed while changing its route to comprehension. (`PM2249`)
- Treat audience selection as shaping the perspective expressed. (`PM2250`)

## Narrative

- Replace a hero-centered account with interacting ordinary contributions. (`PM2251`)
- Replace a villain-centered account with the conditions sustaining harmful conduct. (`PM2252`)
- Replace an intention-driven story with an unintended-consequence account. (`PM2253`)
- Replace a progress narrative with gains and losses by dimension. (`PM2254`)
- Replace a decline narrative with changes in distribution and standards. (`PM2255`)
- Replace a redemption narrative with unresolved continuing consequences. (`PM2256`)
- Replace an inevitability narrative with choices available at each stage. (`PM2257`)
- Replace a single protagonist with several intersecting trajectories. (`PM2258`)
- Replace one narrative climax with multiple consequential turning points. (`PM2259`)
- Replace a completed story with an unfinished ongoing situation. (`PM2260`)
- Begin the account before its customary starting event. (`PM2261`)
- End the account after its customary resolution. (`PM2262`)
- Tell the sequence from the least represented participant's position. (`PM2263`)
- Tell the sequence backward from its consequences. (`PM2264`)
- Tell the sequence without revealing the eventual outcome. (`PM2265`)
- Tell the sequence using only contemporary observations. (`PM2266`)
- Separate event order from the order of disclosure. (`PM2267`)
- Separate a coherent story from evidence supporting each connection. (`PM2268`)
- Separate narrative importance from causal importance. (`PM2269`)
- Separate a character's self-account from independently observed conduct. (`PM2270`)
- Represent several plausible stories compatible with the same facts. (`PM2271`)
- Remove evaluative character labels and retain actions and conditions. (`PM2272`)
- Restore agency erased by an exclusively structural story. (`PM2273`)
- Restore structure erased by an exclusively personal story. (`PM2274`)
- Treat the urge for narrative closure as part of interpretation. (`PM2275`)

## Analogy

- Map a process to another process with the same dependency structure. (`PM2276`)
- Map a conflict to a different conflict with matched incentives. (`PM2277`)
- Map a resource problem to a different resource with similar constraints. (`PM2278`)
- Map a perspective to a tool with specified enabled operations. (`PM2279`)
- Map a classification dispute to another dispute over different membership rules. (`PM2280`)
- Map a conversation to a sequence of changing available continuations. (`PM2281`)
- Replace a familiar analogy with a structurally opposed analogy. (`PM2282`)
- Replace surface resemblance with an explicit relational mapping. (`PM2283`)
- Replace a mechanistic analogy with an ecological analogy provisionally. (`PM2284`)
- Replace an ecological analogy with a contractual analogy provisionally. (`PM2285`)
- Use two incompatible analogies on the same case separately. (`PM2286`)
- Transfer a successful operation across domains while preserving its prerequisites. (`PM2287`)
- Transfer a failure mode across domains and examine corresponding conditions. (`PM2288`)
- Reverse the direction of an analogy to expose asymmetry. (`PM2289`)
- Remove the analogy's familiar vocabulary and retain the mapped relations. (`PM2290`)
- Identify where the source and target cease to correspond. (`PM2291`)
- Preserve useful correspondence without importing unrelated source properties. (`PM2292`)
- Construct an analogy from a deliberately unfamiliar domain. (`PM2293`)
- Construct a minimal analogy containing only the relevant relation. (`PM2294`)
- Extend an analogy until a consequential mismatch appears. (`PM2295`)
- Treat analogy failure as evidence of a target-specific distinction. (`PM2296`)
- Replace an analogy with a literal description after it reveals structure. (`PM2297`)
- Use a concrete analogy to expose an abstract operation. (`PM2298`)
- Use an abstract correspondence to connect dissimilar concrete cases. (`PM2299`)
- Treat analogy selection as a modification of accessible inferences. (`PM2300`)

## Models

- Replace a descriptive model with a predictive model. (`PM2301`)
- Replace a predictive model with a model of interventions. (`PM2302`)
- Replace a deterministic model with a distribution over outcomes. (`PM2303`)
- Replace a probabilistic model with explicit alternative scenarios. (`PM2304`)
- Replace a static model with changing state variables. (`PM2305`)
- Replace an equilibrium model with a transition process. (`PM2306`)
- Replace a homogeneous model with heterogeneous interacting agents. (`PM2307`)
- Replace an individual-agent model with aggregate flows. (`PM2308`)
- Replace continuous dynamics with discrete decision events. (`PM2309`)
- Replace a discrete-state model with continuous variation where needed. (`PM2310`)
- Add a hidden variable required to distinguish observed cases. (`PM2311`)
- Remove a parameter unsupported by available observations. (`PM2312`)
- Separate model structure from fitted parameter values. (`PM2313`)
- Separate model fit from performance on unseen cases. (`PM2314`)
- Separate predictive adequacy from mechanistic interpretation. (`PM2315`)
- Separate simplification from a claim that omitted factors do not exist. (`PM2316`)
- Compare multiple models against the same discriminating cases. (`PM2317`)
- Retain a simpler model within its demonstrated domain. (`PM2318`)
- Use a richer model only where the simpler one fails. (`PM2319`)
- Represent model uncertainty alongside uncertainty within the model. (`PM2320`)
- Change the model's unit of analysis. (`PM2321`)
- Change which variables are inputs and which are outputs. (`PM2322`)
- Make an externally fixed variable determined within the model. (`PM2323`)
- Treat a previously internal process as an external boundary condition. (`PM2324`)
- Model the process by which models are selected and revised. (`PM2325`)

## Systems

- Interpret an outcome through interacting components and constraints. (`PM2326`)
- Interpret a component through its role in several systems. (`PM2327`)
- Interpret a failure through a broken dependency chain. (`PM2328`)
- Interpret apparent redundancy through alternative paths to the same function. (`PM2329`)
- Interpret unused capacity through missing complementary inputs. (`PM2330`)
- Interpret coordination as a substantive system function. (`PM2331`)
- Interpret maintenance as ongoing reconstruction of operating conditions. (`PM2332`)
- Interpret adaptation as changes to internal selection rules. (`PM2333`)
- Separate system purpose from the purposes of its participants. (`PM2334`)
- Separate system behavior from any participant's intention. (`PM2335`)
- Separate component reliability from reliability of the arrangement. (`PM2336`)
- Separate throughput from useful completed output. (`PM2337`)
- Separate local efficiency from overall performance. (`PM2338`)
- Represent inputs, transformations, outputs, and feedback separately. (`PM2339`)
- Represent resource flows separately from authority flows. (`PM2340`)
- Represent information flows separately from material flows. (`PM2341`)
- Represent dependencies across system boundaries. (`PM2342`)
- Represent failures that propagate between otherwise healthy components. (`PM2343`)
- Represent capacities that appear only through coordination. (`PM2344`)
- Represent goals that compete for the same internal resources. (`PM2345`)
- Change which subsystem is treated as the relevant whole. (`PM2346`)
- Replace a rigid hierarchy with distributed coordination in the model. (`PM2347`)
- Replace distributed coordination with explicit central selection in the model. (`PM2348`)
- Treat the observer and their interventions as part of the system. (`PM2349`)
- Interpret the perspective itself as an arrangement of cooperating operations. (`PM2350`)

## Networks

- Represent entities by their connections instead of isolated attributes. (`PM2351`)
- Represent connections by type instead of one undifferentiated link. (`PM2352`)
- Represent direction of influence separately from existence of contact. (`PM2353`)
- Represent connection strength separately from connection frequency. (`PM2354`)
- Represent changing connections over time. (`PM2355`)
- Represent indirect paths alongside direct connections. (`PM2356`)
- Represent missing links that prevent otherwise possible coordination. (`PM2357`)
- Represent bridges between separated groups. (`PM2358`)
- Represent bottlenecks shared by many paths. (`PM2359`)
- Represent redundant paths that preserve connection after failure. (`PM2360`)
- Represent clusters with dense internal and sparse external interaction. (`PM2361`)
- Represent isolated nodes without removing them from the account. (`PM2362`)
- Represent asymmetric dependence between connected participants. (`PM2363`)
- Represent different networks over the same set of participants. (`PM2364`)
- Replace geographic distance with distance through available connections. (`PM2365`)
- Replace node importance with importance to a specified flow. (`PM2366`)
- Replace local degree with influence through longer paths. (`PM2367`)
- Replace a central-node view with a peripheral-path view. (`PM2368`)
- Trace how one change propagates through the network. (`PM2369`)
- Trace where propagation stops despite physical connection. (`PM2370`)
- Distinguish connectivity from actual transmission. (`PM2371`)
- Distinguish transmission from adoption by the receiving node. (`PM2372`)
- Collapse tightly coordinated groups into higher-level nodes. (`PM2373`)
- Expand an aggregate node to reveal internal conflicts. (`PM2374`)
- Treat the network boundary as a choice affecting apparent centrality. (`PM2375`)

## Feedback

- Represent how outcomes alter subsequent actions. (`PM2376`)
- Represent how observations alter subsequent interpretations. (`PM2377`)
- Represent feedback delay explicitly. (`PM2378`)
- Separate the measured signal from the desired state. (`PM2379`)
- Separate error detection from the rule selecting a correction. (`PM2380`)
- Separate correction strength from correction frequency. (`PM2381`)
- Separate stabilizing feedback from amplifying feedback. (`PM2382`)
- Separate genuine improvement from manipulation of the feedback measure. (`PM2383`)
- Separate feedback available to the actor from feedback seen externally. (`PM2384`)
- Represent feedback missing from an otherwise active process. (`PM2385`)
- Represent feedback reaching the wrong decision-maker. (`PM2386`)
- Represent feedback arriving after useful correction is impossible. (`PM2387`)
- Represent conflicting feedback from different evaluators. (`PM2388`)
- Represent self-reinforcing interpretations that shape their own evidence. (`PM2389`)
- Represent self-defeating expectations that alter the predicted outcome. (`PM2390`)
- Replace fixed action with action contingent on observed results. (`PM2391`)
- Replace outcome-only feedback with feedback on intermediate transitions. (`PM2392`)
- Replace immediate feedback with a longer interval where delays matter. (`PM2393`)
- Replace delayed summaries with timely signals for urgent corrections. (`PM2394`)
- Add feedback about the accuracy of the feedback itself. (`PM2395`)
- Treat silence as missing feedback unless its meaning is established. (`PM2396`)
- Trace which results actually change later decisions. (`PM2397`)
- Trace where recorded results fail to enter later decisions. (`PM2398`)
- Distinguish learning from repeated reaction without retained change. (`PM2399`)
- Make the feedback rule itself adjustable through higher-level feedback. (`PM2400`)

## Adaptation

- Interpret a persistent pattern through the conditions selecting it. (`PM2401`)
- Interpret a changed behavior as adjustment to changed conditions. (`PM2402`)
- Interpret a previously useful response as mismatched to the present. (`PM2403`)
- Separate adaptation by individuals from selection among individuals. (`PM2404`)
- Separate deliberate redesign from accumulated local adjustment. (`PM2405`)
- Separate adaptation to the task from adaptation to the metric. (`PM2406`)
- Separate short-term accommodation from durable structural change. (`PM2407`)
- Separate resilience through resistance from resilience through reorganization. (`PM2408`)
- Represent the cost of acquiring a new response. (`PM2409`)
- Represent the cost of maintaining flexibility. (`PM2410`)
- Represent the lag between environmental change and response change. (`PM2411`)
- Represent constraints inherited from earlier adaptations. (`PM2412`)
- Represent benefits that become costs under new conditions. (`PM2413`)
- Represent variation available for selection. (`PM2414`)
- Represent mechanisms retaining successful variations. (`PM2415`)
- Represent mechanisms eliminating unsuccessful variations. (`PM2416`)
- Replace one universal response with condition-dependent responses. (`PM2417`)
- Replace excessive local adaptation with a stable shared procedure. (`PM2418`)
- Compare performance in the original and altered environments. (`PM2419`)
- Compare adaptability with immediate efficiency. (`PM2420`)
- Treat failure to adapt as possibly missing detection. (`PM2421`)
- Treat failure to adapt as possibly missing alternative responses. (`PM2422`)
- Treat failure to adapt as possibly excessive switching cost. (`PM2423`)
- Treat perspective change as adaptation of interpretation to encountered cases. (`PM2424`)
- Adapt the procedure for changing perspectives when its own environment changes. (`PM2425`)

## History and path dependence

- Interpret the current state through the sequence producing it. (`PM2426`)
- Interpret available options through earlier commitments that closed alternatives. (`PM2427`)
- Interpret present capabilities through accumulated practice. (`PM2428`)
- Interpret present expectations through earlier successes and failures. (`PM2429`)
- Interpret an institution through inherited coordination problems. (`PM2430`)
- Interpret a category through the historical cases that shaped it. (`PM2431`)
- Interpret an apparent necessity through its contingent origin. (`PM2432`)
- Separate origin of a practice from its present function. (`PM2433`)
- Separate persistence through usefulness from persistence through switching costs. (`PM2434`)
- Separate current support from inertia created by previous support. (`PM2435`)
- Represent sunk expenditures separately from future consequences. (`PM2436`)
- Represent irreversible historical losses separately from current recoverable deficits. (`PM2437`)
- Represent unchosen historical alternatives at their decision points. (`PM2438`)
- Represent cumulative advantages that alter later opportunity. (`PM2439`)
- Represent cumulative burdens that alter later capability. (`PM2440`)
- Compare similar present states reached through different histories. (`PM2441`)
- Compare different present states reached from similar beginnings. (`PM2442`)
- Reconstruct the earliest divergence between two trajectories. (`PM2443`)
- Change the hypothetical order of earlier events. (`PM2444`)
- Remove one historical commitment in a counterfactual reconstruction. (`PM2445`)
- Treat a tradition as a stored solution to a former problem. (`PM2446`)
- Treat forgetting as a change in accessible historical alternatives. (`PM2447`)
- Treat an archive's omissions as limits on historical reconstruction. (`PM2448`)
- Allow new evidence to revise the interpretation of earlier events. (`PM2449`)
- Read the present as a provisional stage in a continuing history. (`PM2450`)

## Expectations

- Make the expected outcome explicit before examining the result. (`PM2451`)
- Separate desired outcomes from predicted outcomes. (`PM2452`)
- Separate predicted outcomes from outcomes treated as normal. (`PM2453`)
- Separate personal expectation from shared social expectation. (`PM2454`)
- Separate an expectation from a commitment to make it true. (`PM2455`)
- Separate surprise from importance. (`PM2456`)
- Separate disappointment from deterioration relative to a fixed baseline. (`PM2457`)
- Replace one expected continuation with several conditional continuations. (`PM2458`)
- Replace a habitual expectation with one derived from current conditions. (`PM2459`)
- Replace an ideal expectation with ordinary observed performance. (`PM2460`)
- Replace ordinary expected performance with the required standard explicitly. (`PM2461`)
- Lower an expectation experimentally and reinterpret the same outcome. (`PM2462`)
- Raise an expectation experimentally and reinterpret the same outcome. (`PM2463`)
- Change the expected timing while preserving the expected event. (`PM2464`)
- Change the expected beneficiary while preserving the expected outcome. (`PM2465`)
- Track which expectations guide attention toward confirming details. (`PM2466`)
- Track which expectations make observations appear anomalous. (`PM2467`)
- Represent expectations that change others' behavior. (`PM2468`)
- Represent expectations that change your own available responses. (`PM2469`)
- Compare what was expected before and after receiving new information. (`PM2470`)
- Use violated expectations to identify hidden assumptions. (`PM2471`)
- Use fulfilled expectations to examine whether alternatives were discriminated. (`PM2472`)
- Suspend expectation long enough to notice an unfamiliar pattern. (`PM2473`)
- Retain uncertainty about the continuation despite a familiar beginning. (`PM2474`)
- Treat expectation formation as a modifiable part of the perspective. (`PM2475`)

## Anticipation

- Anticipate the next response before selecting the current response. (`PM2476`)
- Anticipate the strongest logical reply to the proposed answer. (`PM2477`)
- Anticipate the correction the current framing would require. (`PM2478`)
- Anticipate what the recipient would still need to ask. (`PM2479`)
- Anticipate what your answer would make newly understandable. (`PM2480`)
- Anticipate what your answer would prematurely close. (`PM2481`)
- Anticipate your own most likely continuation and exclude it experimentally. (`PM2482`)
- Anticipate a different continuation without first proving the usual one wrong. (`PM2483`)
- Anticipate the consequences of remaining silent. (`PM2484`)
- Anticipate the consequences of asking a question instead of answering. (`PM2485`)
- Anticipate the consequences of changing the subject explicitly. (`PM2486`)
- Anticipate the consequences of challenging the question's premise. (`PM2487`)
- Anticipate what would happen if the recipient fully understood already. (`PM2488`)
- Anticipate what would happen if the recipient rejected your central distinction. (`PM2489`)
- Anticipate the next obstacle after the apparent problem is solved. (`PM2490`)
- Anticipate how a successful result changes the next relevant question. (`PM2491`)
- Anticipate how the chosen perspective biases later searches. (`PM2492`)
- Anticipate how a new distinction changes available operations. (`PM2493`)
- Compare anticipated continuations under several candidate perspectives. (`PM2494`)
- Keep the desired continuation fixed while changing the present response. (`PM2495`)
- Keep the present response fixed while varying the recipient's interpretation. (`PM2496`)
- Continue anticipated exchanges until a consequential unresolved branch appears. (`PM2497`)
- Use the anticipated objection to revise the framing before presentation. (`PM2498`)
- Compare the actual reply with the anticipated reply and revise expectations. (`PM2499`)
- Anticipate whether further anticipation would change the next chosen action. (`PM2500`)

## Continuation

- Replace the expected next step with a different available step. (`PM2501`)
- Continue an abandoned interpretation without endorsing it. (`PM2502`)
- Stop a familiar continuation at its first unsupported transition. (`PM2503`)
- Resume from an earlier unresolved branch. (`PM2504`)
- Follow an incidental detail into a new line of inquiry. (`PM2505`)
- Continue a claim through its strongest implication. (`PM2506`)
- Continue a question through the uncertainty its answer leaves. (`PM2507`)
- Continue a distinction into the operations it newly permits. (`PM2508`)
- Continue a contradiction without immediately reconciling it. (`PM2509`)
- Continue a successful explanation into cases beyond its demonstrated scope. (`PM2510`)
- Continue a failed approach far enough to locate its failure condition. (`PM2511`)
- Replace answering with reformulating the question. (`PM2512`)
- Replace reformulating with constructing a discriminating case. (`PM2513`)
- Replace further analysis with a reversible practical trial. (`PM2514`)
- Replace practical action with examining an unresolved prerequisite. (`PM2515`)
- Replace summary with the next consequence not yet examined. (`PM2516`)
- Replace agreement with extending the accepted logic. (`PM2517`)
- Replace opposition with developing the strongest compatible extension. (`PM2518`)
- Exclude the most predictable continuation from a bounded exploration. (`PM2519`)
- Exclude every continuation that merely repeats an established distinction. (`PM2520`)
- Preserve several continuation branches without selecting a winner. (`PM2521`)
- Choose the continuation that reveals the largest unresolved dependency. (`PM2522`)
- Choose the continuation that changes what can be done next. (`PM2523`)
- Let an actual response replace the previously planned continuation. (`PM2524`)
- Treat the available continuation set as part of the perspective. (`PM2525`)

## Actions

- Represent a situation through the actions currently possible within it. (`PM2526`)
- Represent a situation through actions that become possible after one change. (`PM2527`)
- Represent an action through the state transition it produces. (`PM2528`)
- Represent an action through the information it reveals. (`PM2529`)
- Represent an action through the commitments it creates. (`PM2530`)
- Represent an action through the alternatives it closes. (`PM2531`)
- Represent an action through the reactions it invites. (`PM2532`)
- Represent an action through its reversibility conditions. (`PM2533`)
- Separate selecting an action from performing it. (`PM2534`)
- Separate performing an action from achieving its intended result. (`PM2535`)
- Separate immediate action from preparation enabling later action. (`PM2536`)
- Separate intervention from observation that leaves the process unchanged. (`PM2537`)
- Separate refusing an action from lacking the opportunity to act. (`PM2538`)
- Separate stopping work from abandoning the goal. (`PM2539`)
- Replace a large commitment with a reversible trial. (`PM2540`)
- Replace repeated local action with a change to its generating rule. (`PM2541`)
- Replace direct intervention with modification of an enabling condition. (`PM2542`)
- Replace action on the object with action on its representation. (`PM2543`)
- Replace action on the representation with contact with the object. (`PM2544`)
- Hold the action fixed while changing its interpreted purpose. (`PM2545`)
- Hold the purpose fixed while changing the chosen action. (`PM2546`)
- Interpret inaction through the changes occurring while nothing is chosen. (`PM2547`)
- Use an action's actual result to revise its meaning. (`PM2548`)
- Track which actions a perspective prevents you from noticing. (`PM2549`)
- Make the ability to interrupt an action part of its description. (`PM2550`)

## Affordances

- Interpret an object through uses available to a particular actor. (`PM2551`)
- Interpret a surface through the support it provides. (`PM2552`)
- Interpret an opening through passage it permits or blocks. (`PM2553`)
- Interpret a document through operations its format permits. (`PM2554`)
- Interpret an interface through actions discoverable without prior instruction. (`PM2555`)
- Interpret a relationship through possibilities for mutual coordination. (`PM2556`)
- Interpret a rule through the maneuvers it allows. (`PM2557`)
- Interpret a resource through uses not previously considered. (`PM2558`)
- Separate possible use from intended use. (`PM2559`)
- Separate intended use from readily discoverable use. (`PM2560`)
- Separate visible availability from actual accessibility. (`PM2561`)
- Separate accessibility from permission to use. (`PM2562`)
- Separate a potential action from the actor's ability to recognize it. (`PM2563`)
- Represent affordances changing with skill. (`PM2564`)
- Represent affordances changing with bodily position. (`PM2565`)
- Represent affordances changing with available tools. (`PM2566`)
- Represent affordances changing with social permission. (`PM2567`)
- Represent affordances changing with surrounding objects. (`PM2568`)
- Combine two objects to create an action neither permits alone. (`PM2569`)
- Remove a surrounding obstacle to expose an existing possible use. (`PM2570`)
- Change a cue to make an existing action discoverable. (`PM2571`)
- Reinterpret a constraint as support for a different action. (`PM2572`)
- Reinterpret a byproduct as an input for another activity. (`PM2573`)
- Compare what a novice and expert can do with the same object. (`PM2574`)
- Treat newly perceived affordances as substantive perspective changes. (`PM2575`)

## Exploration

- Explore an alternative without first rejecting the current interpretation. (`PM2576`)
- Explore a hypothesis without committing to its truth. (`PM2577`)
- Explore a method without committing to its continued use. (`PM2578`)
- Explore the least familiar representation of the subject. (`PM2579`)
- Explore a case where every ordinary category becomes difficult to apply. (`PM2580`)
- Explore a possibility excluded by an inherited convention. (`PM2581`)
- Explore what remains when the dominant explanatory variable is removed. (`PM2582`)
- Explore a deliberately constrained version using only one kind of relation. (`PM2583`)
- Explore the subject using only observable changes. (`PM2584`)
- Explore the subject using only dependencies among possible actions. (`PM2585`)
- Explore the subject using only questions generated by unresolved distinctions. (`PM2586`)
- Explore several starting points before developing any one of them. (`PM2587`)
- Alternate expansion of alternatives with targeted discrimination among them. (`PM2588`)
- Follow a surprising result before restoring the original agenda. (`PM2589`)
- Vary one perspective component at a time to expose its contribution. (`PM2590`)
- Vary several components together when isolated variation is misleading. (`PM2591`)
- Introduce a random starting cue and follow its consequential connections. (`PM2592`)
- Use an arbitrary restriction to make habitual operations unavailable. (`PM2593`)
- Suspend the demand for immediate usefulness during a bounded trial. (`PM2594`)
- Suspend the demand for coherence across independent exploratory branches. (`PM2595`)
- Resume a discarded branch after gaining a relevant new distinction. (`PM2596`)
- Transfer a discovered operation into a different subject. (`PM2597`)
- Let a failed exploration revise the exploration procedure. (`PM2598`)
- Stop exploring a branch when it no longer produces consequential differences. (`PM2599`)
- Treat the explored possibility space as provisional rather than complete. (`PM2600`)

## Learning

- Replace memorized labels with discriminations among concrete cases. (`PM2601`)
- Replace memorized procedures with recognition of their applicability conditions. (`PM2602`)
- Replace passive exposure with predicting before observing the answer. (`PM2603`)
- Replace recognition of a solution with independent reconstruction. (`PM2604`)
- Replace repeated success cases with contrasts exposing failure conditions. (`PM2605`)
- Replace isolated examples with variations around a shared structure. (`PM2606`)
- Separate learning a fact from learning how to find it. (`PM2607`)
- Separate learning a method from learning when to stop using it. (`PM2608`)
- Separate improvement through practice from familiarity with the presentation. (`PM2609`)
- Separate retained learning from temporary performance support. (`PM2610`)
- Use feedback to change the category applied to an error. (`PM2611`)
- Use a failed transfer to refine the scope of a method. (`PM2612`)
- Use successful transfer to identify an invariant operation. (`PM2613`)
- Learn the same subject through a different representation. (`PM2614`)
- Learn the same subject by explaining it to a new audience. (`PM2615`)
- Learn the same subject by constructing a case that defeats your account. (`PM2616`)
- Learn an unfamiliar role through ordinary participation where appropriate. (`PM2617`)
- Unlearn a distinction that consistently merges consequentially different cases. (`PM2618`)
- Restore a previously learned distinction lost through habitual simplification. (`PM2619`)
- Organize new learning around the operation it enables. (`PM2620`)
- Connect a new result to a previously inaccessible problem. (`PM2621`)
- Change learning order to satisfy conceptual dependencies. (`PM2622`)
- Change practice conditions to expose context-dependent understanding. (`PM2623`)
- Treat confusion as information about the current representation. (`PM2624`)
- Modify the process that determines what you try to learn next. (`PM2625`)

## Memory

- Retrieve an earlier case with the same relational structure. (`PM2626`)
- Retrieve an earlier failure with the same hidden condition. (`PM2627`)
- Retrieve an abandoned alternative relevant under present conditions. (`PM2628`)
- Retrieve an earlier goal before accepting the current goal description. (`PM2629`)
- Retrieve the original question before continuing accumulated analysis. (`PM2630`)
- Retrieve the observation before relying on its later summary. (`PM2631`)
- Retrieve the uncertainty omitted from a confident remembered conclusion. (`PM2632`)
- Separate remembered events from later interpretations of them. (`PM2633`)
- Separate familiarity of a claim from memory of its source. (`PM2634`)
- Separate confidence in recollection from independent confirmation. (`PM2635`)
- Represent an earlier perspective in its original terms. (`PM2636`)
- Compare an earlier perspective with the current one explicitly. (`PM2637`)
- Recover distinctions removed by successive summaries. (`PM2638`)
- Recover intermediate results that were never reused. (`PM2639`)
- Index memories by problems they can help resolve. (`PM2640`)
- Index memories by operations they exemplify. (`PM2641`)
- Index memories by conditions under which they failed. (`PM2642`)
- Replace chronological recall with recall organized by causal role. (`PM2643`)
- Replace thematic recall with the actual sequence of experience. (`PM2644`)
- Use external records to revise an unsupported recollection. (`PM2645`)
- Retain disagreement between recollection and records as unresolved. (`PM2646`)
- Temporarily bracket a familiar precedent to see the present case freshly. (`PM2647`)
- Restore the precedent after developing an independent interpretation. (`PM2648`)
- Treat forgetting as loss of access to possible perspectives. (`PM2649`)
- Change retrieval cues to make a neglected perspective available. (`PM2650`)

## Skill and familiarity

- View a familiar task as a novice with no procedural shortcuts. (`PM2651`)
- View a novice's task through distinctions learned through expertise. (`PM2652`)
- Separate skilled performance from ability to describe the skill. (`PM2653`)
- Separate fluent description from ability to perform the task. (`PM2654`)
- Separate speed from accurate recognition of the relevant case. (`PM2655`)
- Separate automaticity from appropriateness under changed conditions. (`PM2656`)
- Slow a familiar procedure to expose its hidden decisions. (`PM2657`)
- Perform a familiar operation in an unfamiliar representation. (`PM2658`)
- Reconstruct a shortcut as the steps it compresses. (`PM2659`)
- Compare expert and novice interpretations of the same error. (`PM2660`)
- Compare recognition before and after learning a discriminating feature. (`PM2661`)
- Remove a familiar cue and examine whether understanding transfers. (`PM2662`)
- Change surface features while preserving the underlying task structure. (`PM2663`)
- Change task structure while preserving familiar surface features. (`PM2664`)
- Treat an expert intuition as a candidate discrimination to inspect. (`PM2665`)
- Treat a novice's difficulty as evidence of an omitted prerequisite. (`PM2666`)
- Treat ease for you as distinct from simplicity for another. (`PM2667`)
- Treat unfamiliarity as distinct from incoherence. (`PM2668`)
- Replace domain labels with the operations a skilled actor performs. (`PM2669`)
- Expose dependencies hidden inside a seemingly single expert action. (`PM2670`)
- Represent skill as conditional on tools and environment. (`PM2671`)
- Represent skill limits through specific failure cases. (`PM2672`)
- Borrow another field's practiced distinction for the current subject. (`PM2673`)
- Translate a tacit distinction into a repeatable comparison. (`PM2674`)
- Use acquired skill to reopen a previously uninformative perspective. (`PM2675`)

## Experience

- Replace reading about a routine with ordinary participation in it. (`PM2676`)
- Replace watching a task with performing a safe bounded instance. (`PM2677`)
- Replace a general impression with repeated encounters across contexts. (`PM2678`)
- Replace a remembered experience with a fresh observation. (`PM2679`)
- Replace abstract waiting time with attention to the experience of waiting. (`PM2680`)
- Replace a cost label with the lived activities it requires. (`PM2681`)
- Encounter the same setting at a different ordinary time. (`PM2682`)
- Encounter the same activity in a different participant role. (`PM2683`)
- Encounter a familiar object without its customary description. (`PM2684`)
- Encounter an unfamiliar object before learning its assigned category. (`PM2685`)
- Compare expectation before an experience with interpretation afterward. (`PM2686`)
- Compare immediate experience with later recollection. (`PM2687`)
- Compare individual experience with other participants' independent reports. (`PM2688`)
- Distinguish direct experience from imagined identification with another. (`PM2689`)
- Distinguish intensity of experience from representativeness. (`PM2690`)
- Distinguish one transformative episode from a repeated stable pattern. (`PM2691`)
- Attend to the beginning, middle, and end of an experience separately. (`PM2692`)
- Attend to transitions between involvement and detached observation. (`PM2693`)
- Represent experiences that do not fit the available vocabulary. (`PM2694`)
- Retain an unresolved experience without forcing a familiar explanation. (`PM2695`)
- Revisit an experience after acquiring a new distinction. (`PM2696`)
- Reinterpret a prior experience from a changed present concern. (`PM2697`)
- Let a concrete encounter overturn a tidy abstract account. (`PM2698`)
- Preserve variation in experience within the same named activity. (`PM2699`)
- Treat changes in what is experienced as changes in perspective. (`PM2700`)

## Interaction

- Let another person's question expose an unrecognized assumption. (`PM2701`)
- Let another person's example revise your category boundary. (`PM2702`)
- Let another person's objection identify a missing dependency. (`PM2703`)
- Let another person's confusion identify an unavailable distinction. (`PM2704`)
- Let another person's successful use revise your account of usefulness. (`PM2705`)
- Let another person's failed use revise your assumed prerequisites. (`PM2706`)
- Exchange descriptions of the same case before negotiating agreement. (`PM2707`)
- Exchange roles in explaining and questioning the same claim. (`PM2708`)
- Construct a shared example where interpretations yield different responses. (`PM2709`)
- Observe how your framing changes the other person's reply. (`PM2710`)
- Observe how their reply changes your available next response. (`PM2711`)
- Separate mutual understanding from convergence of opinion. (`PM2712`)
- Separate deference from independently reached agreement. (`PM2713`)
- Separate compliance with a request from adoption of its perspective. (`PM2714`)
- Separate public assent from privately reported interpretation when available. (`PM2715`)
- Introduce a third perspective that changes the original disagreement. (`PM2716`)
- Let participants choose different questions about the same situation. (`PM2717`)
- Translate each person's concern into consequences for the others. (`PM2718`)
- Preserve unresolved differences in a shared representation. (`PM2719`)
- Reconstruct a misunderstanding from both sides' successive interpretations. (`PM2720`)
- Use joint practical activity to expose differences hidden in conversation. (`PM2721`)
- Revise your account after the other person corrects your reconstruction. (`PM2722`)
- Allow the interaction to change the original inquiry. (`PM2723`)
- Treat rapport and antagonism as influences on what gets considered. (`PM2724`)
- Treat perspective formation as something occurring between participants. (`PM2725`)

## Perspective acquisition

- Acquire a distinction by encountering a case the current view cannot separate. (`PM2726`)
- Acquire a representation by using another person's working example. (`PM2727`)
- Acquire a perspective by reconstructing its motivating problem. (`PM2728`)
- Acquire a perspective by tracing the failures it was designed to avoid. (`PM2729`)
- Acquire a perspective by learning its permitted operations. (`PM2730`)
- Acquire a perspective by learning its prohibited inferences. (`PM2731`)
- Acquire a perspective by comparing it with your current interpretation. (`PM2732`)
- Acquire a perspective through a role requiring unfamiliar decisions. (`PM2733`)
- Acquire a perspective through ordinary participation in a different practice. (`PM2734`)
- Acquire a perspective through an account of another's situated experience. (`PM2735`)
- Acquire a perspective through an analogy with explicit correspondences. (`PM2736`)
- Acquire a perspective through constructing a model of its distinctions. (`PM2737`)
- Acquire a perspective through reproducing its judgments on varied cases. (`PM2738`)
- Acquire a perspective through testing where its judgments change. (`PM2739`)
- Acquire a perspective through its difficult boundary cases. (`PM2740`)
- Acquire a perspective through a failed prediction of another's response. (`PM2741`)
- Acquire a perspective through reinterpreting previously disconnected observations. (`PM2742`)
- Acquire a perspective by combining previously separate capabilities. (`PM2743`)
- Acquire a perspective by removing a distinction that blocked connection. (`PM2744`)
- Acquire a perspective by inventing a new operation on the subject. (`PM2745`)
- Acquire a perspective by changing what an existing result is used for. (`PM2746`)
- Acquire a perspective by making an implicit interpretation explicit. (`PM2747`)
- Acquire a perspective by discovering an unnoticed alternative starting point. (`PM2748`)
- Acquire a perspective by revisiting a familiar subject with new capacities. (`PM2749`)
- Distinguish access to a perspective's words from access to its operations. (`PM2750`)

## Perspective activation

- Make a dormant perspective operative when its relevant condition appears. (`PM2751`)
- Replace habitual activation with deliberate selection before interpretation. (`PM2752`)
- Replace deliberate selection with a learned cue-response association. (`PM2753`)
- Activate a neglected perspective before the familiar one. (`PM2754`)
- Activate several perspectives before allowing any to dominate. (`PM2755`)
- Activate a perspective only within its demonstrated domain. (`PM2756`)
- Activate a perspective outside its usual domain for exploration. (`PM2757`)
- Use a concrete case to reactivate an abstract perspective. (`PM2758`)
- Use a question to reactivate the distinctions it requires. (`PM2759`)
- Use a remembered failure to reactivate a protective distinction. (`PM2760`)
- Use a physical representation to reactivate an otherwise inaccessible relation. (`PM2761`)
- Use a changed role to activate different concerns. (`PM2762`)
- Use a predicted reply to activate a different communicative perspective. (`PM2763`)
- Remove a familiar cue that automatically activates an unhelpful interpretation. (`PM2764`)
- Substitute a new cue for the same perspective. (`PM2765`)
- Delay activation until the current observation is described independently. (`PM2766`)
- Bring activation forward to shape what is observed. (`PM2767`)
- Allow activation without immediate endorsement. (`PM2768`)
- Allow activation without requiring verbal expression. (`PM2769`)
- Separate a perspective's availability from its current dominance. (`PM2770`)
- Separate temporary activation from durable adoption. (`PM2771`)
- Make conflicting activation cues explicit before resolving them. (`PM2772`)
- Reactivate an earlier perspective after discovering a relevant new condition. (`PM2773`)
- Deactivate a perspective when its triggering condition no longer holds. (`PM2774`)
- Make activation rules responsive to observed consequences of prior activation. (`PM2775`)

## Perspective adoption

- Adopt an interpretation provisionally for one specified case. (`PM2776`)
- Adopt an interpretation for a bounded period of exploration. (`PM2777`)
- Adopt an interpretation conditionally on a stated premise. (`PM2778`)
- Adopt only the distinctions of a perspective. (`PM2779`)
- Adopt only the operations of a perspective. (`PM2780`)
- Adopt only the questions a perspective makes available. (`PM2781`)
- Adopt only the evaluative criteria of a perspective explicitly. (`PM2782`)
- Adopt a perspective while retaining competing accounts. (`PM2783`)
- Adopt a perspective for action without asserting its complete truth. (`PM2784`)
- Adopt a perspective for interpretation without using it to decide. (`PM2785`)
- Replace passive agreement with practical use on an unfamiliar case. (`PM2786`)
- Replace imitation of wording with reproduction of consequential discriminations. (`PM2787`)
- Replace global acceptance with component-specific acceptance. (`PM2788`)
- Replace permanent allegiance with revisable use conditions. (`PM2789`)
- Separate adopting a perspective from adopting its author's identity. (`PM2790`)
- Separate adopting a perspective from endorsing all historical uses. (`PM2791`)
- Separate understanding a perspective from accepting it. (`PM2792`)
- Separate accepting a perspective from making it the default. (`PM2793`)
- Make adoption depend on performance across contrasting cases. (`PM2794`)
- Make adoption depend on a previously unavailable operation becoming usable. (`PM2795`)
- Limit adoption where the perspective removes a required distinction. (`PM2796`)
- Expand adoption when transfer succeeds under new conditions. (`PM2797`)
- Withdraw adoption from one context while retaining it elsewhere. (`PM2798`)
- Replace implicit adoption with an explicit choice among interpretations. (`PM2799`)
- Include conditions for reconsideration in the adopted perspective. (`PM2800`)

## Perspective maintenance

- Replace unexamined persistence with periodic review against new cases. (`PM2801`)
- Replace constant review with review triggered by a failed expectation. (`PM2802`)
- Replace repetition of labels with renewed use of the actual distinctions. (`PM2803`)
- Replace isolated retention with links to relevant future questions. (`PM2804`)
- Replace memory-dependent maintenance with a reusable external representation. (`PM2805`)
- Replace rigid consistency with preservation of specified invariants. (`PM2806`)
- Replace global persistence with persistence only under stated conditions. (`PM2807`)
- Replace automatic reversion with deliberate restoration after interruption. (`PM2808`)
- Add reminders of unresolved objections to the maintained account. (`PM2809`)
- Add boundary cases that prevent gradual overgeneralization. (`PM2810`)
- Add provenance that prevents qualifications disappearing during reuse. (`PM2811`)
- Add records of failed applications to limit inappropriate transfer. (`PM2812`)
- Add cues connecting the perspective to its next relevant use. (`PM2813`)
- Remove maintenance rituals that no longer preserve a useful distinction. (`PM2814`)
- Remove obsolete conditions inherited from an earlier version. (`PM2815`)
- Separate maintaining access from maintaining belief. (`PM2816`)
- Separate maintaining belief from maintaining readiness to revise. (`PM2817`)
- Separate preserving a perspective from preserving its original wording. (`PM2818`)
- Allow local variation while maintaining a shared interpretive core. (`PM2819`)
- Allow temporary suspension without erasing later access. (`PM2820`)
- Change maintenance frequency as the subject becomes more stable. (`PM2821`)
- Change maintenance frequency as operating conditions become less stable. (`PM2822`)
- Replace defensive preservation with comparison against the strongest alternative. (`PM2823`)
- Preserve the perspective through reconstruction from cases instead of memorization. (`PM2824`)
- Make maintenance itself conditional on continued contribution to the inquiry. (`PM2825`)

## Perspective replacement

- Replace the entire interpretation after its central distinction fails. (`PM2826`)
- Replace the explanatory model while retaining the same observations. (`PM2827`)
- Replace the ontology while retaining the original question. (`PM2828`)
- Replace the focal question while retaining the represented subject. (`PM2829`)
- Replace the perspective's criterion while marking the changed evaluative task. (`PM2830`)
- Replace the dominant perspective with a previously subordinate one. (`PM2831`)
- Replace a familiar perspective with an independently constructed interpretation. (`PM2832`)
- Replace a failed perspective with several conditional perspectives. (`PM2833`)
- Replace several fragmented perspectives with one preserving their useful distinctions. (`PM2834`)
- Replace a domain-specific perspective with a transferable relational account. (`PM2835`)
- Replace an overly general perspective with a case-specific interpretation. (`PM2836`)
- Replace a verbal perspective with one enacted through practical operation. (`PM2837`)
- Replace an imagined perspective with one revised through direct encounter. (`PM2838`)
- Replace a borrowed perspective with one reconstructed from its prerequisites. (`PM2839`)
- Replace a perspective because its useful scope has ended. (`PM2840`)
- Replace a perspective because the subject itself has changed. (`PM2841`)
- Replace a perspective because a previously unavailable operation becomes possible. (`PM2842`)
- Replace a perspective without requiring every earlier claim to be false. (`PM2843`)
- Replace only the parts causally responsible for the observed failure. (`PM2844`)
- Preserve valid intermediate results during replacement. (`PM2845`)
- Preserve unresolved questions that the replacement does not answer. (`PM2846`)
- Preserve the original perspective as an accessible alternative. (`PM2847`)
- Compare consequences before and after replacement on shared cases. (`PM2848`)
- Reverse a replacement when it removes a necessary distinction. (`PM2849`)
- Make replacement conditions part of the operative interpretation. (`PM2850`)

## Perspective composition

- Use one perspective to select questions for another perspective. (`PM2851`)
- Use one perspective to interpret another perspective's results. (`PM2852`)
- Use one perspective to expose another perspective's exclusions. (`PM2853`)
- Use one perspective to test another perspective's scope. (`PM2854`)
- Use one perspective to generate cases for another perspective. (`PM2855`)
- Use one perspective to determine when another should activate. (`PM2856`)
- Use one perspective to choose operations within another. (`PM2857`)
- Use one perspective to evaluate another's proposed continuations. (`PM2858`)
- Apply two perspectives independently before comparing their outputs. (`PM2859`)
- Apply two perspectives successively and preserve the intermediate result. (`PM2860`)
- Alternate perspectives whenever one reaches an unresolved dependency. (`PM2861`)
- Combine compatible distinctions from otherwise incompatible perspectives. (`PM2862`)
- Combine one perspective's ontology with another's evaluation criteria explicitly. (`PM2863`)
- Combine one perspective's questions with another's representational tools. (`PM2864`)
- Combine one perspective's evidence access with another's inference methods. (`PM2865`)
- Nest a local perspective within a broader containing perspective. (`PM2866`)
- Place several local perspectives beside each other without a privileged whole. (`PM2867`)
- Compose perspectives around a shared required outcome. (`PM2868`)
- Compose perspectives around mutually complementary blind spots. (`PM2869`)
- Retain disagreements between composed perspectives as explicit branches. (`PM2870`)
- Prevent one component's conclusion from silently becoming another's premise. (`PM2871`)
- Specify interfaces where one perspective's output becomes another's input. (`PM2872`)
- Remove a component whose distinctions duplicate another's without added consequence. (`PM2873`)
- Add a component that makes a missing transition possible. (`PM2874`)
- Treat the resulting composition as a new perspective available for modification. (`PM2875`)

## Perspective translation

- Translate a perspective into another vocabulary while preserving its discriminations. (`PM2876`)
- Translate a perspective into concrete cases showing where judgments differ. (`PM2877`)
- Translate a perspective into operations executable on a new subject. (`PM2878`)
- Translate a perspective into questions a newcomer can actually answer. (`PM2879`)
- Translate a perspective into observable predictions under specified conditions. (`PM2880`)
- Translate a perspective into a formal model with explicit assumptions. (`PM2881`)
- Translate a formal perspective into ordinary consequences for participants. (`PM2882`)
- Translate an expert perspective into the novice's available prerequisites. (`PM2883`)
- Translate a novice's difficulty into distinctions an expert omitted. (`PM2884`)
- Translate an individual's account into relations among interacting participants. (`PM2885`)
- Translate an institutional account into consequences for individual participants. (`PM2886`)
- Translate a historical perspective into present terms while marking mismatches. (`PM2887`)
- Translate a present perspective into historically available concepts. (`PM2888`)
- Translate an embodied perspective into accessible descriptions of action possibilities. (`PM2889`)
- Translate an abstract perspective into a practical demonstration. (`PM2890`)
- Translate an evaluative disagreement into incompatible concrete rankings. (`PM2891`)
- Translate a causal disagreement into different intervention expectations. (`PM2892`)
- Translate an ontological disagreement into different entity-recognition rules. (`PM2893`)
- Translate uncertainty without converting it into apparent certainty. (`PM2894`)
- Translate a distinction even when the target vocabulary lacks one word. (`PM2895`)
- Preserve an untranslatable remainder instead of inventing an equivalent. (`PM2896`)
- Compare the translated perspective with the original on shared cases. (`PM2897`)
- Translate back to expose lost or added distinctions. (`PM2898`)
- Revise the target representation when faithful translation is impossible. (`PM2899`)
- Let translation reveal a new perspective unavailable in either starting vocabulary. (`PM2900`)

## Perspective selection

- Select the perspective that separates the currently confounded cases. (`PM2901`)
- Select the perspective that makes the next required operation available. (`PM2902`)
- Select the perspective matching the actual question rather than familiar terminology. (`PM2903`)
- Select the perspective whose scope includes the present operating conditions. (`PM2904`)
- Select the perspective exposing the most consequential omitted dependency. (`PM2905`)
- Select the perspective requiring the fewest unsupported assumptions for this case. (`PM2906`)
- Select the perspective preserving distinctions the intended action requires. (`PM2907`)
- Select the perspective that produces discriminating predictions among live alternatives. (`PM2908`)
- Select the perspective that reveals previously excluded affected parties. (`PM2909`)
- Select the perspective that clarifies the strongest unresolved objection. (`PM2910`)
- Select the perspective that makes a confusing experience describable. (`PM2911`)
- Select the perspective that exposes why the current selection rule fails. (`PM2912`)
- Replace one permanent default with selection conditional on the encountered case. (`PM2913`)
- Replace immediate selection with several independent initial constructions. (`PM2914`)
- Replace selection by fluency with comparison on shared tasks. (`PM2915`)
- Replace selection by prestige with comparison of available operations. (`PM2916`)
- Replace selection by novelty with comparison of consequential differences. (`PM2917`)
- Replace selection by agreement with comparison of evidential adequacy. (`PM2918`)
- Choose different perspectives for diagnosis and intervention. (`PM2919`)
- Choose different perspectives for exploration and presentation. (`PM2920`)
- Choose different perspectives for immediate action and long-term revision. (`PM2921`)
- Retain several perspectives when no justified criterion selects one. (`PM2922`)
- Randomly select among admissible perspectives for a bounded exploratory trial. (`PM2923`)
- Reopen selection after the next case changes what matters. (`PM2924`)
- Treat perspective-selection criteria as themselves revisable. (`PM2925`)

## Perspective revision

- Revise the interpretation after encountering a separating counterexample. (`PM2926`)
- Revise a category boundary after discovering a previously merged difference. (`PM2927`)
- Revise a causal relation after an intervention contradicts its prediction. (`PM2928`)
- Revise a confidence assignment after evidence loses independence. (`PM2929`)
- Revise an assumed beneficiary after identifying displaced consequences. (`PM2930`)
- Revise a time horizon after observing a delayed effect. (`PM2931`)
- Revise a system boundary after tracing an external dependency. (`PM2932`)
- Revise an entity type after its original description blocks useful operations. (`PM2933`)
- Revise a relevance rule after an excluded detail changes the answer. (`PM2934`)
- Revise a model variable after supposedly identical states behave differently. (`PM2935`)
- Revise an expectation after an actual continuation defeats it. (`PM2936`)
- Revise the next response after recognizing the recipient already understands. (`PM2937`)
- Revise the next question after a prior answer changes the available inquiry. (`PM2938`)
- Revise only the components touched by new evidence. (`PM2939`)
- Revise dependent conclusions when a supporting component changes. (`PM2940`)
- Retain conclusions that do not depend on the revised component. (`PM2941`)
- Distinguish correction of an error from adaptation to a changed subject. (`PM2942`)
- Distinguish increased precision from replacement of the original claim. (`PM2943`)
- Distinguish expansion of scope from reinterpretation of existing scope. (`PM2944`)
- Record which changed distinction produces the changed verdict. (`PM2945`)
- Compare revisions on cases where the previous version already succeeded. (`PM2946`)
- Restore a lost distinction when a revision creates a new blind spot. (`PM2947`)
- Retain several proposed revisions until a discriminating case is available. (`PM2948`)
- Revise without preserving consistency with an unsupported previous assertion. (`PM2949`)
- Make revision possible before a failure becomes publicly undeniable. (`PM2950`)

## Modification sequences

- Reverse the order of two modifications and compare the results. (`PM2951`)
- Apply a modification twice and inspect whether the second application changes anything. (`PM2952`)
- Repeat a modification until it no longer changes the selected distinction. (`PM2953`)
- Alternate two modifications until their interaction becomes visible. (`PM2954`)
- Apply one modification only if another produces a specified condition. (`PM2955`)
- Apply several modifications independently to the same starting perspective. (`PM2956`)
- Apply several modifications successively to each preceding result. (`PM2957`)
- Branch after each modification instead of selecting one path immediately. (`PM2958`)
- Merge branches only where their resulting discriminations agree. (`PM2959`)
- Preserve the original perspective beside each modified version. (`PM2960`)
- Reset to the original perspective before testing the next modification. (`PM2961`)
- Carry forward intermediate results that remain valid after later changes. (`PM2962`)
- Undo one modification while retaining later independent changes. (`PM2963`)
- Replace a modification with its inverse where an inverse exists. (`PM2964`)
- Approximate reversal when the modification destroyed information. (`PM2965`)
- Insert a new operation at the point where a sequence stalls. (`PM2966`)
- Remove a step whose output no later operation uses. (`PM2967`)
- Move a prerequisite-producing modification before the operation requiring it. (`PM2968`)
- Delay evaluation until alternatives have been independently constructed. (`PM2969`)
- Evaluate after every step when early failure would invalidate later work. (`PM2970`)
- Vary sequence length to expose premature stopping or redundant continuation. (`PM2971`)
- Change stopping conditions from elapsed effort to a specified unresolved distinction. (`PM2972`)
- Change stopping conditions when further work cannot alter the intended action. (`PM2973`)
- Let the current result determine which modification runs next. (`PM2974`)
- Treat the whole modification sequence as an operation available for reuse. (`PM2975`)

## Modification of modification

- Change which component an existing modification acts on. (`PM2976`)
- Change the conditions under which a modification is applicable. (`PM2977`)
- Change the distinctions a modification must preserve. (`PM2978`)
- Change the distinctions a modification is allowed to remove. (`PM2979`)
- Change the degree of a modification while preserving its direction. (`PM2980`)
- Change the direction of a modification while preserving its target. (`PM2981`)
- Change a deterministic modification into several alternative outcomes. (`PM2982`)
- Change a branching modification into a rule selecting one branch. (`PM2983`)
- Change a global modification into a local one. (`PM2984`)
- Change a local modification into a coordinated change across the system. (`PM2985`)
- Change a one-time modification into a condition-triggered recurring operation. (`PM2986`)
- Change a recurring modification into a one-time bounded experiment. (`PM2987`)
- Change a modification's input from one perspective to a set of perspectives. (`PM2988`)
- Change a modification's output from one perspective to a family of alternatives. (`PM2989`)
- Change a modification to retain information it previously discarded. (`PM2990`)
- Change a modification to discard distinctions irrelevant to its declared use. (`PM2991`)
- Change a modification so its effects remain reversible. (`PM2992`)
- Change a modification so it exposes its own application conditions. (`PM2993`)
- Change a modification so failure generates a new question. (`PM2994`)
- Change a modification so success makes its result reusable elsewhere. (`PM2995`)
- Apply a perspective to the procedure that produced that perspective. (`PM2996`)
- Apply an operation to its own selection or evaluation rule. (`PM2997`)
- Construct a new operation from a consequential change absent from the catalog. (`PM2998`)
- Distinguish two operation descriptions by a case where their effects differ. (`PM2999`)
- Reopen the catalog whenever a new distinction permits a new transformation. (`PM3000`)
