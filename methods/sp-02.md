Intended mind change: Turn an underspecified physical-interface request into a concrete representation trial with preserved recovery.

Actual starting judgment: I would already keep the six dependencies and the failure return, but the prompt did not fix how to inspect a one-current-step interface or handle an absent state.

Original: ../sources/methods-sp.original.md. Separate requirements receipt: ../sources/methods-sp.requirements.txt.

Depth: SP has no numerical 8x definition. Six passes applied, eight distinct failure realizations tested, concrete reconstruction and actual SVS routing.

Original input restored from the actual v2 capsule:

> Make a six-step setup instruction usable when only one step can be visible at a time; construct and inspect the model artifact while leaving human use unobserved. The initial instruction contains gather materials, connect cable, select input, power on, check indicator, and start; the check can send the reader back to the cable step.

TYPE: CREATIVE + METHOD. Interpretation: compound and failure-prone. Requests: create a one-current-step representation, preserve a six-action setup, allow the indicator check to return to the cable step. Implicit task: represent the branch and retain state. Existing criterion: one visible step; missing criteria: what counts as successful recovery, how an absent state is handled, and whether a claimed usability effect is a performed human outcome.

Failure realizations and their concrete prompt repairs:

| Realization | What fails | Added criterion |
| --- | --- | --- |
| All six instructions in small text | Text is concise but more than one current instruction is visible. | Exactly one current instruction. |
| One instruction with no forward control | A static card cannot reach the check. | State transitions are provided. |
| Failure sends to start | The cable defect remains uncorrected. | Failure returns to connect cable. |
| Failure returns but later state skips power | The original dependency order is lost. | Recovery replays the required sequence. |
| A successful trace only | Failure branch is untested. | Failure and repeated-failure walkthroughs. |
| Missing state silently defaults to start | An unfinished setup is represented as complete. | Unavailable prior state is explicit. |
| A screen model claimed easy for a one-handed person | No person performed the task. | Model-trace scope only. |
| A polished diagram with no usable artifact | No operation can consume the result. | Construct the interface representation. |

Improved copy-paste prompt:

Create a model of a six-step setup interface that shows exactly one current instruction. Use these actions: gather materials, connect cable, select input, power on, check indicator, start. At check indicator, success leads to start and failure returns to connect cable. Preserve the action dependencies; provide a current-step view and the state transitions. Walk through a successful path, a failed check and recovery, repeated failure, and an unavailable prior state. Assess the artifact on those model traces; human usability remains unobserved. Use the original /svs to vary the existing numbered-list representation before selecting and constructing the interface.

Skill routing: SVS is the concrete primary because a numbered-list representation exists. PV is a recovery route if no variant can preserve the branch; a full method search is unnecessary before trying actual transformations. This SP output is a prompt, not the interface answer.

Actual application: the restored capsule selected sp-02 correctly, and the resulting prompt becomes the input of svs-02 below. This demonstrates current in-session resumption-state use, not later-session autonomy. Its branch-preservation distinctions drive that artifact, but the artifact’s beneficial effect is credited only in SVS-02.

Actual mind change: The next request now calls for concrete transitions and four traces, and it is passed to SVS-02.

Benefit: Local adoption is demonstrated; it does not create an additional benefit count beyond the constructed interface’s eventual result. Human usability remains unknown.

Verdict: REJECT

Organization: A copy-paste prompt leads the artifact creation; failure fixtures remain here. The physical constraint is modeled, not reported as changed in a person.

Next attempts: Construct the single-step state model; test a prompt where ambiguity should remain; compare a static card where no branch exists.
