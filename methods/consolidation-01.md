Intended mind change: Make four established beneficial changes available at the next relevant situation without merging their scopes.

Actual starting judgment: An alphabetical set of source-named application files preserves provenance but does not directly expose the situation that calls for each result.

Established content only: K1=sp-01 count semantics, K2=given-01 current readiness, K3=pv-01 absence types, K4=unx-01 spatial access/recipient attention. FOHT-01 was REJECTED for no marginal benefit and supplies no rule.

Organization A: alphabetical skill cards [given, pv, sp, unx]. Advantage: source-name lookup. Organization B: trigger-first cards grouped by the question the next task presents. Organization C: one prose principle joining all four under “better selection.” C removes the individual non-application boundaries and fails the two boundary queries below; it is rejected.

Same four content items, tested queries:

| Query | A result | B result | Boundary retained |
| --- | --- | --- | --- |
| A file count is used as success | sp card found by reading semantic descriptions | count trigger directly identifies K1 | Three repeats can remain one distinct finding. |
| A missing helper appears to block GG | pv card must be located by name or scan | absence trigger identifies K3 | Full-generation option remains available. |
| ADEp has no actual human ratings | pv card supplies same source but needs boundary read | K3 boundary is adjacent to operation | No model-generated calibration. |
| A terse gesture is assumed to reveal sadness | unx card is not named by the input | gesture trigger identifies K4 | Physical side changes, emotion remains unresolved. |
| The exact skill /given is requested | A directly exposes the named card | B needs the evidence column lookup | Source-name lookup favors A. |

Choice: B for the next-operation view, with A preserved as the file list. The trade-off is explicit: no one ordering is globally best. B adds no findings; it changes access to the four established ones.

Selected trigger index:

| Keep | Reuse trigger | Available changed operation | Evidence | Boundary |
| --- | --- | --- | --- | --- |
| K1 | A count is being used as success evidence | Separate required application, novel benefit and uptake counts | sp-01 | No new credit for the same changed operation |
| K2 | Frozen rank is mistaken for next executable priority | Use current readiness while preserving all exposures | given-01 | Never prune required slots on negative-now ROI |
| K3 | A procedure dependency is absent | Distinguish optional helper from mandatory empirical gate | pv-01 | Missing human ratings cannot be replaced by a model rating |
| K4 | A creative object gesture is read as an emotion | Vary physical side and possible recipient | unx-01 | Do not add unseen props or assert a person’s emotion |

Actual use after organization: query “a lower-ranked operation is ready while a higher-ranked one has no ratings” retrieves K2+K3 and selects ready work while leaving ADEp open. Query “the next seed’s gesture must mean resignation” retrieves K4 and produces an alternative recipient rather than stronger emotional prose. The source-name query still uses the file names. Those operations use the chosen index, not newly generated rules.

Content review: K1/K2 are local program conduct changes, K3 is current dependency handling, K4 is creative generation within constructed text. None establishes a human outcome. Organization review: all four boundaries remain adjacent; no common super-rule replaces them. No further beneficial change found in these comparisons; global maximality is not established.

Actual mind change: Subsequent routing uses trigger cards, with file names retained for source lookup.

Benefit: Two routing/creative queries and two boundaries were resolved from the retained content; the source-name exception stays visible. No new substantive KEEP is claimed.

Verdict: KEEP

Organization: Trigger access plus a source-name path beats either single view on the declared mixed queries.

Next attempts: Use this index in WSIB; compare uncertainty cards; test a case outside all four triggers.
