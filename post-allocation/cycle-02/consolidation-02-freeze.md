# Consolidation 02 prospective freeze

Intended mind change: Compare two organizations of exactly four demonstrated findings on a new evidence-retrieval and belief-update case.

Actual starting judgment: The four findings are useful separately; I do not know whether source order or a query-to-inference pipeline will produce lower compulsory retrieval with the same boundary preservation.

Freeze time: 2026-09-08T09:32:00Z. No consolidation-02 result exists.

Eligible findings only:

1. `query-history-location-policy` from cycle-01 IPSS/DIFR.
2. `lossless-does-not-determine-retrieval-cost` from cycle-01 MEMK.
3. `proposal-log-versus-bidirectional-trace` from cycle-01 TRACEMATRIX.
4. `evidence-dependence-before-aggregation` from cycle-02 PBR.

Concrete case: A release reviewer must decide whether to replace a component. Prior P(D)=0.10; threshold is posterior >0.50. A canonical register has eight actor sections and 48 total fields. A trigger index maps `current alarm decision` to event representation L2 and `custody/lineage` to trace representation L3. A proposal log contains A1=HIGH and A2=HIGH as separately submitted fields. L3 links both fields to one sensor event E7. The current trace marks both fields accepted-as-displays but does not mark them as independent evidence. A historical query asks which alarm proposals were considered, including rejected or superseded ones.

Organization A: retrieve the four findings by source/application order and apply each in turn.

Organization B: `query → select representation/location → use task index while preserving canonical register → separate proposal history from current trace → resolve evidence lineage/dependence → aggregate → apply decision threshold`.

Success criteria: answer the current decision and historical query; preserve 48/48 canonical fields; count A1/A2 once for inference but twice as proposals; produce posterior and action; retain provenance; test boundary where A2 instead links to independent event E8; compare compulsory finding/artifact accesses without claiming measured human time; preserve an audit path to original findings.

Actual mind change: none yet; the test is frozen.

Benefit: prevents the consolidation from choosing an organization by rewriting its input after seeing outputs.

Verdict: KEEP for prospective integrity, not a new substantive finding.

Organization: one freeze beside the four sources and later result.

Next attempts: execute both organizations; record ties; keep source order if it serves audit better; reject any organization that deletes proposal history or duplicates evidence.
