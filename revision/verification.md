# Verification of the recipe and deduction revision

This record covers preservation of the accepted corpus, the separate recipe import, finite constructions, current navigation, and checkpoint recovery. It does not certify empirical recipe efficacy, human perspective change, or all historical research. The [reconciliation record](reconciliation.md) identifies the concurrent accepted parent.

The finite constructions have been executed with exact fractions and explicit outputs in [deduction-results.json](deduction-results.json). The all-width XOR claim is established by its proof; enumeration of widths one through eight checks the implemented rule and illustrates its boundary. The original imported catalog is preserved byte-for-byte, and its generated views derive from one data file.

The accepted contributor-inventory check passed with all 616 target entries, 62 families, 169 preserved source files, and 31 canonical subject references intact. The original 300 application statuses remain unchanged. The repository validator verified all 300 recorded application hashes and all ten imported artifacts underlying the 25 inquiries and 373 explored options.

Current navigation validation passed for more than 1,400 relative file-link occurrences across the accepted and added entry points. This establishes path availability, not a measured human navigation advantage. Historical snapshots are retained as evidence with their original text; their old contextual links are not current navigation.

The finite constructions passed the selection/impossibility cases, 50 independently calculated signal-matrix endpoint cases, all 510 seeds across XOR widths one through eight, eight third-control cases, mixed-failure repairs, switching-cost schedules, atomic content-mismatch rejection, duplicate READY handling, independent cancellation, and partial-copy threshold checks. The general theorems have accompanying proofs; these finite checks are not substituted for those proofs.

The separately supplied catalog matches its original file byte-for-byte, with SHA-256 `4a22cfc23162617f2a2f5e7c434570956fdd8f8c25ee863dc85d834cd4b324fc`. Its 72 targets, 100 adverse/beneficial attempt cards, 300 variants, and 23 scoped evidence notes are represented in one data file and 22 generated target/attempt views. The active renderer imposes no fixed variant or step count.

The first reconciliation preserved all 1,822 blobs from its accepted tree. A final reconciliation preserved the later 1,888-blob tree, including the 6,000 catalog entries, rankings, and source artifacts. Active integration controls are deliberately modified and documented in the reconciliation record. Repackaging additionally updates the manifest and recovery parts. The original skill ledger and source imports remain unchanged; their integrity is checked by the retained validators.

`python checkpoint.py` builds the complete current native corpus into the repository's existing recovery format. `python checkpoint.py --check` compares all payload bytes, paths, the corpus manifest, and both compatible transport metadata formats. These are the acceptance checks used before publication; [checkpoint metadata](../checkpoint-parts/metadata.json) provides the resulting archive hash and counts. The non-forced Git commit records the published files and their parent.
