Intended mind change: Replace one undifferentiated unresolved label with distinctions that change which observation would resolve a concrete case.

# DD 01 — uncertainty relevant to the next act of interpretation

Starting working judgment: I could accurately label each seed below unresolved. That label did not yet distinguish ambiguous meaning, unavailable evidence, conflicting testimony, or a decision threshold.

Actor: the current model. The concrete examples below are observations where explicitly identified and otherwise stipulated cases; no invented human outcome is attributed to them. Original: ../sources/inquiry-dd.original.md; requirements: ../sources/inquiry-dd.requirements.txt. 8x source floors: 12 dimensions, seven sources/methods, five hidden dimensions, four validation tests. This record has twelve axes, seven distinct discovery operations with outputs, five or more dimensions hidden by the initial coarse representation, and five validation products.

S1: “Continue next time” has session-resume and autonomous-future interpretations.
S2: A named question-route file has not been located.
S3: Two hypothetical measurements report incompatible values for the same stipulated variable.
S4: A stipulated cost interval of 4–9 crosses an action threshold of 6.
S5: A valid conditional inference depends on an unverified premise.
S6: A known effect has an unsettled value for the user.
S7: The needed source is identified but the actor cannot access it now.
S8: A conclusion is settled while its intervention deadline has passed.

Discovery products:

1. Pair comparison: S2 lacks a located item; S3 has items whose contents conflict. Adding another interpretation cannot by itself retrieve S2’s file.
2. Minimal pair: keep a probability interval fixed and move the decision threshold outside it; inferential uncertainty stays fixed while action uncertainty disappears.
3. Universal scan: WHO separates model and human claimant; WHAT supplies target; WHEN supplies expiry; WHERE supplies access; WHY supplies threshold/value; HOW supplies evidence route; HOW MUCH supplies dispersion. Actor is retained as metadata because it does not replace any twelve-axis value.
4. Boundary probe: S8 is epistemically settled yet operationally closed; “uncertain” cannot describe its obstacle without misclassification.
5. Counterfactual inversion: two identical source receipts can be numerous while evidence origins remain one; count and independence separate.
6. Cross-domain transfer: an unverified premise in a proof and an unavailable route file both block an inference, but only the latter is repaired by locating a file.
7. Interaction pruning: “no numeric quantity” makes dispersion inapplicable rather than zero; “no action at stake” makes threshold relation inapplicable.

| # | Dimension | Values | Concrete origin | Hidden relative to starting representation |
|---|---|---|---|---|
| D1 | Target | meaning; fact; decision | S1/S6 contrast | no |
| D2 | Logical status | affirmed; denied; open | S5 conditional versus premise | no |
| D3 | Interpretive multiplicity | single; multiple | S1 competing readings | no |
| D4 | Evidence conflict | absent; present | S2/S3 contrast | no |
| D5 | Numerical dispersion | narrow; wide | S4 interval | no |
| D6 | Provenance identity | identified; unidentified | S2 named source vs unattributed report | yes |
| D7 | Evidence dependence | shared origin; independent origins | two repeated receipts versus two independent observations | yes |
| D8 | Threshold relation | same action throughout; action changes | S4 threshold | yes |
| D9 | Action reversibility | reversible; irreversible | S6 reversible draft vs irreversible act | yes |
| D10 | Access | now; delayed; unavailable | S2/S7 contrast | no |
| D11 | Time sensitivity | stable; expiring | S5/S8 contrast | yes |
| D12 | Resolution burden | low; high | local read vs a fresh longitudinal observation | no |

Nominal finite product: 3 × 3 × 2 × 2 × 2 × 2 × 2 × 2 × 2 × 3 × 2 × 2 = 13,824 combinations. This is an upper bound on the declared value grid, not a count of coherent real situations. Inapplicable fields and coupled constraints prune cells. Actor and provenance stay attached even when they are not an independent search axis.

Validation products:

1. Independence 1: S4’s interval remains 4–9 while threshold moves from 6 to 12; only threshold relation changes.
2. Independence 2: a named source changes from accessible to inaccessible with its content and provenance fixed; access changes alone.
3. Independence 3: two matching statements can derive from one original or two independently obtained observations; agreement remains fixed, dependence changes.
4. Independence 4: the same settled instruction is encountered before or after its deadline; logical status stays fixed, expiry changes.
5. Completeness: S1=multiplicity; S2=provenance/access; S3=conflict; S4=dispersion/threshold; S5=logical-status dependency; S6=target/value; S7=access; S8=time. No seed requires conflating action readiness with truth confidence.

Distinct later application: On three actual next targets, the representation produces different next observations: missing QR data → locate the archive; “continue next time” → preserve session-resume/autonomous alternatives and inspect available continuation capability; matching source-requirements digests → check common origin before treating repeated receipts as independent support. The resulting operation selections are recorded in uncertainty-routing.json. This is a model classification on these stated inputs, not measured human reasoning improvement.

Certificate: The twelve-axis representation distinguishes the stated minimal pairs while retaining each seed. Its successful classifications establish representational distinctions on those inputs. The strongest contrary branch is that a plain unresolved label is sufficient when no action differs; the boundary below preserves that case. No human transfer or globally optimal taxonomy follows.

Actual mind change: The next observation is now conditioned on uncertainty type; a settled truth with expired access is not routed to more truth testing.
Benefit: Three distinct obstacles now receive observations capable of resolving their own missing premise. The scalar unresolved label alone did not specify those observations.
Verdict: KEEP
Organization: Axes are separate from cases and invalid combinations; the later use consumes only dimensions that change its operation. Boundary: When all candidate actions are the same across the unresolved interval, this classification supplies no reason to delay.
Next attempts: Enumerate concrete combinations of these axes; apply the representation to a held-out case; compare a smaller representation that retains the decisive distinctions; test whether another domain requires an unrepresented axis.
