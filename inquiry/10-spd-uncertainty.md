Intended mind change: Expand the concrete candidate space for uncertainty before selecting an intervention or interpretation.

# SPD 03 — uncertainty

Exact input for the required GG invocation: Find concrete changes to uncertainty, confidence, and action thresholds that help this model choose an adequate next observation on a missing-source case, an ambiguous-source-reason case, and an interval-crosses-threshold case. Use 03-dd-uncertainty.md, 06-qr-sure.md, and the stipulated interval 4–9 with threshold 6. Preserve warranted uncertainty where resolution does not change the action.

Starting working judgment: The immediate candidate space was the concrete parent record 03-dd-uncertainty.md. Its preferred continuation did not enumerate the full frames, stakeholders, timing conditions, or adjacent purposes listed here. This is a change to current model search coverage; no human effect is observed.

Original: ../sources/inquiry-spd-resume.original.md; requirements: ../sources/inquiry-spd-resume.requirements.txt. The current reader output matches these verified copies byte for byte. No numeric 8x multiplier is defined in this original. Actual expanded scope: eight framing alternatives, eight stakeholder perspectives, seven time horizons, ten standard and three custom axes, five adjacent spaces, all eight claim types, a calculated grid, priority/boundary regions, the recovered GG library plus source-specific customization, gated QAG questions, an 8x ARAW, and five performed local/stipulated later-use fixtures.

Frames:

- Literal: find what is unknown and what observation would resolve it.
- Metaphorical: doubt marks a search opening rather than a low-confidence score.
- Surface: all three cases are unresolved.
- Deep: missing access, ambiguous meaning, and threshold sensitivity require distinct changes.
- Best case: a small observation settles the actual next decision.
- Worst case: more confidence conceals unchanged missing evidence.
- Likely local need: condition observation selection on the uncertainty type.
- Alternative need: continue a reversible action while preserving unresolved truth.

Stakeholders:

- Model classifier: chooses the next observation.
- User: receives the useful result and its limits.
- Source author: wording and intended scope may differ from reader inference.
- Later consumer: needs to know what remains unknown.
- Human subject: unperformed intervention must remain proposed.
- Reviewer: checks soundness of the confidence-to-action inference.
- Resource provider: determines what can be learned now.
- Affected decision maker: consequences can differ even with the same belief interval.

Time horizons: immediate active choice; next record in this run; later resumed session; repeated use across several records; historical source/decision context; a recurring trigger if actually configured; irreversible or expired opportunities. Each distinguishes current observation from an unperformed future event.

| Axis | Declared values |
|---|---|
| AGENT | model; human; later reader |
| ACTION | observe; infer; defer; preserve |
| OBJECT | meaning; fact; threshold |
| REASON | truth; action; exploration |
| METHOD | read; compare; calculate |
| TIME | now; later; expired |
| LOCATION | local source; remote source; stipulated case |
| DEGREE | single premise; relation; whole decision |
| CERTAINTY | settled; partial; open |
| SCOPE | same case; boundary case; transfer case |
| Missingness | absent; conflicting; ambiguous |
| Action invariance | same throughout interval; threshold crossed |
| Evidence origin | shared; independent |

Adjacent spaces:

- Logic: valid conditional inference and true premises differ.
- Decision analysis: uncertainty can remain while an action is fixed across possible values.
- Source access: failure to find and evidence of absence differ.
- Meaning: one phrase can encode factive and nonfactive senses.
- Experiment design: a discriminating observation must have outcomes that select different live alternatives.

Claim types:

- Factual: the QR source contains a specific reason string.
- Causal: a new representation changes selected observations in this run.
- Predictive: a held-out uncertainty case will map to a suitable observation.
- Normative: unwarranted confidence is not the request’s success criterion.
- Modal: action can be determinate while truth remains unresolved.
- Relational: a threshold partitions the declared interval.
- Intentional: the model seeks useful resolution, not forced agreement.
- Meta: confidence scoring is a choice of representation.

Space estimate: 8 frames × 8 stakeholders × 7 horizons × 944,784 declared dimension combinations = 423,263,232 nominal cells. Overlap, inapplicability, and incompatible states mean this is a grid upper bound, not a count of distinct viable interventions.

Coverage mode for required GG: BOUNDARY. The declared grid greatly exceeds 10,000 cells. Boundary regions: absent versus present trigger/evidence; same versus changed proposition scope; action invariant versus action-sensitive uncertainty. Priority regions: concrete input’s current model operation; its strongest credible ordinary alternative; one boundary where the preferred change is unnecessary or harms access. Skip candidates for this round: assertions of human physiological improvement without a performed intervention; inaccessible future effects without an actual channel. These remain possible targets, not disproven effects.

## Executed GG dependency

The required library lookup returned `domain_design_decisions`. Its recovered immutable source is `../supplement/Recovered_Sources/inquiry-domain_design_decisions.original.md`, SHA-256 `fd18540a4d2c60c29a6cc0fc864ab679ae475db429a89d503d272b69d1a0d22b`. The source header declares 512 guesses and 268 morphological guesses. Direct parsing finds 492 derivation-tagged numbered entries and 248 morphological entries; its actual section totals are 248+70+38+24+52+36+24=492. The original 512 claim is preserved as a rejected source claim, not silently repaired.

`inquiry-resume-spd3-gg-catalog.json` retains all 492 source entries with derivation and polarity/use/orientation tags, then adds 93 source-specific candidates: eight LOCATION entries, fifteen CONSTRAINT entries, twenty-eight entries for the seven missing named analogy domains, six stakeholder perspectives, four historical-horizon entries, twelve entries for six missing pre-mortem types, and twenty critical candidates. The resulting 585 tagged entries are candidate rows, not 585 independent findings or evidence points. Exact and semantic overlaps remain uncredited until tested.

Coverage mode remains BOUNDARY. All ten standard dimensions are checked after LOCATION and CONSTRAINTS are added; seven SCAMPER operations remain intact; thirteen stakeholder perspectives are represented; immediate, short, medium, long, and historical horizons are present; all eight claim types remain present; the ten original named analogy domains are present; and all fifteen original pre-mortem error types are checked. The custom candidates place output-usable source, meaning, threshold, and provenance propositions in the top twenty; eleven are skeptical and nine constructive.

## Executed QAG dependency

`inquiry-resume-spd3-qag.md` gates questions about source access, origin independence, absent-path inference, factive versus report-confidence sure, threshold/action invariance, confidence-only adequacy, operation interchangeability, and later use. Eight questions proceed. Seven stop because they are sideways, scope-changing, authorially unanswerable, human-response dependent, or low leverage. Answers from the local files and stipulated truth tables remain working guesses until the ARAW tests them.

## Executed ARAW dependency

`inquiry-resume-spd3-araw.md` applies original 8x depth: eighteen exact claims, one hundred findings, seven claim-to-bedrock levels, eight cruxes, and five live fixtures. It rejects the propositions that matching copies are independent support, absent expected path entails absent content, every use of sure is factive, .99 confidence entails truth, every unresolved interval prevents action, confidence increase alone makes an observation adequate, one observation type repairs all three cases, candidate volume establishes benefit, and the recovered source actually contains 512 numbered entries. It retains conditional byte identity, checklist coverage without exhaustiveness, and qualifier retrieval only when the dependency changes scope.

Source-contamination control: the access, dependence, missingness, threshold, action-invariance, operation-type, and qualifier claims were frozen in `03-dd-uncertainty.md` and this space map before the generic library was recovered. Library-derived claims are marked. Three conclusions disagree with source-facing shortcuts: 512 is rejected for 492 observed entries; the route reason is not generalized to every use of sure; candidate volume is not treated as benefit.

## Distinct later use

`inquiry-resume-spd3-observation-tests.json` performs five cases.

1. The expected runtime library content path is absent, but the recovered source exists and matches its receipt. The selected operation changes from search to read-and-audit with recovery provenance.
2. A .99-confidence false world separates report-confidence sure from factive sure. The selected operation changes from adjusting one confidence number to retaining and testing two propositions; authorial intent remains unresolved.
3. Under the stipulated rule LOW iff x<6 and HIGH iff x≥6, [4,9] contains both actions. The selected observation asks only whether x is below 6.
4. On the held-out interval [7,9], both endpoints select HIGH. The selected operation acts while preserving the exact value as unresolved rather than requesting precision for its own sake.
5. Two matching receipts with one recorded origin change the next operation from counting two confirmations to seeking a distinct derivation.

These are model operations on current local or stipulated inputs. They do not establish reader understanding, human confidence, learning, bodily change, authorial intent, or durable future behavior.

Actual mind change: Uncertainty no longer routes to a generic confidence increase. Missing bytes route to provenance-bounded retrieval, ambiguous meaning to paired propositions, threshold-sensitive intervals to discriminating observation, action-invariant intervals to action with truth preserved as unresolved, and repeated receipts to origin checking. The recovered library's 512-entry claim is now rejected in favor of the observed 492 candidate entries.
Benefit: Five performed fixtures select different next operations and preserve their limits. The useful change is the typed routing and the prevention of false coverage/support counts, not the volume of generated rows.
Verdict: KEEP within the current model and declared fixtures; authorial intent, human effects, observation cost/expiry beyond the fixtures, and exhaustive possibility coverage remain UNRESOLVED.
Organization: The intact recovered library, custom gap fillers, QAG gates, ARAW findings, and later-use fixtures remain separate. Candidate entries are not findings; access, semantics, threshold, provenance, and actor/effect scope do not collapse into one score.
Next attempts: Test a nonzero observation cost; test an expired opportunity; obtain direct author evidence for the route reason; compare typed routing against a single-confidence baseline on a fourth uncertainty case; reopen coverage if a new obstacle is not representable by the current axes.
