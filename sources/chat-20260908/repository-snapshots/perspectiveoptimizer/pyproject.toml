[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "perspectiveoptimizer"
version = "0.2.0"
description = "Perspective construction, conditional study relevance and transition development"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools]
packages = ["perspectiveoptimizer"]
