Intended mind change: Change allocation of a fixed local effort budget when a preparation action unlocks a better later contribution.

# Decompose a time budget into prerequisites and deliverable benefit

Starting judgment: Given several eligible actions and a 60-unit budget, my provisional schedule is the feasible set with the largest sum of listed direct benefits; preparation with no direct benefit is easy to omit.

Actor and scope: The assistant's current working representation and operations in this systems line. This record does not establish human effects, model-weight changes, or transfer to another session.

Source: `../sources/systems-gd.md`; current requirements are retained separately in `../sources/systems-gd.requirements.txt`.

Depth: The original defines no numerical 8x floor. The expanded execution below covers the actual operations and multiple dependencies/countercases; no numerical 8x certification is asserted.

Original target from the selector: "Judge operations together when one enables, obstructs, or changes another."

Concrete input is a constructed planning instance, not human outcome data. The assistant controls four possible operations: inspect a source S (cost 10, standalone benefit 0); build a precise comparison C (cost 25, benefit 40, requires S); supply a familiar example E (cost 20, benefit 18); supply a generic summary G (cost 15, benefit 8). Numbers are explicit decision inputs in arbitrary work/value units; their real-world magnitudes are unknown.

Expanded goal: select a feasible ordered subset with cost ≤60 that maximizes the supplied value, preserves C's prerequisite, and does not count impossible benefit. A decomposition by outputs [C,E,G] loses S; it labels C+E+G feasible at cost 60 and value 66, although C lacks its prerequisite. A decomposition by capabilities retains S→C plus E and G.

| Subgoal | Enables | Conflict | Action / acceptance |
|---|---|---|---|
| Reliable comparison input | C | consumes 10 | S completed before C |
| Precise comparison | requested distinction | competes with G | C present only with S |
| Accessible concrete example | interpretation of result | effort | E uses remaining budget |
| Preserve cost bound | all feasible plans | scope of embellishment | sum ≤60 |

The full feasible comparison is calculated in `budget-decomposition.json`. S+C+E costs 55 and yields 58. S+C+G costs 50 and yields 48. E+G costs 35 and yields 26. S+C+E+G costs 70 and is infeasible. All 16 subsets are enumerated; the best feasible value is 58. The direct-output decomposition's value 66 is unattainable under the declared prerequisite.

Integrity: S serves C without replacing C; E remains optional; G is dropped by the budget tradeoff, not by changing the original optimization criterion. Achieving all selected subgoals yields the exact selected feasible contribution. The deeper user program still requires all allocated skills; this example allocates operations inside one intervention, not deletion of quota.

Applied choice: the assistant's selected plan for this instance changes from output-only C+E+G to S→C→E. A zero-direct-benefit precursor receives ten units because of its enabled consequence.

Actual mind change: The instance now allocates ten effort units to a source precursor and selects the feasible S→C→E bundle.

Benefit: The revised plan respects the prerequisite and attains the calculated optimum 58 for the supplied finite instance. This establishes a decision correction under stipulated utilities, not a measured human benefit.

Verdict: KEEP

Organization: Capability dependencies explain the chosen bundle; the output list remains useful only after prerequisites are attached.

Next attempts: Replace additive values with overlap; allow source failure; test whether waiting preserves a better bundle.
