Intended mind change: Change which prior conclusions survive a changed resource or permission state.

# Organize changing resources and authority by invalidation trigger

Starting judgment: The current stage index is useful, but a changed state can arrive after any stage; a fresh trigger index is a credible alternative.

Actor and scope: The assistant's current working representation and operations in this systems line. This record does not establish human effects, model-weight changes, or transfer to another session.

Source: `../sources/systems-gd.md`; current requirements are retained separately in `../sources/systems-gd.requirements.txt`.

Depth: The original defines no numerical 8x floor. The expanded execution below covers the actual operations and multiple dependencies/countercases; no numerical 8x certification is asserted.

Only four new keeps enter: fut1's accumulated retention exposure, requirements1's exclusive resource condition, sysarch1's versioned budget packet, sysarch3's description-versus-execution authority contract.

Candidate A appends these beneath the chronological index. Candidate B links each to the condition that invalidates or reopens its present action: increased stock, resource collision, changed budget, changed authority. Candidate C merges all into "recheck constraints"; C loses which historical evidence remains valid. B is selected for changed-state events.

Actual later use: budget falls from 60 to 40 while S+C+E is pending; the changed-budget trigger reopens that packet. Enumeration now selects S+C at cost 35 and supplied value 40. A simultaneous proposal to execute a description-only human action is rejected by the authority trigger. A later retained-entry count change activates fut1's maintenance review condition, without deleting all history. Source observations are preserved.

`architecture-actual-cases.json` supplies the executed recomputation and three authority cases. The first draft's mistaken E+G recomputation was corrected after enumerating feasible bundles; repair success is retained as a limitation and no first-pass accuracy is claimed. This correction adds no new keep: it repairs the earlier source application.

Content review retains supplied-value scope and unknown human effects. Organization review favors invalidation triggers for asynchronous changes, while commitment stage remains useful before dispatch. The two arrangements are complementary, and neither is silently deleted.

Actual mind change: Changed budget and authority now dispatch to distinct preserved conditions; the old packet is re-evaluated with a corrected finite optimum.

Benefit: The subsequent 40-budget and description-only case produces a feasible bundle and blocks unauthorized execution. The arithmetic repair demonstrates local recovery, not first-pass reliability.

Verdict: KEEP

Organization: Trigger index handles changes after a stage has passed; stage order still handles initial assembly.

Next attempts: Test two simultaneous invalidations; compare changed authority with cancelled task; observe whether stock review finds a genuinely obsolete entry.
