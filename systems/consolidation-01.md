Intended mind change: Change the arrangement of the first four kept consequences so a later systems decision can use them without dropping their exceptions.

# Four changes organized by the next decision

Starting judgment: A chronological collection of four records is small enough to read directly; that is a credible ordinary arrangement. Its query entry point is the application name rather than the live decision.

Prior availability: The shared working instruction already contains this substantive requirement. This record demonstrates local uptake into an arrangement, not discovery of a new global rule; integration must not count it again as an independent conceptual finding.

Actor and scope: The assistant's current working representation and operations in this systems line. This record does not establish human effects, model-weight changes, or transfer to another session.

Source: `../sources/systems-gd.md`; current requirements are retained separately in `../sources/systems-gd.requirements.txt`.

Depth: The original defines no numerical 8x floor. The expanded execution below covers the actual operations and multiple dependencies/countercases; no numerical 8x certification is asserted.

Inputs are only the kept consequences of gd1, gsr1, grf1 and lpd1. No new substantive finding is added.

| Later use | Chronological collection | Decision index plus original records |
|---|---|---|
| Choose next execution | Read four titles, then gd1 | `next_execution` resolves gd1 and quota exception |
| Decide whether four keeps exist | Read gsr1 | `consolidation_eligibility` resolves gsr1 and duplicate exception |
| Report completion | Locate grf1 | `completion` resolves grf1 and receipt exception |
| Hand off to root | Locate lpd1 | `handoff` resolves lpd1 and full-record authority |

The chronological candidate preserves complete content. The indexed candidate preserves the same four complete records and adds four decision entries; it costs a small duplicate summary and one extra file. Exact lookup execution in `consolidation-1-check.json` confirms all four entries point to existing records and carry an exception. That test establishes path coverage, not human reading speed or semantic correctness.

Selected arrangement: `decision-index.json` plus chronological provenance. Actual use: the next-execution entry chooses the architecture/dependency work instead of another numerical progress summary. The completion entry keeps remaining source files out of the execution counter. The handoff entry supplies the milestone sent to root. The eligibility entry prevents using this consolidation as a fifth independent original keep.

Content comparison retains each original reason, actor and limitation. Organization comparison favors the decision index for these four exact later uses; chronological ordering remains useful for reconstructing the work. Remaining improvement: as targets diversify, direct decision keys may collide; add a target field only after a real collision appears.

Actual mind change: The four kept consequences are now reached by a decision index that was used to select and report the next work.

Benefit: Four exact later-use queries have complete existing record links and explicit exception text. Faster or better human retrieval remains unmeasured.

Verdict: KEEP

Organization: Decision index plus full records wins these declared lookup cases; chronology remains provenance. The selection has already changed the next work and milestone contract.

Next attempts: Test a target collision; compare a dependency graph at the next batch; check whether one entry should merge or remain separate.
