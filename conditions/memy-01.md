Intended mind change: Replace trying to retain whole scheduling explanations with retrieval cues that recover the relevant condition and its exception.

Starting working judgment: The cases are easy to recognize immediately after writing them. That familiarity does not establish that I can recover the right one when the next question is phrased differently.

Original source: ../sources/conditions-memy.original.md. Exact stdout and separate requirements receipt: ../sources/conditions-memy.requirements.txt; byte checks are in source-receipts.json.

Execution scope and depth: No numerical 8x scale exists. Eight triaged items, six concrete encoding cues, eight retrieval entries, four executed lookups, a connection map, pruning and an explicitly unperformed review design are included.

Triage of eight inputs: continuous-window relation is a core concept; future release/nonpreemption is a core concept; deadline versus mean-wait trade-off is a core concept; the numerical room total 31 is reference data; offline readability before travel is a core concept; pre-exposure baseline edge is a core concept; criterion-precommitment condition is a core concept; the exact 3.355556-second extraction time is an ephemeral performance detail for ordinary reuse and stays only in its evidence record.

Encoding products: “largest window, not sum” is a contrast cue for indivisible attention; “ready does not mean safe to start” links the release case to preserved future opportunity; “deadline before mean” separates the queue criterion from job-count progress; “open it before you leave” is a physical sequence cue, with authorized format access retained; “baseline before exposure” links the response to the information state under which it was obtained. These are text encodings in the agent’s working artifacts, not claims about strengthened neural memory.

Retrieval cues were constructed as actual practical questions in retrieval-index.json. Each entry contains answer, exception and source. Eight entries cover the target distinctions, and four exact cue lookups were executed into retrieval-results.json. Every tested cue returned its intended source and exception. The useful output is not the hit count alone: the future-release lookup also returns the preemption exception, so the next case can remove unnecessary waiting.

Connection map: time feasibility branches into continuous-window and location-window; opportunity preservation links future-release to criterion-first; information-state ordering links baseline-exposure to offline-material; shared-resource distinguishes graph independence from concurrency. These are relations among cases, not all-purpose advice.

Review design: next suitable in-session use will retrieve the question plus exception; a nominal human retention plan would require actual exposure and later recall at 24 hours, three days, one week and one month. None of those future reviews has been scheduled or observed. Pruning: remove the exact extraction runtime from the compact cue store, retain it in the provenance record. Keep the failed immediate-start route as the counterexample attached to future-release.

Retention summary: encoded relations are six scoped text cues; numerical inputs and original procedures are external references; the executed evidence is four current artifact lookups. Durability, unaided recall and human transfer remain unmeasured. The original’s general forgetting-curve claims are not used to infer those outcomes.

Actual mind change: The conditions agent created and actually queried a compact cue store that returns scope and an exception with the answer.

Benefit: The four tested lookups recovered the needed qualification as well as the main rule. This demonstrates external retrieval support in this session, not long-term retention or unassisted learning.

Verdict: KEEP

Organization: Question→answer→exception→source performed better than retaining a paragraph title alone for the four tested retrievals. The full cases remain available, and exact numbers are looked up rather than promoted into universal facts.

Next attempts: Use the future-release exception on a preemptible task; test a query that needs two entries; perform delayed recall only if a later encounter actually occurs.
